# HX_NATIVE-swallowed function definitions — full census (2026-09-14, lane W16-A)

**What this is.** Every function DEFINITION under `src/system`, `src/band3`,
`src/network` whose enclosing preprocessor state includes `HX_NATIVE`. The match
build never defines `HX_NATIVE` (cflags carry exactly two `/D`s — `/DCURL_STATICLIB`
and `/D_XBOX360`), so **every body listed here is compiled out of the match build
entirely**. The build COMPILES but never LINKS, so an undefined symbol is invisible:
a swallowed body shows up only as a retail row at `fuzzy 0` (unpaired, nothing to
pair with) or as a wrong-callee name charge at each of its callers.

Generated by `tools/hx_native_swallowed_census.py` (`--scan` + `--classify`);
`--selftest` proves the analyzer can FAIL (see "The instrument" below).

## Counts

| class | rows | meaning |
|---|---:|---|
| **RETAIL_HAS_IT** | 1 | retail defines it; un-gate and port |
| **DEFINED_ANYWAY** | 29 | the gate is not actually swallowing the symbol: another TU defines it un-gated |
| **UNKNOWN** | 26 | ctor / dtor / `operator` — the probe cannot construct the mangled name (see below) |
| **NATIVE_ONLY** | 160 | no retail evidence in any of the three channels — leave gated, do NOT re-hunt |
| **total** | 216 | 3,324 files scanned, 0 preprocessor-balance errors |

## Bytes moved

| commit | fix | predicted | measured |
|---|---|---|---|
| `0663aef4` | un-gate `FakeSongMgr::MidiFullPath` (the one RETAIL_HAS_IT row) | +1 fn / +64 B, or +0/+0 | **+1 fn / +64 B**; unit `default/system/utl/FakeSongMgr` 3/4 → **4/4**, 236/300 → **300/300 B** |

The census's *direct* yield is therefore one function. That is the honest number
and it is the point of writing this file down: the vein is **drained**, and the
remaining 160 NATIVE_ONLY rows are listed below precisely so the next lane does
not spend its budget re-deriving that.

(Lane W16-A's other commit, `99ea4e82` — **+5 fns / +364 B** — came from the
*adjacent* vein this census exposed, not from an `HX_NATIVE` gate: see
"The higher-yield adjacent vein".)

## The one RETAIL_HAS_IT row

- `FakeSongMgr::MidiFullPath` — src/system/utl/FakeSongMgr.cpp:30 — retail symbol `?MidiFullPath@FakeSongMgr@@SAPBDPBVSongInfo@@@Z`.

Retail has it, so the `#ifdef HX_NATIVE` around it was simply wrong. Fixed by
deleting the gate (the body is unconditional now), **not** by blanket-removing
`#ifdef HX_NATIVE` — CLAUDE.md's measured whole-binary control for blanket
removal of the sibling `MILO_DEBUG` guards is **−21**.

## Why UNKNOWN is 26 rows and why it is NOT a backlog

`mangle_probe()` builds an MSVC mangled-name prefix (`?Method@Class@@`) to look a
symbol up in `scripts/target_symbol_map.json` / `report.json`. Constructors,
destructors and `operator`s mangle as `??0` / `??1` / `??_E` / `??_G` / `??2` /
`??3` with no derivable prefix, so the probe declines rather than guessing.

**These were checked by hand and yield 0 retail-undefined hits.** 19 of the 26 are
`operator new` / `operator delete` overloads in `utl/MemMgr.h`, `utl/PoolAlloc.h`,
`utl/AllocInfo.cpp` and `utl/MemTracker.cpp` — the native allocator-tracking
overloads, which retail (shipping the untracked allocator) genuinely lacks.

| symbol | site |
|---|---|
| `VocalPlayer::VocalPlayer` | src/band3/game/VocalPlayer.cpp:101 |
| `OvershellProfileProvider::~OvershellProfileProvider` | src/band3/meta_band/OvershellSlot.cpp:2168 |
| `DrumMap::DrumMap` | src/system/beatmatch/DrumMap.cpp:7 |
| `DataNode::operator==` | src/system/obj/DataNode.cpp:777 |
| `Task::~Task` | src/system/obj/Task.cpp:28 |
| `RndAmbientOcclusion::Edge::operator<` | src/system/rndobj/AmbientOcclusion.cpp:31 |
| `RndMultiMeshProxy::RndMultiMeshProxy` | src/system/rndobj/MultiMeshProxy.cpp:7 |
| `AllocInfo::operatornew` | src/system/utl/AllocInfo.cpp:54 |
| `Loader::Loader` | src/system/utl/Loader.cpp:51 |
| `operatornew` | src/system/utl/MemMgr.h:302 |
| `operatornew` | src/system/utl/MemMgr.h:304 |
| `operatordelete` | src/system/utl/MemMgr.h:305 |
| `operatornew` | src/system/utl/MemMgr.h:317 |
| `operatornew` | src/system/utl/MemMgr.h:319 |
| `operatordelete` | src/system/utl/MemMgr.h:320 |
| `operatornew` | src/system/utl/MemMgr.h:430 |
| `operatornew` | src/system/utl/MemMgr.h:432 |
| `operatordelete` | src/system/utl/MemMgr.h:436 |
| `MemTracker::operatornew` | src/system/utl/MemTracker.cpp:169 |
| `operatornew` | src/system/utl/PoolAlloc.h:93 |
| `operatornew` | src/system/utl/PoolAlloc.h:95 |
| `operatordelete` | src/system/utl/PoolAlloc.h:96 |
| `operatornew` | src/system/utl/PoolAlloc.h:113 |
| `operatornew` | src/system/utl/PoolAlloc.h:113 |
| `String::operator+=` | src/system/utl/Str.cpp:359 |
| `WorldCrowd3DCharHandle::WorldCrowd3DCharHandle` | src/system/world/Crowd3DCharHandle.cpp:5 |

## DEFINED_ANYWAY (29) — the gate is not swallowing anything

For these the mangled name IS defined by one of our compiled objects, from some
other un-gated site. The gate is inert for the metric. Listed for completeness
only; no action.

| symbol | gated site |
|---|---|
| `Player::GetSongMs` | src/band3/game/Player.cpp:304 |
| `MakeBSPTree` | src/system/math/Geo.cpp:1197 |
| `BinkMovieSys::PlatformInit` | src/system/moviebink/BinkMovieSys.cpp:119 |
| `DataThisPtr::Replace` | src/system/obj/DataFunc.cpp:1561 |
| `Hmx::Object::Copy` | src/system/obj/Object.cpp:378 |
| `Hmx::Object::SetProperty` | src/system/obj/Object.cpp:933 |
| `ArkFile::Read` | src/system/os/ArkFile.cpp:17 |
| `SystemConfig` | src/system/os/System.cpp:122 |
| `SystemConfig` | src/system/os/System.cpp:154 |
| `SystemConfig` | src/system/os/System.cpp:159 |
| `SystemConfig` | src/system/os/System.cpp:162 |
| `NgPostProc::DoVelocity` | src/system/rndobj/PostProc_NG.cpp:278 |
| `ByteGrinder::GrindArray` | src/system/synth/ByteGrinder.cpp:913 |
| `StandardStream::ConsumeData` | src/system/synth/StandardStream.cpp:762 |
| `VorbisReader::DoFileRead` | src/system/synth/VorbisReader.cpp:517 |
| `ValidateThreadId` | src/system/utl/MakeString.cpp:43 |
| `MemFree` | src/system/utl/MemMgr.cpp:135 |
| `WorldReflection::Highlight` | src/system/world/Reflection.cpp:159 |
| `NgSpotlightDrawer::SetupFogDensityMap` | src/system/world/SpotlightDrawer_NG.cpp:122 |
| `NgSpotlightDrawer::RenderFogProxy` | src/system/world/SpotlightDrawer_NG.cpp:123 |
| `NgSpotlightDrawer::RenderSphere` | src/system/world/SpotlightDrawer_NG.cpp:124 |
| `NgSpotlightDrawer::RenderSheet` | src/system/world/SpotlightDrawer_NG.cpp:125 |
| `NgSpotlightDrawer::SetupXSection` | src/system/world/SpotlightDrawer_NG.cpp:127 |
| `NgSpotlightDrawer::RenderConeDefs` | src/system/world/SpotlightDrawer_NG.cpp:128 |
| `NgSpotlightDrawer::RenderBeams` | src/system/world/SpotlightDrawer_NG.cpp:130 |
| `NgSpotlightDrawer::CheckCam` | src/system/world/SpotlightDrawer_NG.cpp:131 |
| `NgSpotlightDrawer::BlurRT` | src/system/world/SpotlightDrawer_NG.cpp:132 |
| `NgSpotlightDrawer::SetupForPostProcess` | src/system/world/SpotlightDrawer_NG.cpp:133 |
| `NgSpotlightDrawer::RenderScene` | src/system/world/SpotlightDrawer_NG.cpp:134 |

## NATIVE_ONLY (160) — DO NOT RE-HUNT

No evidence in any of the three channels: (a) no named row in
`scripts/target_symbol_map.json` or `report.json` for the mangled name, (b) no
retail call site whose callee our objects cannot define, (c) rb3-Wii / dc3 do not
define it un-gated. These are native-host code: Bink movie playback, the native
gesture/depth-camera path, `MILO_ASSERT`/debug plumbing, the tracked allocator,
and the native `os/` layer.

Concentration by directory:

| rows | directory |
|---:|---|
| 24 | `src/system/moviebink` |
| 23 | `src/system/obj` |
| 17 | `src/band3/meta_band` |
| 17 | `src/system/os` |
| 17 | `src/system/utl` |
| 9 | `src/system/gesture` |
| 9 | `src/system/hamobj` |
| 8 | `src/system/synth` |
| 7 | `src/system/rndobj` |
| 6 | `src/system/ui` |
| 6 | `src/system/world` |
| 5 | `src/system/char` |
| 4 | `src/band3/game` |
| 4 | `src/system/meta` |
| 3 | `src/system/bandobj` |
| 1 | `src/system/movie` |

Full list, grouped by file:

- **`src/band3/game/Band.cpp`** (1)
  - `Band::NativeLoadBonuses` :35
- **`src/band3/game/GameMicManager.cpp`** (1)
  - `GameMicManager::InitFakeMics` :240
- **`src/band3/game/GamePanel.cpp`** (1)
  - `PresenceMgr::SetSongID` :686
- **`src/band3/game/Player.cpp`** (1)
  - `Player::NativeInitParams` :1038
- **`src/band3/meta_band/BandProfile.cpp`** (1)
  - `BandProfile::GetPictureTex` :965
- **`src/band3/meta_band/BandUI.cpp`** (2)
  - `BandUI::WriteToVignetteOverlay` :632
  - `BandUI::UpdateUIOverlay` :639
- **`src/band3/meta_band/InterstitialMgr.cpp`** (1)
  - `InterstitialMgr::PrintOverlay` :133
- **`src/band3/meta_band/OvershellSlot.cpp`** (12)
  - `END_HANDLERSOvershellProfileProvider::OvershellProfileProvider` :1991
  - `OvershellProfileProvider::Text` :2169
  - `OvershellProfileProvider::Mat` :2170
  - `OvershellProfileProvider::NumData` :2171
  - `OvershellProfileProvider::Handle` :2172
  - `WiiProfileActResultOvershellProfileProvider::ActOnProfile` :2173
  - `OvershellProfileProvider::ActOnProfileConfirmed` :2177
  - `OvershellProfileProvider::SetWiiProfileListMode` :2180
  - `OvershellProfileProvider::WiiProfileListModeOvershellProfileProvider::GetWiiProfileListMode` :2181
  - `OvershellProfileProvider::GetWiiProfileCount` :2185
  - `OvershellProfileProvider::Reload` :2186
  - `OvershellProfileProvider::GetWiiProfileSelectedName` :2187
- **`src/band3/meta_band/SongSelectPanel.cpp`** (1)
  - `SongSelectPanel::SetMiniLeaderboardGroupShowing` :69
- **`src/system/bandobj/BandCharacter.cpp`** (2)
  - `BandCharacter::RebindOutfitBonesToOwnSkeleton` :803
  - `BandCharacter::RebindSharedSkinMatsToOwn` :1151
- **`src/system/bandobj/OutfitConfig.cpp`** (1)
  - `END_CUSTOM_PROPSYNCBEGIN_CUSTOM_PROPSYNC` :1398
- **`src/system/char/CharBonesSamples.cpp`** (3)
  - `CharBonesSamples::NativePosElemWidth` :76
  - `CharBonesSamples::NativeQuatElemWidth` :79
  - `CharBonesSamples::NativeRotElemWidth` :83
- **`src/system/char/CharDriver.cpp`** (1)
  - `CharDriver::SetClipWeightMap` :327
- **`src/system/char/FileMerger.cpp`** (1)
  - `FileMerger::ForceReleaseOrganizer` :358
- **`src/system/gesture/DepthBuffer3D.cpp`** (4)
  - `DepthBuffer3D::DrawShowing` :185
  - `DepthBuffer3D::Save` :188
  - `DepthBuffer3D::Copy` :189
  - `DepthBuffer3D::Load` :190
- **`src/system/gesture/LiveCameraInput.cpp`** (2)
  - `OnCameraDumpUnique` :1134
  - `OnCameraDebugDepth` :1138
- **`src/system/gesture/SpeechMgr.cpp`** (1)
  - `mbstowcs_s` :15
- **`src/system/gesture/StreamRenderer.cpp`** (2)
  - `StreamRenderer::DrawToTexture` :279
  - `StreamRenderer::SetCrewPhotoPlayerCenters` :283
- **`src/system/hamobj/FreestyleMoveRecorder.cpp`** (7)
  - `__fsel` :22
  - `FreestyleMoveRecorder::Poll` :152
  - `FreestyleMoveRecorder::DrawDebug` :319
  - `FreestyleMoveRecorder::StartRecording` :366
  - `FreestyleMoveRecorder::StartRecordingDancerTake` :367
  - `FreestyleMoveRecorder::StopRecording` :368
  - `FreestyleMoveRecorder::ReadFreestyleMoveClip` :470
- **`src/system/hamobj/HamCharacter.cpp`** (1)
  - `HamCharacter::Poll` :908
- **`src/system/hamobj/HamDirector.cpp`** (1)
  - `HamDirector_NativeSetFrameCount` :88
- **`src/system/meta/Achievements.cpp`** (3)
  - `Achievements::PlatformInit` :38
  - `Achievements::GetAchievementData` :42
  - `Achievements::SubmitAchievementsFunc` :48
- **`src/system/meta/CreditsPanel.cpp`** (1)
  - `CreditsPanel::DebugToggleAutoScroll` :185
- **`src/system/movie/TexMovie.cpp`** (1)
  - `SYNC_PROP` :77
- **`src/system/moviebink/BinkMovieImpl.cpp`** (19)
  - `BinkMovieImpl::SetWidthHeight` :113
  - `BinkMovieImpl::Ready` :114
  - `BinkMovieImpl::BeginFromFile` :115
  - `BinkMovieImpl::Draw` :120
  - `BinkMovieImpl::Poll` :121
  - `BinkMovieImpl::Save` :122
  - `BinkMovieImpl::End` :123
  - `BinkMovieImpl::IsOpen` :124
  - `BinkMovieImpl::IsLoading` :125
  - `BinkMovieImpl::CheckOpen` :126
  - `BinkMovieImpl::SetPaused` :127
  - `BinkMovieImpl::UnlockThread` :128
  - `BinkMovieImpl::LockThread` :129
  - `BinkMovieImpl::GetFrame` :130
  - `BinkMovieImpl::MsPerFrame` :131
  - `BinkMovieImpl::NumFrames` :132
  - `BinkMovieImpl::SetVolume` :133
  - `BinkMovieImpl::Terminate` :134
  - `BinkMovieImpl::PlatformCacheFile` :135
- **`src/system/moviebink/BinkMovieSys.cpp`** (5)
  - `BinkClose` :121
  - `BinkCloseTrack` :122
  - `BinkGetTrackData` :123
  - `BinkNextFrame` :124
  - `BinkOpenTrack` :125
- **`src/system/obj/DataArray.cpp`** (7)
  - `InternContextPath` :31
  - `DataArray_InitDtaTrace` :41
  - `DataArray_InitDtaValidate` :48
  - `ResolveContextPath` :57
  - `DataArray_ValidateAccess` :82
  - `DataArray_ValidateFindArray` :104
  - `DataArray_LogAccess` :125
- **`src/system/obj/Dir.cpp`** (3)
  - `PopRev` :74
  - `HasExternalDirPtrs` :97
  - `ShouldSkipCascadeNullify` :115
- **`src/system/obj/Dir.h`** (3)
  - `DirPtrRefCounts` :21
  - `MiloDebugChooseModeEnabled` :36
  - `MiloDebugChooseModePath` :45
- **`src/system/obj/ObjMacros.h`** (1)
  - `objType::Handle` :91
- **`src/system/obj/Object.cpp`** (6)
  - `SnapshotRing` :32
  - `ObjRef::ReplaceList` :57
  - `ObjRefSeedRestoreEnabled` :572
  - `Hmx::Object::NullifyAllRefs` :578
  - `GetNextPropPath` :747
  - `Hmx::Object::SetNote` :1037
- **`src/system/obj/Object.h`** (1)
  - `RefPtrOf` :277
- **`src/system/obj/Task.cpp`** (2)
  - `LiveTasks` :15
  - `Task::IsLive` :32
- **`src/system/os/ArkFile.cpp`** (1)
  - `ArkFile::ReadAsync` :96
- **`src/system/os/File.cpp`** (3)
  - `NativeDirExists` :73
  - `NativeSetCanonicalPath` :78
  - `NativeInitSystemRoot` :91
- **`src/system/os/HolmesClient.cpp`** (1)
  - `HolmesClientPrint` :879
- **`src/system/os/PlatformMgr.cpp`** (1)
  - `PlatformMgr::OnSignInUsers` :185
- **`src/system/os/System.cpp`** (3)
  - `EmptyConfigSection` :140
  - `SafeFind` :144
  - `HongKongExceptionMet` :747
- **`src/system/os/Timer.h`** (5)
  - `__mftb` :9
  - `__loadwordbytereverse` :15
  - `__loadshortbytereverse` :20
  - `__storewordbytereverse` :25
  - `__storeshortbytereverse` :29
- **`src/system/os/VirtualKeyboard.cpp`** (3)
  - `VirtualKeyboard::PlatformPoll` :50
  - `VirtualKeyboard::GetInputString` :54
  - `VirtualKeyboard::ShowKeyboardUI` :58
- **`src/system/rndobj/Console.cpp`** (1)
  - `HolmesClientSendMessage` :17
- **`src/system/rndobj/Part.cpp`** (3)
  - `RndParticleSys::SetTileHoldTime` :1007
  - `RndParticleSys::SetNumTiles` :1014
  - `RndParticleSys::SetAnimatedUV` :1048
- **`src/system/rndobj/Rnd.cpp`** (1)
  - `Rnd::OnToggleWatch` :992
- **`src/system/rndobj/Tex.cpp`** (1)
  - `SYNC_PROP_SET` :43
- **`src/system/rndobj/Utl.cpp`** (1)
  - `CreateAndSetMetaMat` :337
- **`src/system/synth/ByteGrinder.cpp`** (1)
  - `magicNumberGeneratorNative` :959
- **`src/system/synth/EQEffect.cpp`** (1)
  - `__fsel` :7
- **`src/system/synth/MidiInstrument.cpp`** (1)
  - `MidiInstrument::SynthPoll` :194
- **`src/system/synth/SampleInst.cpp`** (3)
  - `SampleInst::SetEventReceiver` :97
  - `SampleInst::EndLoop` :99
  - `SampleInst::SynthPoll` :123
- **`src/system/synth/VorbisReader.cpp`** (2)
  - `Decrypt` :479
  - `VorbisReader::Poll` :545
- **`src/system/ui/PanelDir.cpp`** (1)
  - `SYNC_PROP` :191
- **`src/system/ui/UI.cpp`** (1)
  - `DebugUIFlow` :57
- **`src/system/ui/UIListSlot.cpp`** (1)
  - `UIListSlot::EnsureElements` :92
- **`src/system/ui/UIPanel.cpp`** (2)
  - `DebugUIFlow` :17
  - `DebugSharedPanelDirs` :23
- **`src/system/ui/UIScreen.cpp`** (1)
  - `DebugUIFlow` :26
- **`src/system/utl/BinkIntegration.cpp`** (6)
  - `ReadFunc` :89
  - `BinkFree` :90
  - `BinkFileReadHeader` :91
  - `BinkFileReadFrame` :92
  - `BinkSetMemory` :93
  - `BinkSetIO` :94
- **`src/system/utl/DebugGraph.cpp`** (1)
  - `__fsel` :5
- **`src/system/utl/HxGuid.cpp`** (1)
  - `HxGuid::SaveSize` :71
- **`src/system/utl/Song.cpp`** (1)
  - `Song::PollAsyncState` :121
- **`src/system/utl/SongInfoCopy.cpp`** (7)
  - `SongInfoCopy::GetNumVocalParts` :126
  - `SongInfoCopy::GetHopoThreshold` :128
  - `SongInfoCopy::GetCrowdChannels` :130
  - `SongInfoCopy::GetDrumSoloSamples` :132
  - `SongInfoCopy::GetDrumFreestyleSamples` :136
  - `SongInfoCopy::GetMuteVolume` :140
  - `SongInfoCopy::GetVocalMuteVolume` :142
- **`src/system/utl/Str.h`** (1)
  - `strieq` :19
- **`src/system/world/CameraShot.cpp`** (1)
  - `END_CUSTOM_PROPSYNCif` :926
- **`src/system/world/Crowd.cpp`** (1)
  - `__fsel` :40
- **`src/system/world/Crowd3DCharHandle.cpp`** (1)
  - `WorldCrowd3DCharHandle::SyncProperty` :56
- **`src/system/world/Spotlight.cpp`** (1)
  - `__fsel` :38
- **`src/system/world/SpotlightDrawer_NG.cpp`** (2)
  - `NgSpotlightDrawer::CheckRTs` :126
  - `NgSpotlightDrawer::RenderCone` :129

## The instrument, and how it is proved capable of failing

`tools/hx_native_swallowed_census.py --selftest` runs a fixture through the real
analyzer **and through two deliberately sabotaged analyzers**, and FAILS if either
sabotage reproduces the correct answer. A census tool that confirms whatever you
point it at is worth nothing, so the sabotages have to discriminate:

1. **`comment-blind`** — `strip_comments()` disabled. The fixture contains an
   **unbalanced** `#ifdef HX_NATIVE` inside a `/* */` block with no matching
   `#endif`. This is the real `ScatterIncludes.cmake` shape from CLAUDE.md, where a
   *comment-only* commit (`6c087cbd`) broke the native link because the matcher saw
   `#if` anywhere on a line. ⚠ My first version of this sabotage was **VACUOUS** —
   the in-comment directive was a balanced `#ifdef`/`#endif` pair, net-zero effect,
   so the sabotaged analyzer produced the right answer and the selftest could not
   fail. The unbalanced form is load-bearing.
2. **`directive-unanchored`** — `DIRECTIVE` matched anywhere on a line instead of
   at `^[ \t]*#`.

Plus an assertion that the `#if`/`#endif` balance check fires at EOF on a
deliberately unbalanced input.

Two genuine defects were found *by* getting the selftest to discriminate, and both
would have corrupted the real census:

- `buf_start_line` tracked "first line after the last `;`", which **drifts upward**
  because comment stripping appends spaces to the buffer.
- A declarator's start can legitimately **precede** the `#ifdef`. The swallowed
  test is therefore anchored at the **body brace line**, not at the declarator
  start — `find_definitions()` returns `(decl_line, name, declarator, body_line)`.

Two classification defects were also found and fixed:

- `target_symbol_map.json` has **list-valued** entries (`TypeError: unhashable
  type: 'list'`).
- It also carries **multi-KB prose note values**, and a bare substring probe for
  `?MemFree@@` matched *inside one of those notes*, manufacturing a false
  RETAIL_HAS_IT. Fixed with a symbol-shape filter
  (`^[?_A-Za-z@$][A-Za-z0-9_?@$.<>,~\-]*$`); RETAIL_HAS_IT went 2 → 1.

⚠ **Known limit, stated rather than hidden:** 56 files desynced the brace tracker
(templates and macro-heavy headers). No RETAIL_HAS_IT row came from a
brace-desynced file — that was checked — but a swallowed definition inside one of
those 56 could have been missed. The census is a **floor**, not a ceiling.

## The higher-yield adjacent vein (found by this census, not yet drained)

Building the instrument required the complement set: **retail rows at `fuzzy 0`, in
source-bearing units, whose name none of our 1,206 objects defines** (458,091
defined symbols read straight out of the COFF symbol tables).

That set has **198 rows**, of which **14 have source in-tree**. `HX_NATIVE` gating
is only one of the reasons a definition can be missing; "in-tree but wired into no
build at all" is a bigger one, and it is invisible to `objects.json` inspection
because `tools/project.py` drops a missing compile edge **silently**.

`src/system/rnddx9/Tex.cpp` was exactly that — in-tree, absent from
`objects.json`, `#include`d by no compiled TU. Scatter-including it into
`ShaderMgr.cpp` (retail's own TU grouping; every `DxTex` address the map names
falls inside the pinned `.text 0x82733CB0-0x82736EE8`) paid **+5 fns / +364 B**
in `99ea4e82`.

Sized and left on the table, with evidence:

- ✅ **DONE — `?DrawRect@DxRnd@@UAAXABVRect@Hmx@@PAVRndMat@@W4ShaderType@@ABVColor@3@PBV63@4@Z`,
  1,512 B, ported in `2f7d50a4`.** See "The DrawRect port" below: **+1 fn / +0 B**,
  row now fuzzy **99.64286** / mpn **100.0**, residual is a pure register
  permutation.
- `?DoCompress@DxTex@@QAAXPAX@Z` — **284 B**, declared in `rnddx9/Tex.h`, defined
  nowhere.
- `?TexelsPitch@DxTex@@UBAIXZ` — **80 B**, now pairs after `99ea4e82` but sits at
  fuzzy 99.750.
- `?SyncEffectParams@FxSendReverb360@@` — **3,280 B**; `FxSendReverb360.cpp` does
  not exist in-tree at all (write, don't un-gate).
- `FxSendSynapse360.cpp` (256 B) / `FxSendPitchShift360.cpp` (277 B) — unwired, but
  contain only empty stubs against retail bodies of 72/356 B. Wiring buys pairing
  and **zero bytes**. Deprioritized deliberately.

⚠ **Economics warning before anyone funds the above.** Under the shipped
`name_check` ruler, giving an object a local definition converts a *forgiven
placeholder* relocation site into a *checked* one at every caller. Adding a
definition is a **bet at the call sites, not a freebie** — observed live in this
lane as a **−40 B** regression (`fn_82736B14`, 40 B, fell 100 → 99.500) inside an
otherwise +404 B change. Reconcile crossed-vs-net by set-diff; do not round the gap
off.

## Correction to the lane brief

The brief states **17** non-XDK `.cpp` files are neither declared in
`objects.json` nor `#include`d by any compiled TU. Re-derived here: **16**.
Not load-bearing for any conclusion, but the figure is now measured twice and
disagrees — re-derive it rather than inheriting either number.


## The DrawRect port (`2f7d50a4`) — and the residual nobody should re-hunt blind

`DxRnd::DrawRect` (6-param) was **declared** at `src/system/rnddx9/Rnd.h:57` and
**defined nowhere in the tree**; the 5-param overload at `Rnd.cpp:44` forwards to
it. Retail's 1,512 B row therefore read **fuzzy 0 / mpn 0** — unpaired, nothing to
pair with. Ported from the DC3 oracle (`../dc3-decomp/src/system/rnddx9/Rnd.cpp:452`),
which is matched there. Every dependency was checked to exist in our tree with
identical spellings *before* writing — DC3 is the newer engine, so a missing member
is the expected failure mode. `#include "rndobj/Cam.h"` had to be added.

| | |
|---|---|
| predicted | +1 fn / +1,512 B (byte-exact) **or** +0 fns / +0 B (partial) |
| **measured** | **+1 fn / +0 B** — ⚠ **a MISS**: a combination I did not pre-register |
| row | fuzzy **0 → 99.64286**, mpn **0 → 100.0**, 378/378 instructions |

⚠ **The miss is the lesson, and it is a targeting lesson.** `mpn` excludes arg-only
penalties; `matched_code` keys on `fuzzy == 100` and is **all-or-nothing per row**.
So a body port can cross into `matched_functions` while yielding **exactly zero
bytes**. That is not an exotic case — it is the single most common landing spot for
a body port, and it belongs in every pre-registration as a third outcome.

### What the retail bytes show

All 25 charged sites are `diff_arg` **register renumbering** — zero relocation-name
charges, zero instruction differences:

| value | retail | ours |
|---|---|---|
| `mat` | r28 | r27 |
| `this` | r27 | r26 |
| `shader` | r25 | r24 |
| `shaderMgr` (`TheShaderMgr` ptr) | **r24** (allocated last) | **r28** (allocated first) |

Our `RndShaderMgr &shaderMgr` binding takes the **highest** callee-saved slot and
pushes all four parameters down by one. Retail gives it the **lowest**, i.e. the
shortest / latest live range.

### What was tried

**Variant B — drop the local reference, call `TheShaderMgr` directly.** Pre-registered
as: should stop it holding a long-lived callee-saved slot ⇒ fuzzy 100. **Measured
WORSE and reverted**: fuzzy 99.64286 → **99.33334**, mpn 100.0 → **99.46561**,
`matched_functions` 42,879 → **42,878** (the +1 fn is lost). ⇒ **DC3's annotation
that the local reference is load-bearing holds for RB3 too**, which is worth
recording: the oracle's *codegen* comments transferred even though its register
*numbering* did not.

### The untested hypothesis

Retail materialises the `TheShaderMgr` pointer **later** than we do. Variant B moved
it in the wrong direction (earlier, by reloading), and hoisting to the top of the
function — the other placement DC3 documents — moves it earlier still, so both
obvious placements are the wrong sign. The untested hypothesis is that the ordering
of the two adjacent calls (`SetVConstant` then `SetTransform`) is what pins the live
range, and that retail's order differs; testing it means changing call order, which
is a **behavioural** change and was not attempted on a row that is already at
mpn 100.

⛔ **Do NOT re-open this as a hand register-permutation sweep.** The permuter is OFF
by standing user directive, and CLAUDE.md records hand permutation as drained
(12+ byte-identical variants across 4 functions, two zero-gain beam sweeps).
`REGISTER_SWAP` is a symptom, not a diagnosis — but here `mpn == 100` with 378/378
instructions equal, which is the shape CLAUDE.md describes as *structurally* pure
regalloc rather than a masked source defect. The 1,512 B is collectable only by
whatever makes the allocator agree.
