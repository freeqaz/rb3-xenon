# Tiers 4 and 5 of the native-linked body stratum — lane W6-C, 2026-09-11

Branch `w6-tier45`, worktree `~/tmp/wt-w6-c`, based on main `50fd112d`
(asserted ancestor). Ruler: `name_check` (graded), read from `report.json`
`provenance.diff_config` — not assumed. Baseline at this lane's first full
build (full build first; the reflinked target objs are pre-renamer):

```
matched_functions   42,658
matched_code        3,851,188 B
matched_code_percent  37.587395
fuzzy_match_percent   49.064290
total_code          10,245,956
total_functions     69,219
masked_equal        22,928
```

`verify_objs_patched.py --verify-manifest` → `OK: 1205 decomp, 3085 target
objects match` (rc=0) before any edit.

## 1. Population — regenerated, not inherited

`tools/native_linked_tus.py` over a freshly configured `native/build`:
**474** `src/` TUs are emitted into at least one native target (W4-A measured
471 at its base; main has moved). Of those, **418** map to an `objdiff.json`
unit via `metadata.source_path`; 56 are compiled but unpinned and therefore
not auditable.

Rows are the graded sub-100 named rows of those 418 units, split by charged
sites exactly as W4-A defined the stratum (`tools/crossing_worklist.py`
`diff_many` + `classify_arms`; 1,878 rows diffed, **0 cache misses**).

| population (418 native units) | rows | bytes |
|---|---:|---:|
| named rows with `0 < fuzzy < 100` | 1,878 | 645,248 |
| … 1–3 charged sites (W3-A's stratum) | 1,248 | 273,792 |
| … **> 3 charged sites (the body stratum)** | **630** | **371,456** |
| … 0 charged sites | 0 | 0 |

By native-path tier, using W4-A's tier predicate verbatim:

| tier | rows | bytes | W4-A's figure |
|---|---:|---:|---|
| T1 loading/DTA/utl/obj/os/beatmatch/midi | 121 | 60,036 | 128 / 62,316 |
| T2 game scoring / bandtrack | 37 | 25,964 | 37 / 25,964 |
| T3 `synth/` | 5 | 1,288 | 5 / 1,288 |
| **T4 rndobj/char/world/bandobj/math/flow** | **413** | **265,668** | 413 / 265,492 |
| **T5 ui/meta_band/meta/hamobj** | **54** | **18,500** | 51 / 17,884 |

⚠ **The brief's "71 % of its bytes" is wrong in both lanes' favour-neutral
direction and was tested literally before being used.** T4+T5 is
**284,168 / 371,456 = 76.5 %** of the stratum here, and **283,376 / 372,944
= 76.0 %** on W4-A's own numbers. The tier *counts* reproduce almost exactly
(413 / 51→54), so the drift is real main movement, not a different predicate.

(filled in by the rest of this lane)
