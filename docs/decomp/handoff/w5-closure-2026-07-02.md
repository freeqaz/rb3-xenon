# Wave-5 Lander Closure — 2026-07-02

Landed the 5 CLEARED wave-5 lanes sequentially onto main per the harvest SOP.
All post-land gates passed. **Net +15 matched functions, 0 losses.**

## Final numbers
- `measures.matched_functions`: **10918 → 10933 (+15)** (two stable fresh_report.sh runs; 2nd run had no warm-cache WARN).
- Matched-name A/B (`match_percent_normalized >= 99.99`):
  - vs pre-land baseline (this session start): **+15 gained, 0 lost**.
  - vs session baseline `/tmp/w5_baseline_names.txt`: **+116 gained, 0 lost** (the extra +101 is concurrent OWNER activity between the snapshot and now — all gains, no losses).

## Per-lane outcome
| Lane | TU | Unit report | Strict Δ | Notes |
|---|---|---|---|---|
| w5-trackwatcher | TrackWatcher.cpp | default/TrackWatcher 0/18 | +0 | Body-port, fuzzy-paired source. Carves GuitarController `[0x82778700,0x82778860)`. No map pins. |
| w5-bgtwi | BaseGuitarTrackWatcherImpl.cpp | default/BaseGuitarTrackWatcherImpl **4/4** | +4 | 4 pins (CanHopo/Slop/TryToHopo/NonStrumSwing). Carves GuitarController `[0x8277CFE0,0x8277D0D0)`+`[0x8277D278,0x8277D5E0)`. |
| w5-bandlist | BandList.cpp | default/BandList 0/3 | +0 | Body-port, fuzzy-paired source. Fresh unit block (0x82328/0x8232C), no map pins. |
| w5-endingbonus | EndingBonus.cpp | default/EndingBonus **5/5** | +5 | 5 pins (MiniIconData::Failed/Reset, UnisonEnd/Reset/UnisonStart). Fresh unit, EOF-appended. |
| w5-notetube | NoteTube.cpp | default/NoteTube **6/10** | +6 | 6 pins (TubePlate::CurrentStartX/CurrentEndX/PollDeploy, NoteTube::SetDeployTiming/InitializePlate/SetPointPos). Fresh unit + symbols.txt dtk boundary re-split at 0x82BF6548/6570. |

Every landed pin reports at 100% in its **own** unit — none leaked into GuitarController, GemTrackDir, or any foreign unit.

## Splits composition fix (commit 3aaef33)
**The one real hazard this wave.** w5-trackwatcher and w5-bgtwi BOTH re-decompose
GuitarController.cpp's `[0x82777E90,0x8277D790)` text and `[0x82237270,0x82237680)`
pdata, each carving different sub-ranges against **original** main. land.sh's
`resolve_splits_union.py` does a line-union, so after the second land it kept BOTH
lanes' contradictory GuitarController decompositions → **3 text + 3 pdata overlaps**
(the wave-9 shared-boundary failure the SOP warns about; the bgtwi auditor even
flagged it in commit 48bd4d2).

Fix: hand-rewrote GuitarController.cpp's splits block to the correct COMPOSED
decomposition = original minus BOTH children (TrackWatcher.cpp +
BaseGuitarTrackWatcherImpl.cpp text/pdata carve-outs). Verified 0 overlaps and
contiguous coverage of both original spans (`0x82777E90→0x8277D790`,
`0x82237270→0x82237680`). Composed verify then confirmed BaseGuitarTrackWatcherImpl
reports as its own 4/4 unit (not folded into GuitarController), proving the carve.

## Duplicate / no-op unions (expected, harmless)
- `0x825bfb80` (OvershellSlot::InOverrideFlow) appears in both w5-bgtwi and
  w5-notetube map adds; the OWNER had already landed it with the **identical name**
  → union no-ops both times (0 net, name agrees).

## Losses analysis
**None.** Both A/B comparisons show 0 lost names. Owner landed heavily concurrently
(main moved past 0473e17 during the wave: TrackWatcherImpl micro-pins at
0x8276F–0x82771, ADSR, OvershellSlot) — all disjoint from the wave-5 carves; no
collision, no clobber. objects.json wiring for all 5 TUs survived the recursive
union (grep-confirmed post-last-land).

## Leftover DEFERs
None — all 5 lanes landed READY on first attempt. No lane was deferred.

## Files NOT touched (owner WIP)
`git diff HEAD` shows configure.py + tools/project.py modified in the main repo
worktree — these are NOT wave-5 changes (owner/pre-existing); all lander commits
were path-limited and exclude them.
