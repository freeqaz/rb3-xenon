# WAVE-5 PLAN — rb3-xenon port-then-pin (2026-07-02)

Planner: Fable (read-only pass). Baseline: main `44f57c6`, report 10,870 matched,
session baseline names `/tmp/w5_baseline_names.txt` (10,825 names >= 99.99%).

## Verification performed (live, this session)

- Re-derived "remaining" per TU from `build/45410914/report.json` (fuzzy>=99.99 names
  + non-None virtual_addresses) + `scripts/target_symbol_map.json` (13,635 keys,
  skip `_comment`). Totals: sysnet 384 remaining, band3 37 remaining — matches the
  coordinator's numbers (band3 39 -> 37: two consumed by the owner's recent lands).
- Owner collision check (`git log --since='12 hours ago' --name-only`, `git worktree list`):
  owner landed OvershellSlot / TrainerPanel / BandTrack / wave-4 bp-* meta_band+tour+game
  TUs; active worktrees are CharClip-flavored (wt-fz-charclip, ov-CharClipGroup),
  bp-round3, CreditsPanel. **No collision with any lane below.** One caution:
  `24f2c42 match: CharEyes MILO_DEBUG struct-drift — drop phantom mDartOffset (+16)`
  landed <24h ago — CharEyes stays OUT of port lanes and is a re-check-live item
  for the pins lane.
- Wiring status from `config/45410914/objects.json` (module -> objects dict;
  `system/*` lives in module **engine**, `band3/*` in module **band3**):
  - ALREADY WIRED (=> pins lane, not port): BandCharDesc, TrackWidget, MeshAnim,
    Text, UILabel, MemMgr, Joypad, CharEyes, ADSR, Gem.
  - NOT WIRED (=> port lanes): TrackWatcher, BandList, BaseGuitarTrackWatcherImpl,
    EndingBonus, NoteTube, Sfx (xenon has an UNWIRED dc3-scaffold Sfx.cpp),
    TambourineManager.
- Wii source exists for every port lane; **no MILO_DEBUG blocks** found in any of the
  seven port-lane Wii TUs (grep clean). EndingBonus.h is ALREADY ported into xenon
  (ObjPtr<T,ObjectDir> -> ObjPtr<T> adaptation, 85 lines) — cpp only.
- Splits placement for every target addr (see per-lane specs). Three container-unit
  carve situations: GuitarController.cpp [0x82777E90,0x8277D790) holds the
  TrackWatcher cluster AND two BaseGuitarTWI fns (two lanes carve the SAME unit —
  disjoint ranges assigned, coordinator must run the SOP overlap self-check at land);
  StreamNull.cpp [0x826FBD28,0x82700E18) holds the Sfx targets (wired — preserve its
  attribution); DepthBuffer3D.cpp [0x826D9F60,0x826DE1E0) holds the TambourineManager
  targets (splits unit exists but **unwired** — lower-risk carve).

## Lane roster (7 Opus port lanes + 1 Sonnet pins lane)

| # | TU | kind | ids | placement |
|---|----|------|-----|-----------|
| 1 | TrackWatcher | port | 5 | carve inside GuitarController.cpp range |
| 2 | BaseGuitarTrackWatcherImpl | port | 3 | 1 gap pin + carve inside GuitarController.cpp |
| 3 | BandList | port | 3 | clean gap micro-pins |
| 4 | EndingBonus | port | 3 | clean gap micro-pins (header pre-ported) |
| 5 | NoteTube | port | 3 | clean gap micro-pins (huge auto gap) |
| 6 | Sfx | port | 3 | carve inside wired StreamNull.cpp range |
| 7 | TambourineManager | port | 3 | carve inside unwired DepthBuffer3D.cpp range |
| 8 | pins (all wired TUs) | pins | ~30+ | band3_worklist_pin.py --all-wired |

Full self-contained specs are in the StructuredOutput lanes (mirrored below).

## Common lane preamble (every lane spec embeds this)

- Worktree: `cd ~/code/milohax/rb3-xenon && scripts/setup_worktree.sh ~/tmp/wt-w5-<tu> w5-<tu>`.
  Work ONLY there. The one sanctioned discard in your own worktree is the
  setup-planted tools/download_tool.py patch.
- Compile gate (no ninja lock):
  `wibo build/compilers/X360/16.00.11886.00/cl.exe /I src/system/stlport /I src/xdk/LIBCMT /I src /I src/system /I src/system/oggvorbis /I src/band3 /I src/network /I src/system/speex/include /nologo /wd4355 /wd4164 /c /GR /O1 /Oi /EHsc /TP /Fo/tmp/<name>.obj <src>`
- Verify per-fn: build the single unit, diff with the MAIN repo's
  `build/tools/objdiff-cli` from inside the worktree, `-o <file>` (never /dev/stdout —
  ICF banner pollutes). Fields: normalized_match_percent / fuzzy_match_percent.
  cli 99.4–99.7 with size-exact often = report-normalized 100 — note it, don't drop.
- NEVER whole-binary build (owner contention). NEVER bare ninja.
- CHECKPOINT-COMMIT-FIRST: commit the clean-compiling port (source+wiring+splits+map)
  before deep verification; refine in follow-ups.
- Map pins only at true-100 or report-normalized-100 size-exact. Map is ADD-ONLY
  (never regenerate). Revert (hand-edit) any pin scoring size-mismatch <100.
- git add ONLY your paths; no -A/. ; no Co-Authored-By; never push; don't delete
  branches/worktrees. Never touch main-repo untracked: src/band3/tour/TourSavable.cpp,
  tools/dc3_name_eligible.py, auto_*.obj, global_fuzzy_pairs.json, 'Z:tmp*', '9'.
- Splits: units may hold multiple disjoint `.text` ranges (TrackWatcherImpl.cpp
  precedent). After ANY splits edit run the overlap self-check:
  ```python
  import re; t=open('config/45410914/splits.txt').read()
  for k in ['pdata','text']:
      rs=sorted([(int(a,16),int(b,16)) for a,b in re.findall(rf'\.{k}\s+start:(0x[0-9A-Fa-f]+)\s+end:(0x[0-9A-Fa-f]+)',t)])
      print(k, sum(1 for i in range(1,len(rs)) if rs[i][0]<rs[i-1][1]), 'overlaps')
  ```
- Small same-TU bodies: byte-verify before pinning (sibling-aliasing tier bsim15-20).
  Run `tools/icf_alias_check.py --worktree <wt> --baseline-report <pre-build report.json>`
  before declaring pins landable when your newly-100 set is all <=44B bodies.
- Oracle: `cd ~/code/milohax/rb3 && bin/analyze-function <wii-mangled>`.

## Skipped candidates (with reasons)

- **MemMgr** (4 ids): already wired + coordinator-flagged platform-divergence risk
  (Xbox PMalloc/heap internals genuinely differ). Pins-lane measure-only; expect
  failures; no port lane.
- **BandCharDesc** (4), **Text** (4), **TrackWidget** (3), **UILabel** (3),
  **MeshAnim** (3), **ADSR** (3), **CharEyes** (3), **Joypad** (3), **Gem** (3):
  all already wired in objects.json => not port lanes; folded into the pins lane.
  Extra cautions there: TrackWidget `Init__11TrackWidgetFv` (0x827bb458) is the
  KNOWN sibling-aliasing miss (Init slot0x44 vs Empty slot0xc) — byte-verify;
  CharEyes owner-touched 24f2c42 (<24h) — re-derive live before pinning;
  MeshAnim ids are `__ls<Key<vector<...>>>` template instantiations — pin only
  if the emitted section is byte-exact; Joypad is os/ platform-y — measure-only.
- **TambourineManager vs Gem** (band3): Tambourine unwired -> port lane; Gem wired -> pins.

## Coordinator notes for landing

- Lanes 1 and 2 both carve GuitarController.cpp's [0x82777E90,0x8277D790) range with
  DISJOINT sub-ranges (lane 1 near 0x82778738–~0x82778838; lane 2 near
  0x8277D278–~0x8277D31x). If both land, resolve_splits_union must produce a
  consistent GuitarController remainder — run the overlap self-check and re-verify
  GuitarController.cpp still reports its pre-wave matches (it is wired).
- Land order suggestion: 4 (EndingBonus, cleanest) -> 3 -> 5 -> 7 -> 6 -> 1 -> 2 -> 8.
- After last land: per-TU objects.json grep (wave-4 shallow-union incident) + check
  every new pin reports in its OWN unit (7951cb5 incident).
