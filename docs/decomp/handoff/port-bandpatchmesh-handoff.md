# Port handoff — BandPatchMesh.cpp (rb3-xenon, MSVC PPC-Xenon)

Branch: `wt-bandpatchmesh` (based on main 7e296f9)
TU: `src/system/bandobj/BandPatchMesh.cpp` (NonMatching, DC3-shared engine)

## Result: +1 strict match (partition STL template, 100% byte-equal)

A prior agent ported the TU (157-line .cpp: `MeshVert::AddUV`, `WorkVerts`
ctor + `SetMeshVerts`, and the `SortByZ` comparator that instantiates the
`std::sort` partition template) and micro-pinned 3 BSim-identity addresses.
This FINALIZE pass built it, measured each pin honestly, and kept only the
true-100 byte-equal one.

## Pinned (kept) — 1

| addr | fn | size | norm% |
|---|---|---|---|
| `0x82334af8` | `stlpmtx_std::__unguarded_partition<RndMesh::Vert**, RndMesh::Vert*, SortByZ>` | 204B (0xCC) | 100.0000 |

Deterministic: re-split + re-rename + diff run1 == run2 == 100.0000%
(target_size 204 == base_size 204). The one that byte-matched is the STL
template the port's `std::sort(unk18.begin(), unk18.end(), SortByZ())` in the
`WorkVerts` ctor instantiates — not either of the two "real" functions.

symbols.txt carves it out of the auto-split blob: `fn_82334AF0` (0xD4) split
into `fn_82334AF0` (0x8, the trailing tail of the prior fn) + `fn_82334AF8`
(0xCC, the partition body).

## Dropped — 2 (honest negatives, close but not byte-equal)

| addr | fn | norm% | reason |
|---|---|---|---|
| `0x82332bc0` | `BandPatchMesh::MeshVert::AddUV` | 88.11 | tsize 492 vs bsize 500 — 8-byte / instr-count divergence, permuter-class (FP scheduling / regalloc). Not byte-equal. |
| `0x82337aa0` | `BandPatchMesh::WorkVerts::SetMeshVerts` | 92.48 | tsize 1280 vs bsize 1264 — 16-byte divergence, permuter-class. Not byte-equal. |

Removed from `scripts/target_symbol_map.json` and their `.text`/`.pdata` split
ranges removed from `config/45410914/splits.txt`. Their bodies remain in the
.cpp (required: they compile the TU and instantiate the SortByZ partition
template that IS matched — removing them would be a re-port and could break the
instantiation).

## Net delta

Clean same-pipeline A/B (both reports generated by the identical
re-split → batch-rename → `objdiff-cli report generate` sequence, so no
warm-cache skew):

- true baseline (partition un-pinned): **10511**
- after (partition pinned): **10512**
- **net +1**, **0 regressions** (0 base-100 → after-<100 across all 1978 units),
  exactly 1 new 100% = the partition.

NOTE on the "10682" baseline quoted in the task: that figure was captured from a
report generated under a warmer/concurrent obj state (many sibling worktrees
building in the shared repo). A direct A/B against it showed a bogus -170 spread
across ~40 unrelated units (TrackDir, CharEyes, Group, …) — the classic
warm-cache-vs-clean-baseline measurement trap, NOT a real regression. The clean
same-pipeline baseline is 10511; the honest, attributable delta is +1.

## ICF verdict: HONEST (exit 0)

`tools/icf_alias_check.py --worktree … --baseline-report <clean baseline>`:
1 matched, REAL-BODIED (204B > 44B stub-fold threshold), 0 stub-folds, 0
foreign-attributed, longest stub run 0. The partition is NOT in
`icf_aliases.map` (only PoolAlloc / MemOrPoolAlloc groups are) and is a genuine
unique 204-byte body. Splits overlap self-check: the range appears once, overlaps
no other `.text` range.

## Leads (future work, if pushing this TU further)

- `AddUV` (88%) and `SetMeshVerts` (92%) are near-miss permuter-class. Worth a
  source-permuter pass (FP-op scheduling / declaration reorder / temp elision)
  before any hand LLM re-attempt — the size deltas (8B, 16B) suggest a small
  instruction-count difference, not a structural mismatch.
- The `WorkVerts` ctor / dtor and the remaining ~dozen BandPatchMesh methods in
  the Wii source were not ported here (only what was needed to emit the pinned
  set + compile). A full-TU port could pin more of the `auto_03_*` blob around
  0x82332xxx–0x82337xxx.
