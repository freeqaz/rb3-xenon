# Wave-6 Pin-Audit Re-Triage — 2026-06-16

## Run

```
venv/bin/python tools/pin_audit.py --json /tmp/w6_pin_audit.json
```

Baseline: **8173 matched functions** (report.json `measures.matched_functions`, fresh post-wave-5 + reveal-sweep).
15 candidates, 43 filtered, 110 deferred.

---

## Key Changes Since W5

Wave-5 executed the two GO candidates from the w5 pin-audit dossier:
- `PropKeys.cpp` second cluster (+48 @100%, pin `[0x8240EB60..0x824137A0)`)
- `system/rnddx9/Rnd.cpp` new TU (+8 @100%)

The w6 tool run finds **the same 15 candidates** as w5, but several require re-evaluation:

1. **PropKeys** is a **false positive** in w6: the tool finds a cluster at `0x8240ED70`, which is inside the *already-executed* second pin `[0x8240EB60..0x824137A0)`. Drop.

2. **Waypoint** was marked "COMDAT template-scatter (18-fn foreign run CONFIRMED)" in the w5 doc — but that claim is **wrong**. Both w5 and w6 `pin_audit.py` output show 0 `foreign_named_in_range` flags and 9 named/9 substantive. Raw auto_03 COFF parse confirms 88 total fns in the cluster (84 anonymous + 9 named Waypoint + 4 named scatter templates). The w5 doc appears to have carried the roadmap's prior refutation (which was about the **old sliver** at `0x822C8CA8` tested against a *different* cluster). Waypoint has never been tested at the new coordinates.

3. **TypeProps + Character**: TypeProps proposed range `[0x82740E40..0x82741D88)` contains FlowEventListener pin `0x827418D0..0x82741950` — `0x82740E40 <= 0x827418D0 < 0x82741D88` = TRUE. TypeProps relocation is **blocked**. Character relocation also blocked because its lo boundary depends on TypeProps eviction.

---

## Refutation Table (All 15 Candidates)

| Unit | Verdict | Key Evidence |
|------|---------|--------------|
| Waypoint | **GO** | 9 named own, 4 foreign non-contiguous, max-foreign-run=2 (<8), AUTO-BLOB, obj_defines 9/9; prior refutation was about old cluster |
| PropKeys | **FALSE POSITIVE** | Cluster 0x8240ED70 inside existing 2nd pin [0x8240EB60..0x824137A0); already executed w5 +48 |
| Object | DEFERRED | INTERLEAVE:DirLoader.cpp, 7/9 entries inside DirLoader pin (93 matches at risk), regression-sensitive |
| Part | BLOCKED | 34 contiguous Accomplishment stub entries in gap 0x82439D8C..0x8243B7D8 = honesty gate FAIL |
| Character | BLOCKED | Depends on TypeProps eviction; TypeProps blocked by FlowEventListener inside its range |
| TypeProps | BLOCKED | FlowEventListener.cpp [0x827418D0..0x82741950) is inside proposed range [0x82740E40..0x82741D88) |
| Synth | REFUTED (w5) | foreign_named_in_range:18 (DebugGraph, StepMoves, MidiChannel, VenueProvider etc.); obj_defines 1/7 |
| NetCacheMgr_Xbox | REFUTED (w5) | INTERLEAVE:DataPointMgr; obj_defines_none_yet |
| UIGuide | REFUTED (w5) | Net −1: over-pin currently harvests 2 CRT guard thunks that carve would orphan |
| LabelNumberTicker | REFUTED (w5) | Same UIGuide carve — tested as net −1 |
| NetCacheMgr | REFUTED (w5) | INTERLEAVE:DataPointMgr; 0 matched in current 0xDFC pin |
| PlatformMgr | REFUTED (w5) | obj_defines_none_yet; 23,656-byte heterogeneous gap (File/FileCache/UsbMidi TUs) |
| SoundTouch | REFUTED (w5) | foreign_named_in_range:18; INTERLEAVE:TDStretch.cpp; obj_defines 0/8 |
| DataArray | REFUTED (w5) | scatter_class; INTERLEAVE:DataNode.cpp; legitimate co-location |
| InlineHelp | REFUTED (w5) | INTERLEAVE:CrowdAudio.cpp; only 4/7 substantive |

---

## Surviving Candidate

### Waypoint.cpp — Pin Relocation to Real Cluster

**Signal:** Signature A+C (sliver + displaced). Current pin `0x822C8CA8..0x822C8CF8` (0x50 bytes, mf=0/1).

**Evidence:**

Real cluster: `[0x823C7CC8..0x823CA668)` — AUTO-BLOB between `CharBonesBlender.cpp@[0x823C5EF8..0x823C67B8)` and `FitnessFilter.cpp@[0x823CA668..0x823CA6AC)`.

Target symbol map entries in cluster (9 own + 4 scatter):
```
0x823C7CC8: ?ShapeDeltaBox@Waypoint@@AAAXABVVector3@@MMAAV2@@Z
0x823C7EB8: ?ShapeDeltaAng@Waypoint@@AAAMMM@Z
0x823C8440: ?OnWaypointLast@Waypoint@@CA?AVDataNode@@PAVDataArray@@@Z
0x823C8520: ?Constrain@Waypoint@@QAAXAAVTransform@@@Z
0x823C8608: ?ShapeDelta@Waypoint@@QAAXABVVector3@@AAV2@@Z  [+] 0x823C8618 overload
0x823C8A48: ?Init@Waypoint@@SAXXZ
0x823C9C40: ?Init@Waypoint@@SAXXZ  (ICF twin or alias)
0x823CA598: ?Copy@Waypoint@@UAAXPBVObject@Hmx@@W4CopyType@23@@Z
-- foreign scatter (non-Waypoint): --
0x823C98E0: ?_M_fill_insert@?$vector@V?$ObjPtr@VSeqInst@@...  (0x90 bytes)
0x823C99F0: ??_ERndLine@@UAAPAXI@Z                           (0x58 bytes)
0x823CA218: ?resize@?$vector@V?$ObjPtr@VSeqInst@@...
0x823CA528: ??4?$ObjVector@VConstraint@BandIKEffector@@...
```

Raw function count (auto_03 COFF parse): **88 total fns** in `[0x823C7CC8..0x823CA668)`.
- 9 named Waypoint = will pair immediately via target_symbol_map.json (all 9 already have entries).
- 4 named foreign = scattered ICF template insts, non-contiguous, max 2 in a row.
- 75 anonymous fn_ = Waypoint's template/STL instantiations, NOT foreign per honesty gate.

**Honesty gate pre-check:**
- `matched_functions > 0`: YES (9 named entries have map entries; at least some will match).
- No `>=8 contiguous foreign fn_@0%` run: YES — only 2 foreign in a row at most.

**Adjacency:** The cluster sits cleanly between two unrelated char TUs in AUTO-BLOB. The 0x1510-byte gap `[0x823C67B8..0x823C7CC8)` before the cluster is unaccounted but would belong to CharBonesBlender or another char unit (not Waypoint's concern — pin_audit proposes starting the pin at the first Waypoint fn).

**Compiled obj fit:** `Waypoint.obj` own text 0x34FC, 143 own fns. Cluster span 0x29D0 covers ~55% of own text. Not all Waypoint methods live here (some elsewhere via template scatter), which is normal.

**Splits.txt change:** Replace current Waypoint entry:
```
# OLD (sliver):
Waypoint.cpp:
    .pdata      start:0x82208850 end:0x82208858
    .text       start:0x822C8CA8 end:0x822C8CF8

# NEW (real cluster):
Waypoint.cpp:
    .pdata      start:0x822XXXXX end:0x822XXXXX   <- dtk re-derives on next touch config.yml + ninja
    .text       start:0x823C7CC8 end:0x823CA668
```

The pdata entry should be re-derived automatically by dtk on the next `touch config/45410914/config.yml && ninja`. Use a worktree to avoid disrupting main.

**Est delta:** +4 to +18, conserv +6–9 (9 named Waypoint methods × ~70% match rate based on Waypoint.obj being the same source + templates at moderate rate).

---

## Numbered Plan (Cold-Executable)

1. **Set up a worktree** (don't mutate the main repo index):
   ```
   scripts/setup_worktree.sh /tmp/wt-waypoint-w6 waypoint-w6
   ```

2. **Edit splits.txt** in the worktree — find the `Waypoint.cpp:` block and replace the `.text` entry:
   - Remove: `.text start:0x822C8CA8 end:0x822C8CF8`
   - Add: `.text start:0x823C7CC8 end:0x823CA668`
   - Leave the old `.pdata` line in place for now; dtk will re-derive.

3. **Rebuild with fresh report:**
   ```
   touch config/45410914/config.yml
   NINJA_JOBS=8 tools/fresh_report.sh 2>&1 | tee /tmp/rb3_build_waypoint_w6.log
   ```
   Re-run once if a divergence warning appears (known FP for splits-only changes).

4. **Honesty gate:**
   ```
   venv/bin/python tools/pin_audit.py  # should not re-flag Waypoint now
   ```
   AND check the Waypoint unit's function list in `report.json`:
   - `matched_functions > 0` required.
   - No >=8 contiguous function with `match_percent_normalized < 100` AND a foreign class name (non-Waypoint, non-fn_).

5. **Read measures.matched_functions** — compare to 8173. Report the net delta.

6. **If gate passes and delta > 0:** merge to main (rebase + ff-only). Commit message: `Waypoint: relocate sliver pin to real TU cluster [0x823C7CC8..0x823CA668)`.

7. **If gate fails (foreign run >=8):** revert the split change. Document the exact run in the roadmap addendum. The cluster is then confirmed as template-scatter and added to the refuted list.

---

## Function Worklist (Named, Already in target_symbol_map.json)

```
?ShapeDeltaBox@Waypoint@@AAAXABVVector3@@MMAAV2@@Z        0x823C7CC8
?ShapeDeltaAng@Waypoint@@AAAMMM@Z                         0x823C7EB8
?OnWaypointLast@Waypoint@@CA?AVDataNode@@PAVDataArray@@@Z  0x823C8440
?Constrain@Waypoint@@QAAXAAVTransform@@@Z                  0x823C8520
?ShapeDelta@Waypoint@@QAAXABVVector3@@AAV2@@Z              0x823C8608
?ShapeDelta@Waypoint@@QAAMM@Z                              0x823C8618
?Save@Waypoint@@UAAXAAVBinStream@@@Z                       0x823C8A48
?Init@Waypoint@@SAXXZ                                      0x823C9C40
?Copy@Waypoint@@UAAXPBVObject@Hmx@@W4CopyType@23@@Z        0x823CA598
```

All 9 have pre-existing target_symbol_map.json entries. No new map entries needed. The renamer will auto-pair them.

---

## Walls (Remaining Candidates)

| Unit | Wall Class | Notes |
|------|-----------|-------|
| Object | Regression/interleave | DirLoader 93 matches at risk; needs .s per-fn recon before boundary move |
| Part/RndParticleSys | Stub-farm gate | 34 Accomplishment stubs create a 34-fn 0% run in extension range |
| TypeProps | Gate-blocked | FlowEventListener [0x827418D0) lies inside proposed range [0x82740E40..0x82741D88) |
| Character | Depends on TypeProps | Can't relocate until TypeProps is evicted; TypeProps is currently blocked |
| Synth 2nd cluster | Source depth | obj_defines 1/7; 18 foreign entries; needs source port before pinning |
| PlatformMgr 2nd cluster | Source depth | obj_defines_none_yet; 23KB heterogeneous gap confirms different TUs |
| NetCacheMgr | Interleave | Both NetCacheMgr + Xbox cluster inside DataPointMgr pin |
| UIGuide/LNT | Net negative | Carve nets −1 (CRT thunk orphaning) |
| SoundTouch | Interleave/foreign | foreign:18, obj_defines 0/8, inside TDStretch pin |
| DataArray | Scatter-class | Template scatter co-location; no honest boundary exists |
| InlineHelp | Interleave | Inside CrowdAudio pin; 3/7 substantive outside |
