# Static-Symbol-guard + MessageTimer worklist

Generated by `tools/static_symbol_finder.py` over the named near-miss pool `[40.0, 99.99)` of `build/45410914/report.json` (official matched = 6932).

- scanned: 477 named fns
- diff failures: 9
- STATIC_SYMBOL_GUARD candidates: 3
- MESSAGE_TIMER candidates: 0

## Lever A — STATIC_SYMBOL_GUARD (the Gem +8 pattern)

Retail uses `static Symbol x("x")` function-local statics (guard-bit lazy init + atexit); our tree resolves the same names as Symbols-header externs. FIX = port the source to the function-local-static form per flagged function.

`two_sided`=our side also emits Symbol ctors retail drops (layout/logic wall, not a clean one-way port).

| pct | n_sym | guard_ori | two_sided | unit | symbol |
|----:|------:|----------:|:---------:|------|--------|
| 56.71 | 1 | 1 | - | `default/band3/game/Player` | `?SetEnergy@Player@@QAAXM@Z` |
| 77.25 | 12 | 3 | YES | `default/OvershellSlot` | `?UpdateView@OvershellSlot@@QAAXXZ` |
| 76.42 | 2 | 2 | YES | `default/OvershellSlot` | `?UpdateState@OvershellSlot@@QAAXXZ` |

### Evidence — `?SetEnergy@Player@@QAAXM@Z` (default/band3/game/Player, 56.706%)  [clean one-way]

1 target-only `??0Symbol@@QAA@PBD@Z` ctor calls, 1 guard-bit `ori` sets, 1 guard tests; C-string pool: `lbl_820E1E3C`

```
  [  54] DEL clrlwi.    r9, r11, 31
  [  56] DEL ori        r11, r11, 0x1
  [  57] DEL stw        r11, lbl_82DA0017, r10
  [  58] DEL lis        r11, lbl_820E1E3C
  [  59] DEL addi       r3, r31, 0x50
  [  60] DEL addi       r4, r11, lbl_820E1E3C
  [  61] DEL bl         ??0Symbol@@QAA@PBD@Z
  [  62] DEL mr         r11, r3
  [  63] DEL mr         r3, r29, lbl_82DA0017
  [  65] DEL bl         fn_82261BC0
  [  68] DEL bl         atexit
```

### Evidence — `?UpdateView@OvershellSlot@@QAAXXZ` (default/OvershellSlot, 77.245%)  [TWO-SIDED WALL]

12 target-only `??0Symbol@@QAA@PBD@Z` ctor calls, 3 guard-bit `ori` sets, 15 guard tests; C-string pool: `lbl_82013978`, `lbl_8201D278`, `lbl_82028E8C`, `lbl_8209FF68`, `lbl_820B2A88`, `lbl_820B2AC0`

```
  [  24] DEL clrlwi.    r9, r11, 31
  [  25] DEL stw        r10, 0x78, r31
  [  26] DEL bne        0x80
  [  27] DEL ori        r11, r11, 0x1
  [  28] DEL stw        r11, lbl_82DA0017, r16
  [  29] DEL lis        r11, lbl_8209FF68
  [  31] DEL addi       r4, r11, lbl_8209FF68
  [  32] DEL bl         ??0Symbol@@QAA@PBD@Z
  [  34] DEL lis        r10, lbl_82DA0017
  [  35] DEL rlwinm.    r9, r11, 0, 30, 30
  [  36] DEL addi       r30, r10, lbl_82DA0017
  [  37] DEL bne        0xa8
  [  38] DEL ori        r11, r11, 0x2
  [  41] DEL mr         r3, r30, lbl_82DA0017
```

### Evidence — `?UpdateState@OvershellSlot@@QAAXXZ` (default/OvershellSlot, 76.415%)  [TWO-SIDED WALL]

2 target-only `??0Symbol@@QAA@PBD@Z` ctor calls, 2 guard-bit `ori` sets, 2 guard tests; C-string pool: `lbl_8203B97C`, `lbl_82049358`

```
  [  30] DEL clrlwi.    r9, r11, 31
  [  31] DEL addi       r30, r10, lbl_82DA0017
  [  33] DEL ori        r11, r11, 0x1
  [  34] DEL stw        r11, lbl_82DA0017, r27
  [  37] DEL addi       r4, r11, lbl_82049358
  [  38] DEL bl         ??0Symbol@@QAA@PBD@Z
  [  39] DEL mr         r11, r3
  [  40] DEL mr         r3, r30, lbl_82DA0017
  [  41] DEL lwz        r4, 0x0, r11
  [  42] DEL bl         fn_82261BC0
  [  43] DEL lis        r11, fn_82C0E430
  [  44] DEL addi       r3, r11, fn_82C0E430
  [  45] DEL bl         atexit
```


### Apply path (root cause, verified)

The flagged functions call `Handle(<msg>_msg, ...)` (or build a `Symbol`) where `<msg>_msg` resolves to a **Symbols-header extern** (`extern Message <msg>;` in e.g. `src/system/utl/Messages*.h`). Retail compiled these as **function-local statics**: each function that uses the message gets its own `static Symbol <msg>("<msg>")` with a guard bit (shared guard word, distinct bit in declaration order — the `ori 0x1`/`0x2`/`0x4` evidence). To fix a flagged function: replace the extern reference inside that function body with a local `static Symbol`. Verified on the clean top candidate: `Player::SetEnergy` calls `Handle(send_update_energy_msg, true)` and `send_update_energy_msg` is `extern Message` at `src/system/utl/Messages4.h:6`; retail builds a local static there (delete-block idx 54–68 above). `two_sided` rows additionally need the OTHER divergence resolved (a layout/logic wall) and will not reach 100% from the static conversion alone.

## Lever B — MESSAGE_TIMER (BEGIN_HANDLERS MILO_DEBUG instrumentation)

`src/system/macros.h` force-defines `MILO_DEBUG` tree-wide, so every `BEGIN_HANDLERS` emits an inline `MessageTimer` block retail (MILO_DEBUG-off) lacks. FIX = per-TU `#undef MILO_DEBUG` (a force-multiplier; global flip = layout-coupled dedicated wave).

_No MESSAGE_TIMER candidates in this band — pool empty (the `::Handle`/BEGIN_HANDLERS instrumentation lever is already drained across the current named near-miss pool; GuitarController::Handle was converted in batch 2)._

## Limitations

- Scope is the **named** `[lo,hi)` pool; anonymous `fn_` near-misses that carry the pattern are not scanned (re-run with a different band or extend `load_targets` to include `fn_`).
- `two_sided` rows have a *second* divergence (our side emits Symbol ctors retail dropped — e.g. OvershellSlot's `go_to_wiiprofilecreator` block). Converting the static-Symbol half alone will NOT reach 100% (those are layout/logic walls per the roadmap); the clean one-way rows (`two_sided=False`) are the high-EV port targets.
- Detection requires guard machinery (`clrlwi.`/`rlwinm.`/`andi.` test OR `ori rX,rX,0x{1,2,4,8...}` set) to co-occur with a delete-side `??0Symbol` ctor; a one-off Symbol temporary without guard bits is intentionally NOT flagged (it is not the function-local-static lever).
- diff failures (ambiguous/ICF-folded mangled names, STLport template instantiations) are counted, not silently dropped.
- The match% is `match_percent_normalized` from report.json; judge any actual landed fix by a whole-binary A/B, not this tool's ranking.

