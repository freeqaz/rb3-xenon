# WAVE-11 DISCOVER — Campaign.cpp (~0x82590910 anchor)

**Date:** 2026-06-20
**Mode:** DISCOVER/PLANNER (Opus), read-only in main @ 053d2d0 (baseline 9159 matched)
**TU:** `band3/meta_band/Campaign.cpp`
**Verdict:** REAL_ACTIONABLE (port-then-pin), with one named blocker (manual map seeds — oracle is broken for this TU)
**Anchor:** `fn_82590910` refs `"campaign_levels"` = `Campaign::Init(DataArray*)`

---

## 1. Boundary recon — PRECISE, ground-truthed

The Campaign.cpp TU `.text` span is **[0x8258C3D8, 0x82590C70)** (size 0x4898 = 17052 bytes, 169 functions: ~57 real-bodied + ~112 STL/funclet/static-init stubs).

**Lower bound 0x8258C3D8** (exclusive-bounded by the function below it, 0x8258C3B0, which ends exactly at 0x8258C3D8):
- 0x8258C0C0–0x8258C3D8 is the **AccomplishmentTourConditional.cpp tail** (currently UNDER-pinned: AccTourConditional pin = 0x8258BE40–0x8258C0C0). Proof:
  - fn_8258C0C0 references rdata string `0x820a62f0 = "conditions"`, used only by `AccomplishmentTourConditional::Configure` (rb3-Wii `AccomplishmentTourConditional.cpp:38 FindArray(conditions)`).
  - fn_8258C2E0/fn_8258C360 are the ctor/deleting-dtor for vtable `0x820a634c`, whose slots point at 0x8258BA00/0x8258BE98/0x8258BFB0 — all inside the AccTourConditional pinned range → that vtable is AccTourConditional's. (Campaign's own pin must NOT include these.)
- fn_8258C3D8 (sz 100) is the lowest function `Handle@0x8258E898` calls — first Campaign method (reads global `0x82dcc3e0`; plausibly `GetUser/GetProfile`).

**Upper bound 0x82590C70** (exclusive-bounded by SavedSetlist.cpp start):
- fn_82590C70 (sz 528) inits Symbols from strings `0x820a75c0="setlist_internal"`, `0x820a75b0="setlist_local"`, `0x820a75a0="setlist_friend"` → **SavedSetlist.cpp** (rb3-Wii: `battle_friend`/`setlist_local`/`set_description`/`set_shared`/`hmx_%s` all live in SavedSetlist.cpp).
- Last Campaign function fn_82590C3C (sz 44) ends at 0x82590C68; 8 bytes pad to 0x82590C70.

**No interleaved foreign TU inside the span.** Verified middle region (0x8258FC1C–0x82590894, a long run of 32-byte funcs) is all `DAT_82dcdc84 & ~bit` guard-clear funclets + `in_r12`-relative unwind funclets — i.e. Campaign's ~52 HANDLE_* static-Symbol-cache `??__F`/guard destructors + ctor/Init exception funclets. Single TU.

### Anchored method→address map (string + vtable + Ghidra ground truth) — manual map seeds
| addr | method | evidence |
|---|---|---|
| 0x825909B0 | `Campaign::Campaign(DataArray*)` ctor | `new`-allocs 0x1d8, sets vtable `0x820a7334`, inits maps |
| 0x825908C0 | `Campaign::~Campaign` deleting dtor `??_G` | vtable slot[0]; calls 0x825904B8 then MemFree |
| 0x825904B8 | `Campaign::~Campaign()` scalar dtor | re-sets vtable `0x820a7334` |
| 0x82590910 | `Campaign::Init(DataArray*)` | FindArray "campaign_levels"(0x820a7470)+"campaign_keys"(0x820a7460) |
| 0x825903D0 | `Campaign::ConfigureCampaignKeyData` | called by Init w/ campaign_keys arr |
| 0x825902D0 | `Campaign::Cleanup()` | iterates map@0x48 RELEASE-ing entries |
| 0x8258E898 | `Campaign::Handle(DataArray*,bool)` (sz 4996) | BEGIN_HANDLERS strings get_*/is_last_campaign_level |
| 0x8258CE98 | `Campaign::OnMsg(const ProfileSwappedMsg&)` | static Message "init"(0x82000c2d) + InMode "qp_career_songinfo" |

**Campaign vtable = 0x820a7334** (slot0=??_G@0x825908C0).

### Pin to add (`.text` only — dtk back-fills `.pdata`)
```
band3/meta_band/Campaign.cpp:
    .text       start:0x8258C3D8 end:0x82590C70
```
Overlap self-check: ran full splits.txt `.text`-range parse + proposed span — **0 overlaps** (no existing range intersects). Nearest neighbours: AccomplishmentTourConditional.cpp (…0x8258C0C0, leaves a clean 0x8258C0C0–0x8258C3D8 gap = its own un-pinned tail) below; MiniGameMgr.cpp (0x82595540…) above.

---

## 2. Portability — GREEN

- `Campaign.h` already in `src/band3/meta_band/Campaign.h`; member layout matches the Ghidra ctor (0x1d8 obj, m_pAccomplishmentMgr@0x1c, maps@0x34/0x6c).
- All deps present: `CampaignKey.cpp`/`CampaignLevel.cpp`/`AccomplishmentManager.cpp` already compile (objs exist); headers `BandProfile.h`, `ProfileMgr.h`, `ProfileMessages.h`, `bandobj/MeterDisplay.h`, `ui/UIPicture.h`, `ui/UILabel.h`, `game/BandUser.h` (LocalBandUser), `rndobj/Tex.h` (RndTex) all resolve. No missing-type wall.
- Source (`../rb3/.../Campaign.cpp`, 992 lines, ~70 methods + BEGIN_HANDLERS) is clean MWCC; needs the standard MWCC→MSVC port (HANDLE_* macros, std::map/vector, Symbol caches).

---

## 3. THE BLOCKER — map generation (oracle mis-located Campaign; manual seeds required)

`gen_game_target_map.py` keys output entries by the **rb3-Wii BinDiff oracle's `rb3_addr`**. For Campaign the oracle is **broken**: all 30 `bindiff_src=Campaign.cpp` entries sit at SCATTERED addresses across the whole binary (0x8226CBF8 … 0x82A67B80, similarity 0.00–0.68) — **ZERO fall inside [0x8258C3D8,0x82590C70)**. Consequences:
- With a span supplied → every entry is `out_of_span` → **0 matched entries emitted**.
- Without a span → it would emit entries at the WRONG scattered addresses → **POISON** (mis-renames foreign TUs binary-wide). **DO NOT run `gen_game_target_map.py --apply` for this TU.**

Therefore the renamer must be fed **manually-derived** `{"0xADDR":"<mangled>"}` entries. The implementation agent derives them from the *compiled* Campaign.obj's defined COMDAT symbols (the MSVC mangled names) paired to addresses via the ground-truth anchor table in §1 + Handle's callee list + objdiff byte-equality (a compiled method that's byte-identical to its target fn_ at the expected address confirms the pairing). Seed with the 8 anchors above; expand by walking Handle's 30 in-span callees against the BEGIN_HANDLERS order.

---

## 4. Self-contained, independently-landable plan (one worktree, vs main@9159)

1. `scripts/setup_worktree.sh ~/tmp/wt-campaign w11-campaign` (CoW buildable+diffable worktree).
2. Add `"band3/meta_band/Campaign.cpp": "NonMatching"` to `config/45410914/objects.json` (alphabetical, near CampaignKey/CampaignLevel).
3. Copy `../rb3/src/band3/meta_band/Campaign.cpp` → `src/band3/meta_band/Campaign.cpp`; port MWCC→MSVC (HANDLE_* macros already proven on sibling meta_band TUs; keep `static Symbol` caches; std::map<Symbol,...> default 0x18 — do NOT opt into RB3_RBTREE_0x1C unless objdiff proves +4 needed per-TU).
4. Add pin (§1) to `config/45410914/splits.txt`. Re-run overlap self-check (parse all `.text`+`.pdata`, assert 0 overlaps) BEFORE building.
5. `python3 configure.py` (worktree absolute --dtk/--objdiff/--wrapper flags). `touch config/45410914/config.yml`.
6. Build: `tools/ninja-locked 2>&1 | tee /tmp/rb3_build_campaign.log` → produces target `build/.../obj/.../Campaign.obj` + `.s` and our compiled obj. Iterate source until it compiles.
7. **Manual map seeds**: ADD (never wholesale-regenerate) the §1 anchor entries to `scripts/target_symbol_map.json`, then extend by pairing Handle's in-span callees to the compiled obj's defined mangled symbols (verify each by objdiff byte-equality at the expected address). Do NOT run `gen_game_target_map.py --apply`.
8. `rm -f build/45410914/target_symbol_renames.stamp && touch config/45410914/config.yml && NINJA_JOBS=8 tools/fresh_report.sh`; read `measures.matched_functions`; re-run once (splits-only FP).
9. HONESTY GATE: keep iff net≥+1, zero unexplained per-unit regressions, no ≥8-contiguous FOREIGN fn_@0% run (own STL/funclets bracketed by own named = OK). Headline net == sum of intended Campaign gains.
10. Return patch (objects.json + splits.txt + Campaign.cpp + Campaign-only target_symbol_map.json additions). NEVER commit to main.

**Expected delta:** conservatively +12 (per prompt) on the clean named methods (ctor/dtor/Init/Cleanup/Config/OnMsg + the trivial Get*/Has* accessors that match first); plausibly more (the span has ~57 real-bodied methods like WAVE-10 SongStatusMgr +49) but the manual-map cost caps realistic first-landing yield. Booked at **+12**.

---

## 5. flag_foundational: NO

Self-contained: own new TU, own pin, own additive map entries. Touches no shared engine header. Campaign.h already exists. The map-generation gap is Campaign-specific (broken oracle for this TU), not a binary-wide lever — handle it manually in the same worktree.

---

## 6. discovered_frontier (adjacent TUs — next wave)

- **AccomplishmentTourConditional.cpp UNDER-pinned tail** (0x8258C0C0–0x8258C3D8, ~10 fns incl. `Configure`/"conditions", vtable 0x820a634c ctor/dtor). Current pin stops at 0x8258C0C0. Extend-to-next-pin candidate → ~+6.
- **SavedSetlist.cpp** (0x82590C70 → next pin): `battle_friend`/`setlist_*`/`set_description`/`set_shared`/`hmx_%s`. UNWIRED game TU, rb3-Wii oracle exists. Fresh port lane → ~+15.
- **Lock-step networking TU** at ~0x82592270+ (`releasing_lock_step`/`LockResponseMsg`/`EndLockMsg`/`BasicStartLockMsg`/`BasicStartLockMsg`) — Quazal lock-step msgs; network/ (compiled /Od per Quazal note). Separate recon → ~+10.
