{"symbol": "fn_82555424", "unit": "default/band3/meta_band/PrefabMgr", "pct": 99.8, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "10-instruction, 40-byte body: `subi r31, r12, 0x160`/mflr/stw/stwu / `addi r3, r31, 0x78` / `bl ??1String@@UAA@XZ` / epilogue vs base `subi r31, r12, 0x140` ... `addi r3, r31, 0x80` ... `bl ??1String@@UAA@XZ`. Same destructor called on both sides, but the r12-relative EH-frame-base offset (-0x160 vs -0x140, delta -32) and the local-object address offset (+0x78 vs +0x80, delta +8) differ. This is the canonical MSVC/X360 local-unwind funclet shape (subi rXX,r12,N prologue establishing an EH scope frame pointer, single addi to compute the local's address, single bl to its dtor, then epilogue) \u2014 the offsets are dictated by the enclosing try-scope's stack layout/EH state, not by this funclet's own source.", "action": "skip-mirage", "cascade_hint": "Unit-wide: PrefabMgr.cpp likely has multiple similarly-shaped tiny EH cleanup funclets (String/DataNode temporaries in try-scopes) that will show the same subi/addi frame-offset drift from funclet mis-pairing/enclosing-frame differences \u2014 not independent bugs."}
{"symbol": "fn_825A5E98", "unit": "default/band3/meta_band/Campaign", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "10-instruction, 40-byte body, subi r12-shaped prologue: `subi r31, r12, 0x130` (base: `subi r31, r12, 0x80`, delta -176) then `bl ?FinishAsyncUnload@LoadMgr@@QAAXXZ` vs base `bl ??1DataNode@@QAA@XZ` \u2014 entirely unrelated call targets (LoadMgr::FinishAsyncUnload vs DataNode dtor). This is not a real code bug: it is objdiff pairing two structurally-identical-looking 40-byte unwind funclets from different source scopes/functions in the unit, so both the frame-base constant and the destructor called diverge together.", "action": "skip-mirage", "cascade_hint": "Unit-wide: Campaign.cpp's other near-100% tiny functions are likely to show the same funclet mis-pairing pattern (differing subi constant + differing bl target) since the TU has many near-identical EH cleanup stubs."}
{"symbol": "fn_826D7564", "unit": "default/FocusTracker", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "10-instruction, 40-byte body, subi r12-shaped prologue (`subi r31, r12, 0xd0`, matching on both sides) but `addi r3, r31, 0x50` (base 0x54, delta +4) feeding `bl fn_82280280` (unnamed, target-only) vs base `bl ??1DataArrayPtr@@QAA@XZ`. The destructor identity itself differs (DataArrayPtr dtor vs an anonymous fn_), confirming this is a mismatched EH cleanup funclet pairing rather than a genuine logic difference in FocusTracker source.", "action": "skip-mirage", "cascade_hint": "Unit-wide: FocusTracker.cpp likely has several DataArrayPtr/other RAII-guarded temporaries whose unwind funclets will show the same small addi-offset + call-target mismatch from funclet mis-pairing."}
{"symbol": "fn_822C0710", "unit": "default/BandCrowdMeter", "pct": 99.8, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "10-instruction, 40-byte body, subi r12-shaped prologue: `subi r31, r12, 0xe0` (base `subi r31, r12, 0xb0`, delta -48) and `addi r3, r31, 0x70` (base 0x68, delta -8), calling `bl fn_822BCBD0` (anonymous target-only) vs base `bl ??1DataNode@@QAA@XZ`. Both the frame constant and the destructor invoked diverge \u2014 a funclet-pairing artifact, not a BandCrowdMeter source bug.", "action": "skip-mirage", "cascade_hint": "Unit-wide: BandCrowdMeter.cpp probably has other DataNode-cleanup funclets from try-scoped locals that will show the identical dual-offset + call-target mismatch pattern."}
{"symbol": "fn_82407744", "unit": "default/Draw", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "10-instruction, 40-byte body, subi r12-shaped prologue: `subi r31, r12, 0xd0` (base `subi r31, r12, 0xe0`, delta +16), identical `addi r3, r31, 0x58` on both sides, but `bl fn_82270560` (anonymous target-only) vs base `bl ??1DataNode@@QAA@XZ`. Only the frame-base constant and the destructor called differ, consistent with mis-paired EH unwind funclets rather than a real Draw.cpp logic bug.", "action": "skip-mirage", "cascade_hint": "Unit-wide: rndobj/Draw.cpp likely has multiple DataNode-cleanup funclets guarding config-node temporaries; other near-100% entries in this unit are probably the same funclet-pairing artifact, not independent code bugs."}
{"symbol": "?SetFrame@RndMorph@@UAAXMM@Z", "unit": "default/Morph", "pct": 99.59, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "195 total instructions, only 16 diff_arg forming 2 clean register-swap pairs: `addi r11,...->r10` / `addi r10,...->r11` and `lfs/stfs r10<->r11`, plus a second pair `r31<->r30` at indices 98-99/108. Same opcodes, same instruction count, purely register-number swaps (documented REGISTER_SWAP pattern, MaybeFixable). sqlite unicorn_verdict for this symbol = EQUIVALENT (confidence high), confirming behavioral equivalence.", "action": "permuter", "cascade_hint": "Function-local: a big single-function (780B) local reg-alloc permutation from float local caching order, not a class-layout issue shared with other Morph.cpp near-misses."}
{"symbol": "fn_8252AFF4", "unit": "default/Memcard_Xbox", "pct": 99.8, "classification": "SPILL_QUANTIZED", "confidence": "low", "evidence": "Tiny (40B) function: `subi r31, r12, 0x1f0` (target) vs `0x1e0` (ours) \u2014 target's frame is a constant 0x10 (16B) larger \u2014 then `addi r3, r31, 0x68` vs `0x60` (same +8 shift) before `bl ??1String@@UAA@XZ`. r12 is consumed before being set by the following `mflr r12`, matching the documented '40B subi-r12' EH-funclet/cleanup-path prologue shape, but the delta is a small, reproducible constant frame-size difference rather than wildly different code \u2014 consistent with an EH-homing local slot our parent scope isn't reserving.", "action": "skip-wall", "cascade_hint": "The identical +0x10 constant frame-size delta recurs verbatim in NetCacheMgr (fn_827CE790) and SongStatusMgr (fn_825D0C24) \u2014 three unrelated units \u2014 pointing to a systemic /EHsc local-object cleanup-scope stack quantum rather than a unit-specific bug; unlikely to be worth chasing per-function."}
{"symbol": "fn_825D0C24", "unit": "default/SongStatusMgr", "pct": 99.91, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "`addi r3, r11, 0x38` (target) vs `addi r3, r11, 0x4` (ours), a 0x34(52)-byte drift feeding a slist/hashtable destructor call (`bl fn_82B69618` target vs our named `??1?$slist<pair<const int,SongStatus*>,...>` dtor \u2014 same conceptual dtor, just unidentified on the target side, so that part is naming noise). SongStatusMgr.h documents `mSongStatusCache` (hash_map<int,SongStatus*>) at exactly offset 0x38 from `this`, matching the target's constant \u2014 implying our compiled code is deriving the STL internal slist-node address from a different (wrong-offset) base than the documented member location.", "action": "recon-needed", "fix_sketch": "Compare our STLport hash_map/hashtable internal layout (buckets vector, allocator, num-elements) against retail's for the 0x38-based mSongStatusCache member in src/band3/meta_band/SongStatusMgr.h/.cpp; confirm which local/member this destructor call actually targets (likely SongStatusMgr::~SongStatusMgr's mSongStatusCache teardown) via Ghidra decompile of the target symbol before editing.", "cascade_hint": "Unit-wide: if the hash_map internal offset used for cleanup is wrong, every accessor built around mSongStatusCache (HasSongStatus/GetSongStatus/CreateOrAccessSongStatus, all inlining the same find) is a plausible co-victim in this TU."}
{"symbol": "fn_827CE790", "unit": "default/NetCacheMgr", "pct": 99.88, "classification": "SPILL_QUANTIZED", "confidence": "low", "evidence": "`subi r31, r12, 0x160` vs `0x150` (+0x10 frame delta, same constant seen in Memcard_Xbox/SongStatusMgr) and `addi r3, r31, 0xa8` vs `0x68` (+0x40) feeding `bl ??1String@@UAA@XZ`. The bit-test/clear logic at offset 0x50 (`clrlwi./beq/clrrwi/stw`) is real and matches byte-for-byte between target and base \u2014 only the trailing frame-size and local-String-slot addresses shift by constants, again matching the cross-unit EH-cleanup-scope pattern rather than a unique class bug.", "action": "skip-wall", "cascade_hint": "Same +0x10 frame-size signature as fn_8252AFF4 (Memcard_Xbox) and fn_825D0C24 (SongStatusMgr) \u2014 cross-unit, systemic; likely an /EHsc local-object cleanup stack reservation quantum common to any function with a non-trivial-dtor local, not specific to NetCacheMgr.cpp."}
{"symbol": "fn_82341868", "unit": "default/BandLabel", "pct": 99.91, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "`addi r3, r11, 0x218` (target) vs `addi r3, r11, 0x16c` (ours), a -172 (0xAC) byte drift feeding a `~UILabel` base-dtor tail call (`bl fn_827F4478` target-unidentified vs our named `??1UILabel@@UAA@XZ` \u2014 same conceptual call). BandLabel.h itself documents this exact gap: `mRetailLayoutReserve[0xAC]` with a comment stating 'retail AppLabel's Hmx::Object virtual base sits at 0x25C ... ours sat at 0x1B0 (-172). The missing 0xAC bytes are real UILabel/BandLabel members whose true split is not yet reconstructed.'", "action": "recon-needed", "fix_sketch": "Reconstruct the true member split inside the 0xAC-byte `mRetailLayoutReserve` pad in src/system/bandobj/BandLabel.h (comment cites 'retail BandLabel.s shows member traffic at 0x214..0x258') so the vbase Hmx::Object offset and ~UILabel dtor address land at the documented 0x218/0x25C instead of the current placeholder pad.", "cascade_hint": "Unit-wide and explicitly documented as such: the header comment says the pad exists specifically so other already-matched BandLabel functions don't regress, implying most/all other BandLabel.cpp near-misses share this exact same unresolved 0xAC layout gap."}
{"symbol": "fn_8240F3F0", "unit": "default/system/rndobj/Rnd", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte, 10-instruction body with the canonical X360 funclet prologue `subi r31, r12, 0x70` (frame computed off r12, the funclet establisher-frame register, not r1/sp) followed by a single `bl` to a cleanup routine and epilogue. Only mismatch is idx[4]: target `lwz r3, 0x54, r31` vs base `lwz r3, 0x50, r31` (off:-4) \u2014 loading the object pointer to pass into the call. Function-call diff shows target calls `fn_8240DDB0` where base calls `??3@YAXPAX@Z` (operator delete) \u2014 i.e. this is an EH cleanup funclet freeing a local object; the -4 offset is inherited drift from the parent function's stack layout, not a bug in the funclet body itself.", "action": "skip-mirage", "cascade_hint": "Unit-wide: Rnd.cpp is large and recarve identified EH funclets by exactly this `subi rXX, r12, N` prologue shape (project memory: '40B subi-r12' screen). Other near-100% tiny functions in this unit are very likely sibling cleanup funclets whose single offset mismatch tracks whatever local-layout drift exists in their parent (non-funclet) function, not an independent bug."}
{"symbol": "?SyncProperty@BandDirector@@UAA_NAAVDataNode@@PAVDataArray@@HW4PropOp@@@Z", "unit": "default/BandDirector", "pct": 99.33, "classification": "FIXABLE_SOURCE_EDIT", "confidence": "high", "evidence": "5 nearly-identical mismatched blocks (idx 637-638, 692-693, 734-735, 776-777, 818-819), one per instrument: target does `lis r10, lbl_82013FA0` / `addi r4, r10, lbl_82013FA0` (address of a fixed data label) where base does `lis r11, ??_C@_04LBAEAJF@bass?$AA@` / `addi r4, r11, ??_C@_04LBAEAJF@bass?$AA@` (address of the raw string-literal constant) -- for bass/drum/guitar/mic/keyboard respectively. Source at src/system/bandobj/BandDirector.cpp:1817-1824 passes raw literals `SendMessage(v, \"bass\")`, `\"drum\"`, `\"guitar\"`, `\"mic\"`, `\"keyboard\"` inline inside the SYNC_PROP_SET macros for bass_intensity/drum_intensity/guitar_intensity/mic_intensity/keyboard_intensity, whereas retail clearly pre-declares local-static Symbol constants for these five names (the same technique this file already applies just above, at lines 1786-1788, for coop_bg/coop_bk/coop_gk, with an explicit comment about retail's guard-ordered function-local statics). This also plausibly explains the correlated OFFSET_SWAP of stack slots 0x58/0x5c (idx 635/640) and the repeated -0x10 shift at idx 856/919/973: eliminating the extra per-call literal->Symbol temporary reclaims a stack slot and shifts everything after it.", "action": "fix-now-cheap", "fix_sketch": "In src/system/bandobj/BandDirector.cpp, ahead of the SYNC_PROP_SET(bass_intensity...) line (~1817), add `static Symbol bass_msg(\"bass\"); static Symbol drum_msg(\"drum\"); static Symbol guitar_msg(\"guitar\"); static Symbol mic_msg(\"mic\"); static Symbol keyboard_msg(\"keyboard\");` (mirroring the existing coop_bg/coop_bk/coop_gk pattern), then change lines 1817/1818/1820/1822/1824 to `SendMessage(v, bass_msg)`, `SendMessage(v, drum_msg)`, `SendMessage(v, guitar_msg)`, `SendMessage(v, mic_msg)`, `SendMessage(v, keyboard_msg)`. Two smaller residual clusters remain unexplained by this alone: idx 639-649 (a `TheBandWardrobe` global-access reordering) and idx 1305-1308 (extra `bl fn_8274AF68` sequence deleted on target side) \u2014 re-diff after the Symbol fix to see if those close too or need separate investigation.", "cascade_hint": "This is the project's known 'local-static Symbol lever' (project_localstatic_symbol_lever.md): remaining candidates are exactly cached-Message/handler body-ports like this one. Very likely explains other near-100% SYNC_PROP_SET-macro functions across BandDirector.cpp and sibling PROPSYNCS blocks in other band3/bandobj files that still pass literal C-strings to SendMessage/GetModeInst instead of pre-declared local-static Symbols."}
{"symbol": "fn_826E7A64", "unit": "default/band3/game/VocalPlayer", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte, 10-instruction body, same funclet shape as fn_8240F3F0: `subi r31, r12, 0xf0` (target) vs `subi r31, r12, 0x90` (base) is the only mismatch (idx[0], off:-96), body is `addi r3, r31, 0x58; bl <DataNode dtor>`. Function-call diff: target only `fn_82B6EA70`, base only `??1DataNode@@QAA@XZ` -- this is a DataNode-destructor cleanup funclet. The -96 delta on the frame-establishing subi is inherited from the parent function's local-variable layout, not a defect in the funclet.", "action": "skip-mirage", "cascade_hint": "Function-local root but structurally unit-wide: VocalPlayer.cpp likely has several DataNode-destructor cleanup funclets recarved out of larger try/catch bodies; each will show the identical single-instruction frame-size mismatch tied to whatever local layout drift exists in its owning function."}
{"symbol": "fn_82404D10", "unit": "default/Anim", "pct": 99.56, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "64-byte body with the same r12-based funclet prologue (`subi r31, r12, 0xe0` vs `0x160`) followed by a conditional guard-flag check/clear (`lwz/clrlwi./beq/clrrwi/stw` at r31+0x5c vs +0x94, off:+56 each) and a call to `fn_82270560` (target) / `??1DataNode@@QAA@XZ` (base) -- the classic 'if constructed, clear flag bit0, then destruct' EH object-cleanup idiom. All 7 mismatches are constant-offset immediate diffs (frame base, stwu size, and the flag/pointer offsets, all consistently +56/+128/+32/-32), no diff_op/insert/delete -- stack-layout tool flags 'frame \u0394 +0x20 (structural)'. This is a conditional DataNode-cleanup funclet, not a body-diverged function; the offsets are inherited from the parent function's frame layout.", "action": "skip-mirage", "cascade_hint": "Same family as fn_8240F3F0/fn_826E7A64: Anim.cpp (rndobj) likely has multiple conditional DataNode-cleanup funclets sharing this shape; their offset deltas track the parent function's stack layout, so fixing the parent's local ordering (if ever pursued) would move all of them in lockstep, but the funclets themselves are not independently fixable."}
{"symbol": "?SetFrame@QuatKeys@@UAAXMMM@Z", "unit": "default/PropKeys", "pct": 99.92, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "Only 1 of 125 instructions mismatches: idx[87] `fmuls f12, ..., ...` vs `fmuls f6, ..., ...` with operand registers swapped (f12<->f6) -- objdiff's own pattern detector tags this COMMUTATIVE_OP_ORDER (LikelyFixable via permuter). Every other instruction, including the surrounding float math, is byte-identical.", "action": "permuter", "cascade_hint": "Function-local: a single commutative fmuls operand-order choice by the register allocator in one expression, not indicative of a class/struct-layout issue in PropKeys.cpp. Worth a /permute pass on this function specifically; unlikely to explain other near-misses in the unit unless they share the exact same multiply-order idiom."}
{"symbol": "??1RockCentral@@UAA@XZ", "unit": "default/RockCentral", "pct": 99.78, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "8 diff_arg instructions, all a constant +/-0x50 (80 byte) displacement shift: target `lwz r11,-0xcc(r3)` vs base `lwz r11,-0x11c(r3)` (idx6), `stw r10,-0xcc(r11)` vs `-0x11c(r11)` (idx13/18), and the base-subobject dtor-call address computations `subi r3,r30,0x58` vs `subi r3,r30,0xa8` (idx25, arg to JobMgr::~JobMgr), `0xb4->0x104` (idx27, DataResultList dtor), `0xb0->0x100` (idx29, MsgSource dtor). This is the classic MSVC vtordisp/vbtable-displacement pattern for RockCentral's virtual base (RockCentral : public MsgSource, and MsgSource : public virtual Hmx::Object per src/system/obj/Msg.h:206) -- our compiled object's vbtable-relative field is 0x50 further from 'this' than retail's, i.e. our layout has ~80 extra bytes of *something* before this point relative to target. lookup_struct_offset found no known field at 0xcc/0x11c in RockCentral (not a plain member -- it's the virtual-base displacement machinery). decomp.db confirms this is a REAL bug: unicorn_verdict=DIVERGENT, unicorn_class=call_arg (the base-class dtor calls receive a wrong 'this' pointer at runtime, not just cosmetic).", "action": "recon-needed", "fix_sketch": "", "cascade_hint": "Unit-wide and beyond: this is a class-hierarchy layout drift on the MsgSource->virtual-Hmx::Object boundary, not a RockCentral-local field. Same mechanism (vtordisp offset drift, different magnitude 4B not 80B) shows up in #66 MusicLibrary::PlaySetlist calling TheGameMode->Property() (GameMode also derives from MsgSource) -- suggests a shared root cause somewhere in MsgSource/Hmx::Object's own layout that would explain other near-misses in any unit whose class derives (directly or indirectly) from MsgSource, not just RockCentral.cpp."}
{"symbol": "?PlaySetlist@MusicLibrary@@QAAX_N@Z", "unit": "default/MusicLibrary", "pct": 99.2, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "3 real mismatches at idx57-60, all inside `TheGameMode->Property(setlists_can_be_saved, true)` (GameMode : public MsgSource, virtual-inherits Hmx::Object::Property). Target: `lwz r10,0x4(r11)` / `lwz r10,4(r10)` / `add r11,r10,r11` / `addi r3,r11,0x4` (4 insns, final this = vbase_ptr+4). Base(ours): `lwz r10,0x0(r11)` / `lwz r10,4(r10)` / `add r3,r10,r11` (3 insns, no final +4). Net effect: our computed 'this' for the Property() virtual call is missing a final +4 and reads GameMode's vbtable-pointer field from offset 0x0 instead of 0x4 -- an off-by-4 vtordisp shift at the very front of the GameMode/MsgSource object, distinct from the already-landed `mUnkTU5_0x18`/`mMode` (0x18->0x1c) placeholder in src/band3/game/GameMode.h:15-20 (that one is further into the object; this drift is near offset 0).", "action": "recon-needed", "fix_sketch": "Investigate GameMode/MsgSource layout near offset 0-4: retail places the vbtable pointer (for reaching the virtual Hmx::Object base) at GameMode+0x4, ours computes it at +0x0. Likely something is missing/misplaced ahead of the vbtable slot in MsgSource (src/system/obj/Msg.h) or GameMode itself -- same family of issue as #65 RockCentral's -0x50 vtordisp drift, just smaller magnitude here.", "cascade_hint": "Directly related to #65 RockCentral in the same slice -- both show vtordisp/vbtable-pointer offset drift on classes deriving from MsgSource (: public virtual Hmx::Object). Since GameMode.h already documents one TU5 struct-rebase fix (0x18->0x1c) as still leaving this front-of-object drift unresolved, expect any other unit calling a virtual Hmx::Object method (Property, HandleType, etc.) through a MsgSource-derived pointer to show the identical off-by-4 pattern."}
{"symbol": "fn_823B6E7C", "unit": "default/CharBoneDir", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte function matching the documented '40B subi-r12' EH-funclet prologue signature (project_wave17_2026-07-10.md / recarve prologue screen): `subi r31,r12,0x200/0x1f0; mflr r12; stw r12,-8(r1); stwu r1,-0x60(r1); addi r3,r31,0x88; bl <dtor>; epilogue`. 9/10 instructions equal including the identical field address `addi r3, r31, 0x88`, but the frame size differs by 0x10 (0x200 vs 0x1f0) and the single call target is an entirely different function: target calls `?FinishAsyncUnload@LoadMgr@@QAAXXZ`, base calls `??1FilePath@@UAA@XZ` -- unrelated semantics, which is only explainable as objdiff pairing our generic per-member cleanup funclet against a different, structurally-identical funclet in the target binary (there are ~16,821 such EH funclets per docs/plans/recarve-pipeline.md, ~24% of all functions).", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local artifact of funclet over-subscription pairing, not a real code bug. This TU (CharBoneDir) likely has several sibling 40-byte EH cleanup funclets (one per destructible member of the class) that are equally susceptible to being mis-paired by objdiff -- expect the same shape to recur among other near-100% entries in this unit."}
{"symbol": "fn_8274D8F8", "unit": "default/DataArray", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "Same 40-byte '40B subi-r12' EH-funclet shape as fn_823B6E7C: `subi r31,r12,0xf0` (target) vs `0x100` (base), identical `addi r3,r31,0x80` field address, single `bl` target completely different -- target calls `??1DataNode@@QAA@XZ` (DataNode destructor), base calls anonymous `fn_82345030`. 9/10 instructions equal; the frame-size delta and unrelated call target are the signature of funclet mis-pairing, not a genuine source bug.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local funclet pairing artifact. Likely one of several DataNode-member-cleanup funclets generated for DataArray/DataNode's multi-member construction; other near-100% functions in this unit may show the identical pattern."}
{"symbol": "fn_823A1C9C", "unit": "default/CharMeshHide", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "Same 40-byte '40B subi-r12' EH-funclet shape (subi r31,r12,0x70 identical both sides this time -- frame size matches). Sole mismatch is the destructed-field address `lwz r3,0x54(r31)` (target) vs `lwz r3,0x84(r31)` (base) -- a 0x30 (48-byte) offset difference -- paired with an entirely different call target: target calls `fn_8240DDB0`, base calls `??1?$ObjRefConcrete@VRndDrawable@@VObjectDir@@@@UAA@XZ` (ObjRefConcrete<RndDrawable,ObjectDir> destructor). Different member being cleaned up + different destructor callee is again the funclet-pairing-ambiguity signature rather than a real class-layout bug in CharMeshHide itself (the field offset difference is exactly explained by the two funclets destroying different members).", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local funclet pairing artifact. CharMeshHide likely holds multiple ObjRef-typed members each getting their own per-field cleanup funclet; expect siblings of this exact 40-byte shape elsewhere in the same unit's near-100% list."}
{"symbol": "?LinearizeKeys@@YAXPAVRndTransAnim@@MMMMM@Z", "unit": "default/system/rndobj/Utl", "pct": 99.9, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "Normalized diff shows only 4 mismatched instructions out of 324, all pure register renumbers with no diff_op/insert/delete: idx11/13 `li` reg:r22->r20 / r20->r22 with matching stack-offset swap (off:-20/+20, tool tags OFFSET_SWAP), and idx56/104 `add` reg:r11->r29 / r29->r11 (and r10<->r29). diagnose mode confirms 'Register swaps: 6 instructions across 3 pairs' (r20<->r22, r11<->r29, r10<->r29) and 'diff_op: none (good!)'. The 12 'symbol relocation' diffs the diagnose tool also counts are address-relocation noise already filtered out of the normalized 99.9% score.", "action": "permuter", "cascade_hint": "Function-local register-pressure churn in a large (324-instr) float-heavy leaf function; not a class-layout or unit-wide issue, unlikely to predict other Utl.cpp near-misses unless they share similarly large local-variable counts."}
{"symbol": "fn_826F8B28", "unit": "default/DepthBuffer3D", "pct": 99.91, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "44-byte, 11-instruction body with the classic funclet prologue shape (`subi r31,r12,0xNN / mflr / stw / stwu`) doing exactly ONE thing: load `this`-ish pointer, `addi r3,r11,OFF`, call a single ObjPtr<RndMesh> destructor, then return. Target calls the still-unnamed `fn_826F8380` at OFF=0x25c; ours calls the named `??1?$ObjPtr@VRndMesh@@@@UAA@XZ` at OFF=0x120. Neither 0x120 nor 0x25c line up with DepthBuffer3D.h's documented `mMesh` (0x160) \u2014 0x120 actually lands mid-way inside `mPlayerPalette` (ObjPtr<RndTex> @0x118-0x12c), which rules out a coherent single-field struct-offset bug and instead points to objdiff pairing two structurally-identical-but-different per-member unwind funclets (DepthBuffer3D's ctor sequentially constructs ~8 ObjPtr/DoubleExponentialSmoother members, each throw-point generating a nearly identical tiny cleanup funclet).", "action": "skip-mirage", "cascade_hint": "DepthBuffer3D's constructor has many sequential ObjPtr/DoubleExponentialSmoother member inits, so this unit likely contains several more of these tiny single-call unwind funclets that will show the same mis-paired-offset mirage shape \u2014 treat other DepthBuffer3D near-misses of similar (~40-50 byte, single-call) size with the same suspicion before chasing a struct fix."}
{"symbol": "??$GatherObjectsFromGroup@VRndMesh@@@@YAIPAVRndGroup@@AAV?$vector@PAVRndMesh@@V?$StlNodeAlloc@PAVRndMesh@@@stlpmtx_std@@@stlpmtx_std@@@Z", "unit": "default/AmbientOcclusion", "pct": 99.85, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "objdiff's own pattern detector already flags this: 'ADDRESS_RELOCATION_NOISE (RarelyHandFixable): 3 address relocation(s)... linker artifact \u2014 different .text layout' and issues an explicit AtLimit(High) verdict. The 3 mismatches are a register rotation (r28->r25, r27->r28, r25->r27) on `addi ... sym` address-materialization instructions \u2014 a cyclic reassignment consistent with different final .text placement of referenced symbols, not a code-logic change.", "action": "skip-mirage", "cascade_hint": "Unit-wide risk: any AmbientOcclusion function that materializes many cross-unit symbol addresses (vtables, statics) via addi/lis pairs is likely to show the same address-relocation-noise pattern from differing .text layout, independent of source correctness."}
{"symbol": "fn_822D0D74", "unit": "default/MoveMgr", "pct": 99.55, "classification": "CONSTANT_DIFF", "confidence": "medium", "evidence": "Tiny (44B/11-instr) destructor-shaped helper: loads two stack values (`lwz r11,0x58(r31)` / `lwz r4,0x54(r31)`), computes `slwi r3,r11,2` (target, i.e. element size *4) vs our `slwi r3,r11,3` (element size *8), then both call the SAME `MemOrPoolFreeSTL` and (matched, unflagged) throw-stub. Since the call target and surrounding loads match exactly and only the shift-amount immediate differs, this looks like a genuine element-size constant bug rather than tooling noise: a *4 shift matches a pointer-sized element (e.g. `std::vector<const MoveParent*>` or `std::vector<const MoveVariant*>`, both declared in MoveMgr.h), while our *8 shift matches an 8-byte element (e.g. a `std::pair<...>` like `mRoutineMeasures`). fn_822D0D74 is anonymous so I can't pin the exact source line without more recon.", "action": "recon-needed", "fix_sketch": "MoveMgr.h declares several vector<pointer> members freed by near-identical tiny destructor helpers (mMoveParents[2]: vector<const MoveParent*>, mPreferredVariants[2]: vector<const MoveVariant*>, mRoutineMeasures[2]: vector<pair<const MoveVariant*,const MoveVariant*>>). Need to identify which vector's dtor this is (via caller xrefs / Ghidra) and confirm whether it's a real elemSize mismatch or another ICF-style template-instantiation mispairing like the FileMerger case below before touching source.", "cascade_hint": "MoveMgr.h has ~5-6 std::vector<pointer-or-pair> members, each compiling to a nearly byte-identical tiny destructor helper differing only in element size and dtor-call target \u2014 other MoveMgr near-misses of similar tiny size are likely the same family (either a genuine template-arg mismatch or ICF-fold mispairing) and worth batch-checking together."}
{"symbol": "??$_Destroy_Range@PAUMerger@FileMerger@@@stlpmtx_std@@YAXPAUMerger@FileMerger@@0@Z", "unit": "default/FileMerger", "pct": 99.95, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "Only 1 of 20 instructions differs, but it exposes a full identity mismatch, not a real bug: target calls `??1?$ObjRefConcrete@VRndTransformable@@VObjectDir@@@@UAA@XZ` and increments the range pointer by `0x10` (sizeof that unrelated type), while our compiled code calls `??1Merger@FileMerger@@QAA@XZ` (the correct destructor for the templated `T=FileMerger::Merger*` this function is instantiated for) and increments by `0x64`=100 bytes, which is architecturally consistent with `FileMerger::Merger`'s actual fields (2 Strings, several bools/enums, several ObjPtr/ObjPtrList members) per FileMerger.h:25. A template range-destroy helper for a completely different, unrelated type (ObjRefConcrete<RndTransformable,ObjectDir>) getting paired against our `_Destroy_Range<Merger*>` is a classic ICF/target-symbol-map mispairing, not a code defect \u2014 our own semantics (dtor + size step matching Merger) are self-consistent and correct.", "action": "skip-mirage", "cascade_hint": "Unit-wide: any other stlpmtx_std template helper instantiated in FileMerger.cpp (`_Destroy_Range`, `_Construct`, etc.) is susceptible to the same ICF-fold/target-symbol-map mispairing against an unrelated type's identically-shaped instantiation \u2014 treat other FileMerger near-100% template helpers with the same suspicion before assuming a real bug."}
{"symbol": "fn_8250E274", "unit": "default/EntityUploader", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "Only mismatch is idx0: `subi r31, r12, 0xe0` (retail) vs `subi r31, r12, 0xa0` (ours), the function's very first instruction, before `mflr r12`/frame push. This is the SEH funclet parent-frame-establisher pivot pattern (r12 = runtime establisher frame pointer). The single call `bl fn_8240DDB0` vs `bl ??3@YAXPAX@Z` (operator delete) is already reloc-name-normalized and not flagged.", "action": "skip-mirage", "cascade_hint": "Any other ~40B near-100% fn in this unit whose first instruction is `subi rX, r12, IMM` is the same documented unfixable funclet-establisher artifact; skip them without individual diagnosis."}
{"symbol": "?FastInvert@@YAXABVTransform@@AAV1@@Z", "unit": "default/BandCharacter", "pct": 99.45, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "diagnose fully explains all 9 diff_arg instructions with 0 unexplained: f30<->f31 FPR swap on 4 instructions (idx17/19 `fmuls [reg:f31->f30]`, idx30/32 `fmadds [reg:f30->f31]`) plus 2 symmetric `lfs` offset swaps (+16/-16 at idx15/16/24/25) plus 1 normalized symbol reloc. 44/44 instructions both sides, no inserts/deletes.", "action": "permuter", "cascade_hint": "Function-local leaf-math regalloc noise (3x3 matrix invert); does not indicate a class-layout issue affecting other BandCharacter near-misses."}
{"symbol": "?Poll@CharIKFingers@@UAAXXZ", "unit": "default/CharIKFingers", "pct": 99.95, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "1/219 mismatch: idx128 `lbzx r9,r8` (retail) vs `lbzx r8,r9` (ours) -- pure base/index register swap on an indexed byte load inside the mFingers[i-1]/mFingers[i] chain-fixing loop, functionally commutative, same instruction count both sides.", "action": "permuter", "cascade_hint": "Single-instruction function-local swap; unrelated to other near-misses in this unit."}
{"symbol": "?Mats@WorldCrowd@@UAAXAAV?$list@PAVRndMat@@V?$StlNodeAlloc@PAVRndMat@@@stlpmtx_std@@@stlpmtx_std@@_N@Z", "unit": "default/Crowd", "pct": 99.97, "classification": "PAIRING_TOOLING", "confidence": "medium", "evidence": "diagnose explicitly labels the sole mismatch (idx87, `beq 0x23c` target vs `beq 0x178` base) as 'Branch destination diffs: 1 (address relocation noise)' with 0 unexplained mismatches; the tool reports the function as 100.0% normalized (99.6% raw). Every other branch pair in the 159-instruction listing shows a similar unflagged offset delta (e.g. idx84 `b 0x240`/`0x248`, idx93 `b 0x234`/`0x23c`, idx151 `beq 0x274`/`0x27c`) without being counted a mismatch, consistent with a benign layout-driven branch-encoding difference rather than a different logical target -- though this wasn't hand-verified against source, hence medium not high confidence.", "action": "recon-needed", "cascade_hint": "If other near-100% Crowd unit functions show the same 'diagnose calls it relocation noise + normalized=100%' signature, batch-classify them the same way rather than hand-diagnosing each."}
{"symbol": "fn_82366CAC", "unit": "default/DataFunc", "pct": 99.8, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "idx0 `subi r31, r12, 0x140` vs `0xc0` and idx4 `addi r3, r31, 0x78` vs `0x50` -- funclet parent-frame-establisher offset plus the local-object address computed relative to it, immediately preceding a single call to a DataArrayPtr destructor (`fn_82B69618` <-> `??1DataArrayPtr@@QAA@XZ`, already normalized, not flagged). Matches the documented DataFunc-unit case in docs/decomp/near-miss-classification-2026-06-06.md ('Per-funclet subi delta survives (FP)').", "action": "skip-mirage", "cascade_hint": "Same funclet-establisher pattern as fn_8250E274; other ~40B near-100% DataFunc fns with the same subi-first-instruction shape are the same unfixable artifact."}
{"symbol": "??1?$vector@VDataArrayPtr@@V?$StlNodeAlloc@VDataArrayPtr@@@stlpmtx_std@@@stlpmtx_std@@QAA@XZ", "unit": "default/PropAnim", "pct": 99.7, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Only 2 diff_arg instructions (31/33 equal). [24] `srawi r11, r11, 3` (target) vs `srawi r11, r11, 2` (base); [25] `slwi r3, r11, 3` (target) vs `slwi r3, r11, 2` (base). This shift computes (end_of_storage - start) / sizeof(DataArrayPtr) then back to bytes for MemOrPoolFreeSTL(count*sizeof(T), ptr) inside StlAlloc.h's deallocate(). Shift-by-3 means retail's compiler used sizeof(DataArrayPtr)==8; our build (shift-by-2) uses sizeof==4, matching the current header (src/system/obj/Data.h:695-697, sole member `DataArray *mData`, 4 bytes). dc3-decomp's Data.h has the identical single-member definition, so this isn't a dc3-vs-rb3wii merge issue -- it looks like a genuine class-size gap between our source and TU5 retail.", "action": "recon-needed", "fix_sketch": "Investigate whether TU5 retail's DataArrayPtr (src/system/obj/Data.h ~line 695) carries an extra 4-byte member beyond `DataArray *mData` (e.g. a cached size/tag field) that would double sizeof to 8 and match the target's shift-by-3. Don't guess-add a field without corroborating it via another DataArrayPtr-touching function or Ghidra struct recon -- this is a class-wide layout claim, not a local fix.", "cascade_hint": "Likely unit-wide/cross-unit: decomp.db already shows other std::vector<DataArrayPtr> operations flagged DIVERGENT (push_back in this same PropAnim unit -> class stack_layout; _M_insert_overflow_aux in default/MetaMusic), which corroborates a real class-size mismatch touching every vector<DataArrayPtr> instantiation across the binary, not just this destructor."}
{"symbol": "fn_823BA5AC", "unit": "default/CharLookAt", "pct": 99.8, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "40-byte body with the funclet-ABI prologue `subi r31, r12, N` (r12 = incoming parent frame pointer, not mflr-saved link). Mismatches: [0] `subi r31, r12, 0xf0` (target) vs `0x70` (base), diff +0x80; [4] `addi r3, r31, 0x80` (target) vs `0x50` (base), diff +0x30; and the sole call target differs: target calls `fn_823B9750`, base calls `??1DataNode@@QAA@XZ`. Inspecting fn_823B9750's body shows it stores a vtable pointer (`lbl_8204DC20`) into *this, calls fn_823B8618, then stores a second vtable (`lbl_820009EC`) -- a virtual-class destructor pattern, not DataNode's plain non-virtual dtor. So this funclet is a compiler-generated unwind cleanup for a local object in some (unidentified) larger CharLookAt.cpp function, and the local being destructed differs in type/placement between target and our build.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "The actionable divergence is not in this 40-byte funclet but in whichever parent CharLookAt.cpp function owns the try-scope local at this stack slot -- its local variable set differs (likely a wrong/missing virtual-class local vs a plain DataNode). Fixing that parent function's locals should regenerate this funclet correctly for free; hand-editing this funclet directly is not possible."}
{"symbol": "?PoseMeshes@CharBonesMeshes@@QAAXXZ", "unit": "default/CharBonesMeshes", "pct": 99.51, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "12/188 diff_arg instructions, all pure operand reorderings, no diff_op/insert/delete. Pattern detector: REGISTER_SWAP (12 instrs/3 pairs, dominated by f11<->f12), COMMUTATIVE_OP_ORDER (6 swaps), OFFSET_SWAP (1 swap of 0x38/0x3c). Concretely: [42]/[43] `add [reg:r28->r30, r9->r8]` immediately mirrored by the inverse swap; [153]/[165]/[167]/[169]/[180]/[182] `fmuls` operand-order flips (f13<->f0, f0<->f11, f11<->f13, f11<->f12, f13<->f11, f12<->f11) -- classic commutative-multiply reordering from source statement order, not a semantic bug. Confirmed via lookup_struct_offset that CharBonesMeshes has no field at 0x38 or 0x3c, so the OFFSET_SWAP is a local stack-slot reorder, not a class-layout issue.", "action": "permuter", "fix_sketch": "", "cascade_hint": "Function-local (statement-ordering noise in PoseMeshes' own math), not a shared-layout signal -- unlikely to predict other CharBonesMeshes near-misses since it isn't tied to a class field."}
{"symbol": "fn_8259878C", "unit": "default/SongSortMgr", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "40-byte funclet, identical `subi r31, r12, 0xc0` prologue on both sides (total frame size matches). Sole mismatch: [4] `addi r3, r31, 0x60` (target) vs `addi r3, r31, 0x68` (base), an 8-byte shift in where the destructed local sits inside the (same-size) frame. The call target `fn_82B69618` (target) vs our named `??1?$vector@VSymbol@@...` (base) is not counted as a real mismatch in normalized mode -- verified fn_82B69618's body directly: `lwz/subf/srawi 2/slwi 2` then tail-branch to fn_827BCA50, the exact shape of the vector<Symbol> destructor (element size 4, matches our named symbol), just unpaired in target_symbol_map.json.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local artifact of the parent SongSortMgr.cpp function's frame layout, not this funclet itself; the 8-byte shift suggests an extra/missing 8-byte local earlier in that parent's scope. Worth checking the parent function (not this funclet) if pursuing further, but the funclet itself is not independently editable."}
{"symbol": "??_GRndGroup@@UAAPAXI@Z", "unit": "default/Group", "pct": 99.95, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Only real mismatch: [5] `subi r31, r3, 0x158` (target) vs `subi r31, r3, 0x148` (base), a this-adjustment of exactly 0x10 (16 bytes) more in target before calling the vbase-destructor-shaped function (target: unnamed fn_82477B10; base: ??_DRndGroup@@QAAXXZ -- naming-only difference, excluded from the normalized mismatch count, as is the operator-delete-vs-MemFree naming at [12]). RndGroup multiply-inherits RndAnimatable/RndDrawable/RndTransformable (struct_db.sqlite); a 16-byte constant delta in the scalar-deleting-destructor's this-adjustor thunk points at a size drift somewhere in that base-class chain (or the shared Hmx::Object virtual base), consistent with a TU5-era member insert.", "action": "recon-needed", "fix_sketch": "Diff RndAnimatable/RndDrawable/RndTransformable member layouts (struct_db.sqlite has: RndAnimatable{mFrame@8,mRate@12}, RndDrawable{mShowing@8,mSphere@12,mOrder@32}, RndTransformable{...,mTarget@168}) against a fresh Ghidra/rb3wii cross-check to find which base gained/lost 16 bytes; do not hand-edit RndGroup itself, the drift is upstream in one of the bases or the vbase.", "cascade_hint": "Likely unit-wide/cross-hierarchy: a 16-byte base-class size drift in RndAnimatable/RndDrawable/RndTransformable would perturb this-adjustor thunks for every multiply-inherited Rnd* class built on that chain, not just RndGroup. Note: decomp.db tags this symbol unicorn_verdict=DIVERGENT/class=regalloc, which doesn't obviously match a pure constant/displacement diff -- worth flagging as a possible unicorn mis-bucketing rather than trusting it over the raw instruction evidence."}
{"symbol": "?SetRoot@Strand@CharHair@@QAAXPAVRndTransformable@@@Z", "unit": "default/CharHair", "pct": 99.93, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "Only 1 real diff_arg out of 149 instructions (99.9% normalized). Instruction [135]: target `fmadds f0, ..., f12` vs base `fmadds f12, ..., f0` -- a pure operand-register swap on a single multiply-add. The other 10 apparent 'diff_arg' entries seen in raw/diagnose mode (SetOwnerObj/fn_823F7A80 vs SetObjConcrete/WorldXfm_Force call-name pairs, 3x each) are relocation-name normalization noise, already excluded from the 99.9% normalized score -- not real code differences. unicorn_verdict=EQUIVALENT confirms behavioral parity.", "fix_sketch": "", "action": "permuter", "cascade_hint": "Function-local: an isolated single-instruction FPR operand-order ambiguity, not indicative of a CharHair.cpp-wide issue."}
{"symbol": "?BeginTiling@DxRnd@@AAAXABVColor@Hmx@@MI@Z", "unit": "default/Rnd_Xbox", "pct": 99.44, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "4 of 57 instructions mismatch, all in one contiguous cluster (idx 30-33): target `fctidz f13,f0 / stfd f13,-0x10(..) / fctidz f0,f11 / stfd f0,+0x10(..)` vs base with f0/f13 swapped between the two float-to-int conversions, plus a correlated stack-slot swap (0x50<->0x60). Pattern detector flags REGISTER_SWAP f0<->f13 (RarelyHandFixable, 1 pair) + OFFSET_SWAP(0x50,0x60) -- textbook regalloc scheduling ambiguity between two independent scratch conversions, not a semantic difference. unicorn_verdict=EQUIVALENT.", "fix_sketch": "", "action": "permuter", "cascade_hint": "Function-local: isolated scratch-register scheduling for two adjacent float->int conversions; no class-layout implication for the rest of Rnd_Xbox.cpp."}
{"symbol": "fn_822C2668", "unit": "default/BandIKEffector", "pct": 99.6, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "40-byte, 10-instruction body with the canonical funclet prologue `subi r31,r12,N / mflr r12 / stw r12,-8(r1) / stwu r1,-N,r1`, one destructor call, epilogue, ret. Target(retail) uses N=0x20 frame and `lwz r3,0x34(r31); bl fn_822702 98` (anonymous, unidentified retail target); base(ours) uses N=0x60/0x70 frame, `lwz r3,0x84(r31); bl ??1?$ObjRefConcrete@VRndTransformable@@VObjectDir@@@@UAA@XZ`. Critically, the retail call target address 0x82270298 is IDENTICAL to the one called from the unrelated BandLeadMeter funclet (fn_822CA060) in a completely different unit -- strong evidence this is an ICF-merged/shared generic destructor-cleanup thunk in retail that our per-TU compiled destructor calls can't byte-match no matter which specific ObjRef<T> destructor we call.", "fix_sketch": "", "action": "skip-mirage", "cascade_hint": "Cross-unit systemic: this exact 40-byte subi-r12-prologue + single-destructor-call funclet shape, paired against the same retail address 0x82270298, recurs at least once more in a different unit (BandLeadMeter/fn_822CA060) -- expect this mirage anywhere a local ObjRef<T>-typed automatic has an EH cleanup path, not just in BandIKEffector."}
{"symbol": "?Copy@RndCam@@UAAXPBVObject@Hmx@@W4CopyType@23@@Z", "unit": "default/Cam", "pct": 99.73, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "14 of 52 instructions mismatch, all D-form displacements off by a constant -0x40 (diagnose: 'dominant delta = -64, 10 instructions'). Four distinct this-relative address computations shift by exactly 0x40 between target/base: `lwz r11,-0x32c(r3)` vs `-0x36c(r3)`; `subi r3,r11,0x32c` vs `0x36c`; `subi r3,r31,0x278` vs `0x2b8` (feeds the `RndTransformable::Copy` base-class call); `subi r3,r31,0x330` vs `0x370` (feeds SetObjConcrete on mTargetTex). Correlated field-write offsets into *this also shift by 0x40 (`stfs f0,-0x7c(r31)` vs `-0xbc(r31)`, and 6 more stw/stfs pairs same delta). lookup_struct_offset had no field recorded at 0x278/0x2b8 in the RndCam struct_db entry -- these land past what's currently annotated, consistent with a class-size gap our header doesn't yet reflect (Cam.h RndCam fields start at 0xc0, run through mViewProjMatrix@0x300/mInvViewProjMatrix@0x340; a 0x40-byte gap is exactly the size of one Hmx::Matrix4). No lwz/stw touching the dynamic_cast'd *source* object (r11, offsets 0x2b4-0x2e4) mismatches at all -- the drift is entirely on the `this`(RndCam)/RndTransformable side of the copy.", "fix_sketch": "Audit RndTransformable (or the Object/RndTransformable/RndCam boundary) for a 0x40-byte (Matrix4-sized) member our header is missing/misplaced vs retail TU5; compare against ../dc3-decomp and ../rb3 oracles for RndTransformable's tail fields, and re-check RndCam.h's `// 0x` offset comments (0xc0 base) against a fresh struct-info dump once found.", "action": "recon-needed", "cascade_hint": "Likely unit-wide (and beyond): the same +/-0x40 shift recurs at 4 independent locations within this one function alone (vtordisp/base-subobject calc, RndTransformable::Copy call, SetObjConcrete call, and the field-copy write block) -- any other RndCam/RndTransformable-touching function in default/Cam (and any other unit that reaches into RndCam's tail members) should show the identical drift."}
{"symbol": "fn_822CA060", "unit": "default/BandLeadMeter", "pct": 99.8, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "40-byte, 10-instruction body, same funclet shape as fn_822C2668: `subi r31,r12,N / ... / stwu r1,-0x60,r1 / lwz r3,off(r31) / bl <dtor> / addi r1,r1,0x60 / ...`. Target(retail): N=0x80, `lwz r3,0x54(r31); bl fn_82270298`. Base(ours): N=0x70, `lwz r3,0x84(r31); bl ??1ObjRefOwner@@UAA@XZ`. The retail call target 0x82270298 is the SAME address the BandIKEffector funclet (fn_822C2668) calls, even though our two TUs compile two different destructors (ObjRefConcrete<RndTransformable,ObjectDir> vs ObjRefOwner) -- confirms this is a shared/ICF-folded generic destructor-cleanup thunk in retail, not two independently-matchable functions.", "fix_sketch": "", "action": "skip-mirage", "cascade_hint": "Same systemic mirage as fn_822C2668 (BandIKEffector) -- both funclets pair to the identical retail address 0x82270298, so this is a cross-unit ICF-fold artifact of EH cleanup thunks for local ObjRef<T>/ObjRefOwner destructors, not specific to BandLeadMeter."}
{"symbol": "?_M_fill_insert_aux@?$vector@VFlowMathOp@@V?$StlNodeAlloc@VFlowMathOp@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAVFlowMathOp@@IABV3@ABU__false_type@2@@Z", "unit": "default/DrivenPropertyEntry", "pct": 99.9, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Frame setup `subi r31,r1,0xf0`/`stwu r1,-0xf0,r1` (target) vs `0xe0` (base) and element-size math `mulli r26,r26,0x34` (target) vs `0x30` (base), repeated at 5 other sites (li r24,0x34/0x30; subi/addi r30,0x34/0x30; mulli r10,r27,0x34/0x30; addi r28,0x34/0x30). Target/retail computes sizeof(FlowMathOp)=0x34 (52B); our header (src/system/flow/DrivenPropertyMathOps.h) computes 0x30 (48B) via mDefault(4)+mOp(4)+mLhs DataNode(8)+mRhs DataNode(8)+mDrivenObj FlowPtr<Hmx::Object>(0x18=FlowPtrBase 0xc + ObjPtr 0xc). ObjPtr's 0xc retail layout {vtable,mOwner,mObject} is already verified elsewhere (obj/Object.h comment, ring-free fn_82451A48), so the missing 4 bytes are not in the ObjPtr/FlowPtr chain but likely a field directly on FlowMathOp itself that our header is missing (a TU5-era insert) or a DataNode/alignment quirk not yet identified. The 16-byte (not 4-byte) stack-frame delta is consistent with a local FlowMathOp temporary rounding from 52B up to the next 16-byte-aligned slot (64B) vs our 48B (already 16-aligned) -- i.e. one true 4-byte member drift manifesting as a larger apparent stack delta due to alignment.", "action": "recon-needed", "fix_sketch": "Diagnose exactly which field is missing from FlowMathOp (src/system/flow/DrivenPropertyMathOps.h, class at line 31) to grow it from 0x30 to 0x34; ObjPtr/FlowPtr layout already verified correct elsewhere, so look at FlowMathOp's own members (mDefault/mOp/mLhs/mRhs) or DataNode padding first. Cross-check any RB3-specific DrivenProperty fields dc3 may have dropped (dc3-decomp's copy is textually identical to ours, so dc3 doesn't help directly here -- may need Ghidra struct recon on retail's FlowMathOp ctor/copy-ctor/dtor).", "cascade_hint": "Unit-wide: sizeof(FlowMathOp) feeds every vector<FlowMathOp> operation (insert/resize/copy/dtor) in DrivenPropertyEntry.cpp and any other TU touching this class, so this same displacement pattern likely explains several other near-misses in the flow/DrivenProperty subsystem."}
{"symbol": "fn_826919A8", "unit": "default/band3/game/NetGameMsgs", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte, 10-instruction body with the canonical funclet establisher-frame prologue `subi r31,r12,0xe0` (target) vs `subi r31,r12,0xd0` (base) [off:-16], then a single call `bl fn_82270560` (target, anonymous/unpaired) vs `bl ??1DataNode@@QAA@XZ` (base) -- both are the same DataNode-destructor cleanup call, just named differently on each side -- followed by a plain epilogue. This is the textbook EH cleanup funclet shape (unwind-time destructor call for a local DataNode), not authored game code.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local artifact: the `r12`-relative offset encodes the parent function's establisher-frame layout, so it drifts whenever the PARENT function's stack frame size changes for any reason. If the actual owning function in NetGameMsgs.cpp has a near-miss, check its stack layout there -- this funclet symbol itself is not a source bug to fix."}
{"symbol": "fn_827E242C", "unit": "default/TrackWidget", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "Identical 40-byte, 10-instruction shape to fn_826919A8: `subi r31,r12,0xd0` (target) vs `subi r31,r12,0xa0` (base) [off:-48], then `bl fn_82270560` (target) vs `bl ??1DataNode@@QAA@XZ` (base) -- same DataNode-dtor cleanup call, then plain epilogue. Same funclet-pairing artifact pattern as the NetGameMsgs case.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Function-local: the -48 establisher-offset drift tracks the owning TrackWidget.cpp function's frame size, not this funclet's own code. Check the real parent function in TrackWidget.cpp for its own near-miss/stack-layout drift if one exists."}
{"symbol": "?SetAndClearShadowViewport@NgLight@@MAAXXZ", "unit": "default/Lit_NG", "pct": 99.95, "classification": "VTABLE_SLOT_DRIFT", "confidence": "medium", "evidence": "Two vcalls through TheNgRnd's vtable (`lwz r11,0x0,r3` then `lwz r11,OFF,r11; mtctr r11; bctrl`, r3=&TheNgRnd both times). Call 1 (NgRnd::SetViewport, args match `const Viewport&`): target slot 0xf0 vs base slot 0xf4 [off:+4] -- a single-entry drift. Call 2 (args `li r4,0x1` + `addi r5,r1,0x50`&zeroed-Color match NgRnd::Clear(uint,const Color&)): target slot 0xf8 vs base slot 0x6c [off:-140] -- a ~35-slot drift, far too large to be the same single-entry cause as call 1. Rnd.h already documents a known, actively-managed drift mechanism: 'Retail X360 RB3 (rev-11-era) Rnd vtable lacks 8 virtuals that DC3's newer Rnd [has]' gated via the RND_DC3_VIRTUAL macro; this function's huge second-call delta suggests there is at least one more undiscovered gap in that same Rnd/NgRnd vtable region (between Clear's declaration and SetViewport/Clear's actual call site) not yet covered by the existing RND_DC3_VIRTUAL fix.", "action": "recon-needed", "fix_sketch": "Re-audit src/system/rndobj/Rnd.h's already-established RND_DC3_VIRTUAL gating list against retail's real vtable slot for Clear() -- our header computes Clear at vtable+0x6c, retail wants +0xf8 (35 slots later), while SetViewport is only off by one slot (+0xf0 vs +0xf4). Likely 1+ additional Rnd virtuals need the same virtual-keyword gating treatment already applied for the known '8 missing virtuals' case.", "cascade_hint": "High, engine-wide: TheNgRnd's vtable is shared by every rndobj/ call site invoking Rnd/NgRnd virtuals (Clear, SetViewport, DoWorldBegin, etc.). If Clear's true retail slot is +0xf8 not +0x6c, every other function in the binary calling Clear() (or any virtual declared after it) through this vtable is computing the wrong slot -- worth checking other Lit_NG.cpp / rndobj near-misses for the same signature."}
{"symbol": "?resize@?$vector@ULod@Character@@V?$StlNodeAlloc@ULod@Character@@@stlpmtx_std@@@stlpmtx_std@@QAAXIABULod@Character@@@Z", "unit": "default/Character", "pct": 99.94, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "Element-size constants used for both the pointer-difference divide and the offset multiply: `li r8,0xc` (target) vs `li r8,0x1c` (base) [off:+16], and `mulli r11,r11,0xc` (target) vs `mulli r11,r11,0x1c` (base) [off:+16]. Target/retail computes sizeof(Character::Lod)=0xc (12B); our header (src/system/char/Character.h, struct Lod at line 33) computes 0x1c (28B) from mScreenSize(float,4B) + mGroup ObjPtr<RndGroup>(0xc) + mTransGroup ObjPtr<RndGroup>(0xc) = 4+12+12=28. 12B fits exactly if mGroup/mTransGroup were raw `RndGroup*` (4B each) instead of the 12B ObjPtr smart-pointer wrapper: 4+4+4=12, matching the observed +16 delta (2 fields x 8B smaller each) exactly.", "action": "recon-needed", "fix_sketch": "Change `ObjPtr<RndGroup> mGroup;`/`ObjPtr<RndGroup> mTransGroup;` in Character::Lod (src/system/char/Character.h ~lines 45,48) to raw `RndGroup *mGroup;`/`RndGroup *mTransGroup;`. Requires updating the Lod ctor init list (currently `mGroup(owner,0), mTransGroup(owner,0)` -- ObjPtr 2-arg form) to plain pointer init, and auditing every other Character.cpp/.h use of these fields (Group()/TransGroup() accessors already just return the stored pointer so those are compatible) plus any Save/Load code that may rely on ObjPtr ref-counting/ownership semantics before landing -- not a pure mechanical rename.", "cascade_hint": "Unit-wide: sizeof(Character::Lod) feeds every vector<Lod> operation in Character.cpp (resize, _M_fill_insert, _M_erase, ctor/dtor, Group()/TransGroup() loops) plus Save/Load, so this same 16-byte size drift should explain multiple other near-misses across the Character unit, not just this one function."}
{"symbol": "fn_827D3D68", "unit": "default/WaveFile", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte body, single mismatch: `subi r31, r12, 0x1c0` (target) vs `subi r31, r12, 0x1f0` (base) \u2014 a frame-size constant baked into a compiler-generated funclet prologue. Everything else (mflr/stw/stwu/addi r3,r31,0x68/bl <vector<CuePoint> dtor>/epilogue) matches byte-for-byte; the bl target itself pairs fine (fn_826D0DA0 == ??1?$vector@UCuePoint...@@QAA@XZ under normalized diff). This is the classic X360 EH local-unwind funclet shape called out in the triage guide (~40B body, subi r12 prologue) that destroys a std::vector<CuePoint> local on the exception path of some larger WaveFile function.", "action": "skip-mirage", "cascade_hint": "The differing subi constant reflects the enclosing (parent) function's total stack-frame size, not this funclet's own code \u2014 if it's wrong, the real function that owns this try-block (and any sibling funclets it spawns) likely has its own near-miss in the same unit from the same frame-size drift; not fixable by editing the funclet."}
{"symbol": "fn_824D9938", "unit": "default/Spotlight", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte body, identical shape to the WaveFile case: single mismatch `subi r31, r12, 0xf0` (target) vs `subi r31, r12, 0xe0` (base); body calls `??1RndEnvironTracker@@QAA@XZ` (name matches exactly on both sides) via `addi r3, r31, 0xa0`. Only the funclet-prologue frame-size immediate differs.", "action": "skip-mirage", "cascade_hint": "Same mechanism as WaveFile: the frame-size constant is inherited from Spotlight.cpp's enclosing function that owns a RndEnvironTracker local; a real stack-layout difference there (extra/missing local) is the root cause and would show up as near-misses on the enclosing function itself, not on other unrelated Spotlight functions."}
{"symbol": "fn_82443C80", "unit": "default/TexRenderer", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "Same 40-byte funclet shape (subi r31, r12, 0x80 \u2014 this part matches exactly on both sides, unlike the other two). The sole mismatch is `lwz r3, 0x54(r31)` (target) vs `lwz r3, 0x50(r31)` (base) feeding `bl ??3DataArray@@SAXPAX@Z` (operator delete on a local DataArray*, name matches). run_diff_inspect stack-layout confirms: frame sizes are identical (0x60 both sides), but exactly one user slot is SHIFTED by -0x4 (`int sz=4 [4..4]` at 0x54 vs 0x50), i.e. \"one side has an extra local that pushes the rest.\"", "action": "recon-needed", "cascade_hint": "Unlike the other two funclets, the enclosing function's overall frame size is already correct (0x80/0x60 match) \u2014 only one 4-byte local is mis-ordered/missing before the DataArray local. This is a small, isolated, plausibly fixable stack-layout issue local to whichever TexRenderer.cpp function owns this DataArray temporary; unlikely to explain other near-misses in the unit unless that same parent function has other funclets."}
{"symbol": "?clear@?$_List_base@VSetlistArtRecord@MusicLibraryNetSetlists@@V?$StlNodeAlloc@VSetlistArtRecord@MusicLibraryNetSetlists@@@stlpmtx_std@@@stlpmtx_std@@QAAXXZ", "unit": "default/band3/meta_band/MusicLibraryNetSetlists", "pct": 99.95, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "22-instruction _List_base<T>::clear() body matches everywhere except `li r3, 0x4c` (target) vs `li r3, 0x10` (base) \u2014 the `MemOrPoolFreeSTL(sizeof(_List_node<T>), ptr)` size argument passed at the deallocate call. Our source's SetlistArtRecord (Symbol unk0 + RndTex* unk4 = 8 bytes) is byte-identical to rb3-Wii's oracle definition, and stlport's `_List_node = _List_node_base(8B) + _Tp` gives exactly 16 = 0x10, matching what we compiled. Target expects 0x4c = 76 bytes total node size \u2014 implying a `_Tp` of 68 bytes, ~8.5x larger than the verified-correct 8-byte SetlistArtRecord. Since this templated clear() body is identical machine code across every `_List_base<T,...>::clear` instantiation except this one literal, it is exactly the shape that trips up the target_symbol_map renamer's fuzzy/structural matching between different T instantiations.", "action": "skip-mirage", "cascade_hint": "This is a systemic risk for the STL-template autoid pipeline (tools/gen_game_target_map.py), not specific to this unit: any `_List_base<T>::clear` / `vector<T>::push_back` / similar templated STL method whose body differs from another instantiation only by one baked-in size literal is a candidate for the same mis-pairing. Worth auditing target_symbol_map.json entries for STL container methods broadly rather than trusting individual per-unit near-misses."}
{"symbol": "?push_back@?$vector@VTransform@@V?$StlNodeAlloc@VTransform@@@stlpmtx_std@@@stlpmtx_std@@QAAXABVTransform@@@Z", "unit": "default/TrackDir", "pct": 99.94, "classification": "PAIRING_TOOLING", "confidence": "medium", "evidence": "32-instruction push_back body matches except two instructions sharing the same delta: `li r5, 0x78` vs `li r5, 0x40` (memcpy size) and `addi r11, r11, 0x78` vs `addi r11, r11, 0x40` (finish-pointer advance) \u2014 both are sizeof(Transform) baked in from the memcpy fast-path for appending one element. Our computed 0x40=64 is internally consistent with the current header: Hmx::Matrix3 (3x Vector3, each already padded to 16B for SIMD per the `u32 PAD` in Vec.h) = 48B, + Vector3 v (16B) = 64B, matching both dc3-decomp's and rb3-Wii's identical `m`+`v`-only Transform definition (rb3-Wii's version, without the SIMD pad, would be 48B \u2014 still not 0x78). Target wants 0x78=120B, 56 bytes larger than any plausible 2-field layout for this class in either oracle; no evidence in dc3 or rb3-Wii of extra Transform members.", "action": "recon-needed", "cascade_hint": "If sizeof(Transform)=120 is genuinely correct for retail, this is binary-wide (every struct embedding a Transform, every memcpy/array-stride over Transform, everywhere in the game) \u2014 worth a dedicated verification pass (Ghidra struct-size check at this call site or a second independent Transform-sized function) before trusting it, since it's equally consistent with a target_symbol_map mis-pairing (see the SetlistArtRecord::clear case above, same failure mode: templated container method differing only by an element-size literal)."}
{"symbol": "?_M_fill_insert_aux@?$vector@VTransformArea@@V?$StlNodeAlloc@VTransformArea@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAVTransformArea@@IABV3@ABU__false_type@2@@Z", "unit": "default/HamCamTransform", "pct": 99.91, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "9 diff_arg instrs, all the sizeof(TransformArea)-derived divisor/multiplier (`li r24,0x30` target vs `li r24,0x70` base) and the +0x40 stack-frame delta. Crucially, target's call targets at this address are named `??0TransformArea` / `??1TransformArea` / `??4TransformArea` in OUR compiled obj, but in the TARGET obj the equivalent slots resolve to `??0Pose@RndMorph`-unrelated case aside, here specifically `??4ScrollAnims@HamListRibbon@@QAAAAV01@ABV01@@Z` (HamListRibbon::ScrollAnims::operator=) appears where TransformArea::operator= is expected. scripts/target_symbol_map.json line 531 maps addr 0x822a70a0 to this TransformArea symbol, but addr 0x822a4838 (called from within it) is independently mapped to ScrollAnims::operator=. Generic vector<T>::_M_fill_insert_aux bodies for different small T are near byte-identical except the embedded sizeof(T) constant and T-specific ctor/dtor/assign callees, so the auto-generated symbol map plausibly cross-wired two structurally-similar template instantiations.", "action": "skip-mirage", "fix_sketch": "Not a source-edit; if pursued, re-verify scripts/target_symbol_map.json's addr 0x822a70a0 entry via Ghidra/fingerprint_match (does the real callee chain there really belong to TransformArea's stride, or to a small ObjPtr-holding class near 0x30 bytes?). Do NOT resize HamCamTransform.h's TransformArea pad (lines 42-47) based on this diff \u2014 that comment's own 0x70 claim is actually what OUR code produces, not what target wants.", "cascade_hint": "Unit-wide risk: every other vector<TransformArea> op in HamCamTransform.cpp (_M_erase, destructor loop, resize, PropSync iteration) likely shows the identical collision pattern since they'd share the same flawed target_symbol_map heuristic."}
{"symbol": "?Terminate@Synth360@@UAAXXZ", "unit": "default/system/synth_xbox/Synth", "pct": 99.95, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "Single diff_arg at instr 82: target `lwz r5, 0x4, r30` vs base `lwz r5, 0xb8, r31`. r30 was set at instr 43 (`addi r30, r31, 0xb4`, common to both sides), so r30+0x4 == r31+0xb8 -- mathematically the same address (mSubmixVoices' end pointer for a vector<T>::erase(begin,end) call), just computed by reusing the already-live r30 (target) vs recomputing from the base pointer r31 (ours). Stack frame sizes match exactly (0x90==0x90); unicorn_verdict=EQUIVALENT. This is pure instruction-selection/regalloc entropy, not a logic or layout bug.", "action": "permuter", "fix_sketch": "", "cascade_hint": "Function-local; this is a one-off addressing choice for a single erase() call site, not indicative of a class-wide layout problem in this TU."}
{"symbol": "?erase@?$list@UAnim@EventTrigger@@V?$StlNodeAlloc@UAnim@EventTrigger@@@stlpmtx_std@@@stlpmtx_std@@QAA?AU?$_List_iterator@UAnim@EventTrigger@@U?$_Nonconst_traits@UAnim@EventTrigger@@@stlpmtx_std@@@2@U32@0@Z", "unit": "default/EventTrigger", "pct": 99.96, "classification": "PAIRING_TOOLING", "confidence": "medium", "evidence": "Single diff_arg: target `li r3, 0x24` (36) vs base `li r3, 0x3c` (60) as the node-deallocation size passed to MemOrPoolFreeSTL, immediately after a call target mismatch (`bl fn_8249BAC8` target vs `bl ??1?$ObjOwnerPtr@VRndAnimatable@@@@UAA@XZ` base). Our header (EventTrigger.h lines 34-48) sizes Anim through mType@0x38+4=0x3c, matching base exactly, so our own model is internally consistent -- the mismatch is that target wants 0x24. This mirrors the confirmed cross-class collision pattern seen in the TransformArea/CamShotFrame siblings (same generic-STL-glue shape, one differing size constant, differing/unnamed adjacent callee), though here target's callee (fn_8249BAC8) is still unresolved so the collision can't be confirmed by name the way it was for the other two.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "If confirmed as a pairing artifact (analogous to TransformArea/CamShotFrame), it would apply to any other vector/list ops over EventTrigger::Anim in this TU. Lower confidence than the confirmed pair since fn_8249BAC8 isn't yet named -- worth a quick Ghidra check on that address before broadly dismissing."}
{"symbol": "?_M_fill_insert_aux@?$vector@VCamShotFrame@@V?$StlNodeAlloc@VCamShotFrame@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAVCamShotFrame@@IABV3@ABU__false_type@2@@Z", "unit": "default/CameraShot", "pct": 99.91, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "9 diff_arg instrs, dominant delta = elemSize constant (`li r24,0x1c` target=28 bytes vs `li r24,0x108` base=264 bytes) plus +0xf0 stack-frame delta. Target's call targets at the ctor/dtor/assign slots resolve to totally unrelated classes: `??0Pose@RndMorph@@QAA@ABU01@@Z` (RndMorph::Pose copy-ctor) and `??4Lod@Character@@QAAAAU01@ABU01@@Z` (Character::Lod::operator=) where our code correctly calls `??0CamShotFrame`, `??1CamShotFrame`, `??4CamShotFrame`. A struct with true sizeof 28 bytes cannot be CamShotFrame (our header alone spans mDuration@0x0 through mCamShot@0x114, i.e. >0x118) -- this is definitively the target_symbol_map mapping a different, small, structurally-similar template instantiation (RndMorph::Pose, plausibly ~28 bytes) onto CamShotFrame's symbol name.", "action": "skip-mirage", "fix_sketch": "Not a source-edit for CameraShot.h. If pursued: re-run identification (fingerprint_match/Ghidra) on this target address to find the real vector<CamShotFrame>::_M_fill_insert_aux (elsewhere, with the correct ~0x118-byte stride and CamShotFrame ctor/dtor callees) and repoint target_symbol_map.json.", "cascade_hint": "Unit-wide: other vector<CamShotFrame> ops in CameraShot.cpp (destructor loop, resize, insert, PropSync) likely show the same misidentification since they'd be assigned by the same heuristic pass."}
{"symbol": "?resize@?$vector@UDynamicPropertyEntry@Flow@@V?$StlNodeAlloc@UDynamicPropertyEntry@Flow@@@stlpmtx_std@@@stlpmtx_std@@QAAXIABUDynamicPropertyEntry@Flow@@@Z", "unit": "default/Flow", "pct": 99.94, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "2 diff_arg instrs: target `li r8, 0x88` (136 bytes) / `mulli r11,r11,0x88` vs base `li r8,0x38` (56 bytes) / `mulli r11,r11,0x38` -- the vector<DynamicPropertyEntry> element-size constant used for the erase-vs-insert element count divide. Direction is opposite the TransformArea/CamShotFrame cases: here target wants a LARGER struct (136B) than we model (56B), i.e. we're plausibly missing ~80 bytes (20 words) of members. Unlike the two PAIRING_TOOLING cases above, target's remaining callee (fn_823AADE0, in place of our named `_M_fill_insert`) is still unresolved/anonymous, so there's no confirmed cross-class collision evidence here -- this reads as a genuine undersized-struct case consistent with the repo's known TU5 struct-insert pattern (per MEMORY.md TU5 P5 struct-rebase notes).", "action": "recon-needed", "fix_sketch": "Flow.h's DynamicPropertyEntry (lines 19-49) currently ends at mObjectType@0x2c+4=0x30 (padded to 0x38). Need to find ~0x50 (80) bytes of additional members between 0x30 and 0x88 -- check rb3-Wii's Flow.h (../rb3/src) for a DynamicPropertyEntry with more fields, or Ghidra-decompile a caller that touches offsets beyond 0x30 on this struct to recover the missing members before touching the pad size.", "cascade_hint": "Unit-wide: any other vector<Flow::DynamicPropertyEntry> op (erase/insert/copy) or direct member access on this struct elsewhere in Flow.cpp will show the same elemSize/offset drift once the true size is found."}
{"symbol": "?OnEnterCloset@BandWardrobe@@QAA?AVDataNode@@PAVDataArray@@@Z", "unit": "default/BandWardrobe", "pct": 99.47, "classification": "REGALLOC_PERMUTER", "confidence": "medium", "evidence": "9/89 instrs mismatch, all a clean r23<->r24 swap with identical opcodes/counts: `li r24,0x0`/`li r23,0x0`; `mr r23,r3`/`mr r24,r3`; and the store pair `stw r24,0x4,r22`+`stw r24,0x0,r22` (base) vs target's `stw r23,0x0,r22`+`stw r23,0x4,r22` (two adjacent DataNode/temp locals stored to stack slots 0x0/0x4 in swapped order). No inserted/deleted instructions, pure register-number/declaration-order permutation. Caveat: decomp.db unicorn_verdict for this symbol is DIVERGENT/call_arg (stable_divergent) - worth a permuter pass to confirm the swap alone closes it rather than a deeper arg-passing issue.", "action": "permuter", "fix_sketch": "", "cascade_hint": "Function-local: two locally-scoped temporaries (likely two DataNode-ish stack locals declared in opposite order from source) drive the whole diff; doesn't predict other near-misses in this unit."}
{"symbol": "fn_825FCA48", "unit": "default/AccomplishmentPanel", "pct": 99.91, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "11-instruction / 44-byte anonymous stub: prologue, `lwz r11,0x84(r31)` (load a captured this-ish pointer), one `addi r3,r11,OFF` then a single `bl` to a destructor, epilogue. Target calls `??1Object@Hmx@@UAA@XZ` (Hmx::Object dtor) at this+0x4; our compiled code calls `??1UIPanel@@UAA@XZ` (UIPanel dtor) at this+0x40 - a completely different callee, not just an offset shift. fn_825FCA48 sits in a dense run of near-identical tiny anonymous fn_ symbols in this unit (fn_825FC9B0 112B, fn_825FCA20 40B, fn_825FCA48 44B, fn_825FCA80 136B, fn_825FCB08 40B, ...) consistent with a cluster of compiler-generated EH cleanup/scalar-destructor funclets for the various small classes (AccomplishmentPanel/TexLoadPanel/UIPanel virtual-inherit Hmx::Object, plus local functor/provider classes) used in this TU - exactly the shape where objdiff's structural pairing can match our funclet against the wrong sibling of identical shape.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Unit-wide: predicts the same kind of mispairing among the other tiny fn_825FC9B0..fn_825FCB08 anonymous destructor/cleanup funclets in this unit - treat that whole cluster as low-signal for hand-fixing."}
{"symbol": "?ListNextShots@BandCamShot@@IAA_NAAV?$list@PAVBandCamShot@@V?$StlNodeAlloc@PAVBandCamShot@@@stlpmtx_std@@@stlpmtx_std@@@Z", "unit": "default/BandCamShot", "pct": 99.9, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "5/50 instrs mismatch, all field-offset drifts on `this` (BandCamShot's own guard bool + list, which live past CamShot's inherited region): `lbz/stb r11,0x1e8,r3/r30` (target) vs `0x1e9` (ours, +1), and `lwz r31,0x1bc,r30`/`addi r3,r30,0x1b4` (target) vs `0x1c0`/`0x1b8` (ours, +4). This matches the documented VBASE_WALL comment already in src/system/world/CameraShot.h (lines ~243-254): retail's real CamShot layout is DC3-style virtual-base MI with an extra `mShotStartedPending` member at 0x100 that our simplified single-inheritance CamShot model omits; re-adding it was previously tried and measured net -3 whole-binary (+7 funclets vs -10 in GetKey/CacheFrames/GetTotalDuration/OnRadio), so it was deliberately left out.", "action": "skip-wall", "fix_sketch": "", "cascade_hint": "Unit-wide (and beyond BandCamShot.cpp): this is the known CamShot vbase-layout wall - it will show the same +1/+4-class offset drift in every CamShot/BandCamShot member function touching fields declared after CamShot::mCategory (0xfc) or any of BandCamShot's own members (mTargets@0x124 onward). Do not re-attempt a naive member-insert fix without redoing the full vbase reconstruction noted in the header comment."}
{"symbol": "?_M_fill_insert_aux@?$vector@V?$Key@V?$vector@VVector2@@V?$StlNodeAlloc@VVector2@@@stlpmtx_std@@@stlpmtx_std@@@@V?$StlNodeAlloc@V?$Key@V?$vector@VVector2@@V?$StlNodeAlloc@VVector2@@@stlpmtx_std@@@stlpmtx_std@@@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAV?$Key@V?$vector@VVector2@@V?$StlNodeAlloc@VVector2@@@stlpmtx_std@@@stlpmtx_std@@@@IABV3@ABU__false_type@2@@Z", "unit": "default/MeshAnim", "pct": 99.91, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "Only 2/111 instrs mismatch (`srawi r11,r11,4`/`slwi r3,r11,4` in target vs shift-3 in ours), but the Function Call Diff shows the two sides calling entirely different template instantiations: target calls `__uninitialized_copy<Key<vector<Vector3,StlNodeAlloc<Vector3>>>>`, `vector<Vector3,...>::vector(copy ctor)` and `operator=` (3x), while our compiled code (matching our source, which is `Keys<std::vector<Vector2>, ...> mVertTexsKeys` per MeshAnim.h:53) calls the Vector2-flavored `__uninitialized_copy`/`__uninitialized_fill_n`/copy-ctor/`operator=`. objdiff has paired our Key<vector<Vector2>>::_M_fill_insert_aux against retail's Key<vector<Vector3>>::_M_fill_insert_aux (almost certainly the analogous helper for a Vector3-keyed Keys<> field, e.g. normals) - a template-instantiation mispairing, not a real code bug in our function.", "action": "skip-mirage", "fix_sketch": "", "cascade_hint": "Unit-wide: MeshAnim.cpp instantiates Keys<vector<T>,...> for multiple T (Vector2 texcoords, Vector3 normals/verts, etc.) producing several near-identical _M_fill_insert_aux template bodies of the same instruction shape - expect the same kind of Vector2/Vector3 (or similar) cross-instantiation mispairing on other MeshAnim STL-template near-misses in this unit."}
{"symbol": "?SyncProperty@StorePanel@@UAA_NAAVDataNode@@PAVDataArray@@HW4PropOp@@@Z", "unit": "default/StorePanel", "pct": 99.95, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "3/57 instrs mismatch, all offsets computed off r27 (=`this`, saved at idx5 `mr r27,r3`): `subi r3,r27,0x38` (target) vs `0x4f` (ours, this-side +0x17/23), `lwz r11,-0x88,r27` (target) vs `-0xa0` (ours, -0x18/24), `subi r3,r11,0x88` (target) vs `-0xa0` (ours, +0x18/24). Since StorePanel : public UIPanel : public virtual Hmx::Object, `this` inside a virtual override like SyncProperty is a vbase-adjusted pointer, and these subi's are computing addresses of StorePanel's own bool/PropSync member state relative to that adjusted this - the same shape of vbase-layout drift as CamShot (a documented ~+4-class wall), just a larger ~23-24 byte delta here. (Also present but not counted as diff_arg: matching +8 branch-offset drift and our named local-static `?_s@...@4VSymbol@@A`/`?$S1@...@4IA` guard vs target's anonymous `lbl_82E068F0/F4` for a `static Symbol(\"load_ok\")`-once idiom - normal HANDLE_LOCAL_STATIC noise, already-known-pattern, not the cause of the 3 real mismatches.)", "action": "skip-wall", "fix_sketch": "", "cascade_hint": "Likely broader than this unit: the ~23-24 byte vbase-adjusted-this offset drift stems from UIPanel's virtual Hmx::Object inheritance (same family as the CamShot VBASE_WALL), so other UIPanel-derived classes' virtual SyncProperty/Handle overrides that address their own members via the adjusted this pointer probably show the same class of drift - recon needed before attempting a source fix, since the CamShot precedent showed naive member-count fixes can regress net whole-binary."}
{"symbol": "fn_8250BDB8", "unit": "default/ContextWrapper", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte body: `subi r31, r12, 0x1e0` (target) vs `subi r31, r12, 0xf0` (base) \u2014 classic funclet prologue establishing a frame pointer relative to the parent's unwind establisher frame (r12), then a single `bl <dtor>; addi r1,r1,0x60; lwz r12,-8(r1); mtlr r12; blr` cleanup shape. Crucially the destructor called differs entirely: target calls `??1JsonConverter@@UAA@XZ`, base calls `??1RockCentralOpCompleteMsg@@UAA@XZ`. Grepping ContextWrapper.cpp shows zero references to JsonConverter anywhere in the file \u2014 the target funclet at this address doesn't belong to this TU's source at all. This is an EH-funclet cross-pairing artifact (objdiff paired our RockCentralOpCompleteMsg-destructing cleanup funclet against an unrelated JsonConverter-destructing funclet from elsewhere in the binary that happens to share the same tiny skeleton), not a real code difference.", "action": "skip-mirage", "cascade_hint": "Unit-wide risk: ContextWrapper.cpp has several local RAII objects (RockCentralOpCompleteMsg `msg` at 4 call sites) each generating its own tiny EH cleanup funclet; any other near-100% 'fn_' entries in this unit with ~40-byte bodies and a single destructor call are likely the same funclet-pairing mirage, not real source bugs \u2014 do not hand-fix them."}
{"symbol": "?Init@VocalTrack@@UAAXXZ", "unit": "default/VocalTrack", "pct": 99.96, "classification": "BODY_DIVERGED", "confidence": "medium", "evidence": "Only mismatch: idx 77 `beq 0x148` (target) vs `beq 0x17c` (base), both 136 instrs / 544 bytes total (byte-identical size). Source uses `if(options){ if(staticArr){ if(FindInt) SetVocalStyle(0); goto next; } SetVocalStyle(options->GetVocalStyle()); } next: ...`. Our asm listing shows the compiler tail-merges BOTH SetVocalStyle call paths into one shared vtable dispatch site ($LN39: mtctr r11; bctrl at vtable slot 0x180), with the `goto next` (`b $LN39@Init`) landing on the shared call rather than skipping it, then falling through to the real `next:` label ($next$215041). Target apparently reaches the post-call join point via a different intermediate branch target despite identical total code size \u2014 i.e. a different code-shape/tail-merge choice around the shared SetVocalStyle vtable call, not a byte-count difference. decomp.db unicorn probe independently flags this symbol DIVERGENT/call_arg (stable_divergent), corroborating that this is a real (if subtle) semantic-adjacent divergence rather than pure noise.", "action": "recon-needed", "cascade_hint": "Function-local \u2014 this is about how the compiler folds the two SetVocalStyle call sites for the goto/if-else `force_static_vocals` branch; unlikely to explain other near-misses in VocalTrack.cpp unless another method has the same goto-into-shared-vcall shape."}
{"symbol": "?SetFrame@RndGenerator@@UAAXMM@Z", "unit": "default/Gen", "pct": 99.91, "classification": "FIXABLE_SOURCE_EDIT", "confidence": "medium", "evidence": "Single mismatch at idx 88/110: `fadds f0, f0, f31` (implied) vs `fadds f0, f31, f0` \u2014 objdiff's own COMMUTATIVE_OP_ORDER pattern detector flags this explicitly as idx 88: fadds (f0,f31 vs f31,f0), i.e. same opcode/registers, just operand order swapped in a commutative float add. Source-line correlation (idx 88 sits in the tail of SetFrame, near the `while(frame >= mNextFrameGen){ Generate(...); mNextFrameGen += RandomFloat(mRateGenLow, mRateGenHigh); }` loop and the `frame - fabs`/`frame + mRateGenHigh` comparisons just above it) points at one of these additions as the site.", "action": "fix-now-cheap", "fix_sketch": "In src/system/rndobj/Gen.cpp RndGenerator::SetFrame, try swapping the operand order of the float addition in `mNextFrameGen += RandomFloat(mRateGenLow, mRateGenHigh);` (rewrite as `mNextFrameGen = RandomFloat(mRateGenLow, mRateGenHigh) + mNextFrameGen;`) \u2014 or if that doesn't move the mismatch, try the other addition candidates at lines ~208-212 (`frame - fabs`, `frame + mRateGenHigh`). Per docs/decomp/patterns/fixable-operators.md#commutative-operand-order this is a known MSVC operand-order sensitivity; use /permute if manual swapping doesn't land it.", "cascade_hint": "Function-local \u2014 a single isolated commutative-operand quirk, not a class-layout issue; shouldn't explain other Gen.cpp near-misses unless they share the identical RandomFloat/accumulator addition idiom."}
{"symbol": "?Poll@StreamReceiver360@@UAAXXZ", "unit": "default/StreamReceiver360", "pct": 99.94, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "3 of 4 mismatches are `lis r11,0x1`/`ori r11,r11,0x803c` (target, i.e. absolute this-relative offset 0x1803c) vs `lis r11,0x0`/`ori r11,r11,0x803c` (base, offset 0x803c) \u2014 and the same pattern at 0x18038 vs 0x8038, and `addis r31,r31,0x2`/`subi 0x7fd4` vs `addis r31,r31,0x1`/`subi 0x7fd4`. Every target offset is exactly 0x10000 (65536) bytes larger than ours. These are field accesses to StreamReceiver360::mSlipVoice (annotated `// 0x8038` in our header) and mVoice (`// 0x803C`) via lwzx off the object base \u2014 both live in a base class (StreamReceiver) whose layout is dominated by `unsigned char mBuffer[kStreamRcvrBufSize]` with `#define kStreamRcvrBufSize 0x8000` (src/system/synth/StreamReceiver.h:8). rb3-Wii's oracle (~/code/milohax/rb3/src/system/synth/StreamReceiver.h:68) has `unsigned char mBuffer[0x18000];` \u2014 exactly the missing 0x10000. dc3-decomp carries the same (wrong-for-RB3) 0x8000 value, so this was inherited uncritically from dc3 per the CLAUDE.md caution. The 4th mismatch, idx 45, is unrelated: `li r3, 0x74` (target=116) vs `li r3, 0x7c` (base=124) as the pool-size arg to `PoolFree` right after `~Voice()` \u2014 our Voice class (src/system/synth_xbox/Voice.h) is 8 bytes larger (0x7c=124, ending at mDisposeTick@0x78) than retail's true pool size-class (0x74=116), a second, independent struct-size bug in the same function.", "action": "fix-now-cheap", "fix_sketch": "1) src/system/synth/StreamReceiver.h:8 \u2014 change `#define kStreamRcvrBufSize 0x8000` to `0x18000` (confirmed by rb3-Wii oracle `mBuffer[0x18000]`); this shifts every field after mBuffer (mRingFreeSpace..mLastPlayCursor and all of StreamReceiver360's own members) by +0x10000 and should fix 3/4 mismatches here plus likely fixes/improves other synth_xbox near-misses (GranularSynth, PitchDetector, Synapse_dsp, StreamReceiver.cpp) that touch this layout. 2) src/system/synth_xbox/Voice.h \u2014 Voice is 8 bytes too large (0x7c vs true 0x74); investigate mPadding76(0x76,2B)+mDisposeTick(0x78,4B) at the tail \u2014 one of them (likely mDisposeTick, an apparent voice-GC addition) may not exist in true retail Voice and should be removed/relocated.", "cascade_hint": "Strongly unit-wide (and beyond): kStreamRcvrBufSize sizes the base StreamReceiver class used by every synth_xbox subclass, so this single #define fix should cascade to other near-100% functions across GranularSynth.cpp, PitchDetector.cpp, Synapse_dsp.cpp, and StreamReceiver.cpp itself. The Voice 8-byte size bug separately cascades to any function that constructs/destroys/pools a Voice (Voice.cpp, StreamReceiver360.cpp call sites)."}
{"symbol": "??4?$vector@UBoneOp@CharSignalApplier@@V?$StlNodeAlloc@UBoneOp@CharSignalApplier@@@stlpmtx_std@@@stlpmtx_std@@QAAAAV01@ABV01@@Z", "unit": "default/CharSignalApplier", "pct": 99.9, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "All 8 mismatches are the same delta: `li r30,0x18`/`mulli ...,0x18`/`addi ...,0x18` (target) vs `0x1c` (base), consistently \u2014 this is sizeof(CharSignalApplier::BoneOp) used as the vector element stride (StlNodeAlloc is a stateless contiguous allocator, confirmed via src/system/utl/StlAlloc.h, so the constant genuinely is sizeof(T)). Target/retail sizeof(BoneOp)=0x18=24; our compiled sizeof(BoneOp)=0x1c=28 \u2014 we are 4 bytes too big. BoneOp (src/system/char/CharSignalApplier.h:13-22) = ObjPtr<RndTransformable> mBone (confirmed 0xc/12B per Object.h's 'Retail X360: 0xc {vtable,mOwner,mObject}' note) + int mOp(4) + float mApplyPercent(4) + float mMinAngle(4) + float mMaxAngle(4) = 12+16 = 28 = 0x1c, matching our base value exactly. All four post-mBone fields (mOp/mApplyPercent/mMinAngle/mMaxAngle) are actively read/written individually in CharSignalApplier.cpp's ctor, operator=, Save/Load (BinStream <<), and SYNC_PROP macros, and used with distinct semantics in Poll()'s angle-interpolation math \u2014 so a wholesale missing field is not obviously supported by usage, meaning the true cause is more subtle (possibly a field that's smaller than a full 4-byte int/float in retail, or padding/packing difference) rather than a simple dropped member.", "action": "recon-needed", "cascade_hint": "Unit-wide: mBoneOps is CharSignalApplier's only vector-of-BoneOp member, so this 4-byte struct-size drift should be dragging down every method in CharSignalApplier.cpp that touches mBoneOps (Handle, SyncProperty, Save, Load, Copy, Poll) \u2014 worth checking those as a batch once the exact spurious field/byte is identified (e.g. via a Ghidra struct-info dump of the true retail BoneOp layout)."}
{"symbol": "fn_82384C68", "unit": "default/CharEyes", "pct": 99.64, "classification": "BODY_DIVERGED", "confidence": "medium", "evidence": "44-byte per-field cleanup funclet. Target(retail) calls `bl ??1?$ObjRefConcrete@VRndTransformable@@VObjectDir@@@@UAA@XZ` directly (fully-inlined base dtor, no derived call), our build instead emits `bl ??1?$ObjPtr@VRndTransformable@@@@UAA@XZ` (calls the ObjPtr-level dtor first) -- consistent with the frame-size delta (subi r31,r12,0x20 target vs 0x70 base) and the member-load delta (lwz r11,0x34,r31 vs 0x84,r31). Object.h already documents (line 377-381) that ObjPtr's implicit dtor is supposed to fully elide into the ObjRefConcrete base call (the 'Implicit Destructor / Vtable-Store Elision' pattern) -- that elision is not happening for this call site. Checked decomp.db: siblings fn_82384BF0/C18/C40/C94/CBC/CE8/D10 (consecutive addresses in the same cluster) are ALL at the identical 99.6/99.64% with the same shape -- a clean repeating pattern, not a one-off pairing accident.", "action": "recon-needed", "cascade_hint": "Unit-wide and confirmed: at least 7 consecutive sibling funclets in the 0x82384BF0-0x82384D10 range show the exact same mismatch shape, while earlier siblings (0x82384588-0x823845E0) are 100%. Likely one EyeDesc-related construct (probably the mEyes vector's per-field partial-construction cleanup path) generates this whole family; fixing the root cause should move all ~7-8 siblings to 100% at once."}
{"symbol": "?SetQuarantined@Player@@UAAX_N@Z", "unit": "default/band3/game/Player", "pct": 99.97, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "Single mismatch: `stb r11, 0x2fc, r31` (target/retail) vs `stb r11, 0x2f8, r31` (base/ours) -- both storing the tail `li r11,0x0` (false) into the field named `unk2c4` (matches rb3-Wii `unk2c4 = false;` in SetQuarantined). A flat +4-byte drift. src/band3/game/Player.h already carries a TODO for exactly this: `int mUnkTU5_tail; // TODO: TU5-inserted 4-byte member (P5 struct re-base probe)`, currently appended at the very end of the class instead of at its true position before unk2c4. Unicorn verdict for this symbol is EQUIVALENT/high, consistent with a pure layout offset (no behavioral change).", "action": "recon-needed", "cascade_hint": "Unit-wide: this is Player's known-but-unplaced TU5 4-byte insert (see project memory 'TU5 P5 wave-1'). Any other Player.cpp function that touches a field physically after the true insertion point (between mBeatMaster/unk2b0 area and unk2c4) will show the same +4 drift -- likely explains several other Player near-misses. Fix = relocate mUnkTU5_tail from the tail to its real slot (needs 1-2 more probe functions to pin the exact position, since only unk2c4 access confirms 'somewhere before 0x2c4-equivalent')."}
{"symbol": "fn_8274FEA0", "unit": "default/system/obj/Dir", "pct": 99.9, "classification": "BODY_DIVERGED", "confidence": "low", "evidence": "40-byte single-member cleanup funclet destructing an `std::list<Symbol,StlNodeAlloc<Symbol>>` (calls fn_82270560 in target / named list dtor in base -- that call itself is NOT flagged as a scored mismatch, only the naming differs). The one scored mismatch is instr[0]: `subi r31, r12, 0xe0` (target) vs `subi r31, r12, 0xa0` (base) -- a 0x40 (64-byte) difference in the funclet's notion of the *parent* frame size, even though this funclet's own local allocation (`stwu r1,-0x60,r1`) is byte-identical on both sides. That points to the enclosing function (whichever Dir.cpp routine owns this list member/local and its exception path) having a differently-sized stack frame in our build vs retail -- i.e. a real (if indirect) stack-layout divergence in the parent, not in this funclet itself.", "action": "recon-needed", "cascade_hint": "Function-local, not unit-wide: decomp.db shows the surrounding ~20 sibling 40/44-byte functions in this unit (fn_8274DF10..fn_8274FF94) are already at 100%, so this is an isolated case, not a systemic funclet-pairing wall. Root-causing requires identifying the actual parent function (via Ghidra) that owns this cleanup funclet and diffing its stack layout."}
{"symbol": "?UpdatePoints@CharIKHead@@IAAX_N@Z", "unit": "default/CharIKHead", "pct": 99.97, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "All 3 scored mismatches are the SAME constant used 3 different ways: `li r26, 0x24` (target=36) vs `li r26, 0x34` (base=52) as the divisor in `divw r11,r11,r26` (computing mPoints vector element count from byte-range), then `addi r28,r28,0x24` vs `0x34` and `addi r11,r11,0x24` vs `0x34` as the per-element pointer stride in the two loops over mPoints. This is sizeof(CharIKHead::Point) itself: retail's Point is 36 bytes, ours compiles to 52 bytes (16 bytes larger). Notably neither dc3-decomp's nor rb3-Wii's Point layout (ObjPtr mBone @0x0 + Vector3 mPos @0xc + float mLen @0x18 + float mLenRatio @0x1c + Vector3 mWorldPos @0x20, total 0x2c=44) matches retail's actual 36 either -- so the TU5/retail X360 Point struct is smaller than both oracles, not just smaller than ours.", "action": "recon-needed", "cascade_hint": "Unit-wide: every CharIKHead.cpp function that touches mPoints (resize, element iteration, operator[]) computes this same sizeof/stride and will show the identical off-by-16 pattern -- this one function's diagnosis should directly fix multiple other CharIKHead near-misses once the real Point layout (36 bytes) is confirmed via Ghidra struct probing and CharIKHead.h is corrected."}
{"symbol": "fn_82757910", "unit": "default/DirLoader", "pct": 99.91, "classification": "BODY_DIVERGED", "confidence": "low", "evidence": "44-byte single-member cleanup funclet destructing an `ObjOwnerPtr<ObjectDir>`. Only scored mismatch: `addi r3, r11, 0x1c` (target=28) vs `addi r3, r11, 0xa8` (base=168) -- a 140-byte gap, far too large to be a small TU5 insert. Our computed offset (0xa8) closely tracks src/system/obj/DirLoader.h's own annotated field `ObjOwnerPtr<ObjectDir> mProxyDir; // 0xa0` (small +8 delta plausibly from a multi-inheritance this-adjustor, since DirLoader : public Loader, public ObjRefOwner), i.e. our code is destructing the field our own header says it should. Retail's much smaller offset (0x1c) doesn't correspond to any known field near the start of DirLoader and may reflect a completely different local/member this particular funclet targets in retail, or a genuinely different DirLoader/Loader base-class layout.", "action": "recon-needed", "cascade_hint": "Function-local, not unit-wide: decomp.db shows dozens of sibling 40/44-byte functions in DirLoader are already at 100% (fn_8275643C, fn_827565D4, etc.), so this is an isolated case rather than a systemic funclet-pairing issue. Needs a Ghidra look at the true retail offset-0x1c member to determine if it's really mProxyDir under a different (smaller) DirLoader layout, or a mis-attributed different funclet entirely."}
{"symbol": "fn_82629FA0", "unit": "default/band3/meta_band/PatchPanel", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "40-byte body is the canonical PPC/360 EH local-unwind cleanup funclet shape: `subi r31,r12,0x80 / mflr r12 / stw r12,-8(r1) / stwu r1,-0x60(r1)` (r12-relative frame reconstruction, not a normal `mr r31,r1` prologue), then a single `bl` to a destructor/deallocator, then epilogue. Only mismatch is `lwz r3, 0x54(r31)` (target) vs `lwz r3, 0x50(r31)` (base) [off:-4], feeding a call to `bl fn_8240DDB0` (target, unidentified retail address) vs `bl ??3DataArray@@SAXPAX@Z` (base, DataArray::operator delete). PatchPanel.cpp has raw `DataArray*` locals allocated with `new DataArray(...)` at multiple call sites (e.g. SwapLayers/ClearLayer/DupeLayer take `DataArray*`, plus config-array walks around line 684-687) that each spawn a textually-identical 'delete raw ptr on exception' funclet; the diff tool's address-based auto-pairing plausibly grabbed a different-but-identical-shaped funclet instance than the true match.", "action": "skip-mirage", "cascade_hint": "PatchPanel.cpp likely has several near-identical DataArray-cleanup funclets scattered through the TU (any function with a raw new DataArray(...) local); other near-100% tiny functions in this unit with the same subi-r12 prologue shape are probably the same funclet-pairing artifact, not independent bugs.", "fix_sketch": ""}
{"symbol": "fn_825D664C", "unit": "default/band3/meta_band/ViewSetting", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "Same 40-byte EH-cleanup-funclet shape (subi r31,r12,N prologue reconstructing frame from r12, single bl, epilogue). Mismatch is at index 0: `subi r31, r12, 0xa0` (target) vs `subi r31, r12, 0x90` (base) [off:-16], i.e. the enclosing function's frame size the funclet reconstructs from differs by 0x10 between the paired target/base instances -- both sides otherwise call an operator-delete-shaped function (`bl fn_8240DDB0` vs `bl ??3DataArray@@SAXPAX@Z`). ViewSetting.cpp has `DataArray *da = new DataArray(2);` (line ~193) as a bare local pointer -- exactly the pattern that produces this generic 'free raw ptr on unwind' funclet. The frame-size delta is consistent with this funclet instance being paired against a different (but byte-identical-body) funclet belonging to a different enclosing function/TU offset.", "action": "skip-mirage", "cascade_hint": "unit-wide risk: ViewSetting.cpp constructs raw DataArray*/String locals in several methods (option-list building, config parsing); expect other tiny near-100% functions in this unit to show the same subi-only mismatch from funclet mis-pairing, not distinct source bugs.", "fix_sketch": ""}
{"symbol": "fn_82278920", "unit": "default/PatchDir", "pct": 99.91, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "Same EH-funclet prologue shape (`subi r31,r12,0x70` equal on both sides here). Sole mismatch: `addi r3, r11, 0x4` (target) vs `addi r3, r11, 0xc` (base) [off:+8], feeding `bl fn_82278490` (target) vs `bl ??1FilePath@@UAA@XZ` (base, FilePath destructor). PatchDir.cpp constructs a temporary `FilePath(FileGetPath(...), ...)` at line 125 -- exactly the kind of local temporary that generates a construction-cleanup funclet. r11 here is loaded from the frame (`lwz r11, 0x84(r31)`, an outer-scope pointer/this), and the +8 delta on the FilePath sub-object address looks like it could be a real struct-offset difference (candidate FilePath-containing aggregate layout) rather than pure funclet noise -- flagged lower-confidence-fixable since the call target name itself (`fn_82278490` vs `??1FilePath@@UAA@XZ`) is ambiguous (unidentified retail address, consistent with generic-funclet mis-pairing rather than a genuinely different callee).", "action": "recon-needed", "cascade_hint": "PatchDir.cpp has other FilePath-temporary constructions in the same function region; if the +8 offset is a real struct layout drift (TU5 member insert into whatever aggregate embeds this FilePath) it would affect every accessor/funclet touching that same struct in this unit, not just this one function -- worth a lookup_struct_offset pass on the containing class before writing off as mirage.", "fix_sketch": ""}
{"symbol": "fn_82B7DEE8", "unit": "default/TourDescPanel", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "Same EH-cleanup-funclet shape. Mismatch at index 0: `subi r31, r12, 0xb0` (target) vs `subi r31, r12, 0x80` (base) [off:-48], and the single call differs by CALLEE TYPE, not just naming: `bl ??1String@@UAA@XZ` (target -- retail, already identified as String::~String via the target symbol map) vs `bl ??1DataNode@@QAA@XZ` (base -- our compiled code calls DataNode::~DataNode). This is the strongest signal in the batch that these are generic, textually-identical cleanup-funclet bodies scattered through the TU (one per local temporary that needs unwind cleanup -- TourDescPanel/TexLoadPanel construction code plausibly has both String and DataNode temporaries), and the auto-pairer matched our DataNode-cleanup funclet against retail's unrelated String-cleanup funclet purely by address/ordinal proximity, not true identity.", "action": "skip-mirage", "cascade_hint": "unit-wide: TourDescPanel.cpp (src/band3/tour/TourDescPanel.cpp) has both String and DataNode temporaries in its construction/init paths, so expect several more of these tiny cleanup-funclet near-misses in the same unit with similarly-swapped callee identities -- all funclet-pairing noise, not real bugs.", "fix_sketch": ""}
{"symbol": "fn_8250F7F8", "unit": "default/Debug", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "medium", "evidence": "Same EH-cleanup-funclet shape (`subi r31,r12,0x80` equal both sides). Mismatch: `addi r3, r31, 0x54` (target) vs `addi r3, r31, 0x50` (base) [off:-4], feeding `bl fn_822AF1C8` (target, unidentified) vs `bl ??1String@@UAA@XZ` (base, String destructor). Debug.cpp (src/system/os/Debug.cpp) is full of local `String` temporaries needing unwind cleanup: `String notifyStr` (241), `String msgCopy` (383), `String config`/`String version` (397-398, 477-478), `String jsonStr` (538) -- each spawns a byte-identical 'destruct this String local' funclet differing only by its enclosing function's stack-local offset. The 4-byte local-slot delta is consistent with the diff tool pairing fn_8250F7F8 against the wrong (but structurally identical) String-cleanup funclet instance among several candidates in the same TU.", "action": "skip-mirage", "cascade_hint": "strongly unit-wide: Debug.cpp has at least 6 separate local String temporaries in different functions, each producing this exact generic cleanup-funclet body; expect a cluster of other 99.9% near-misses in this unit from the same funclet-pairing ambiguity rather than independent source bugs.", "fix_sketch": ""}
{"symbol": "fn_82826CFC", "unit": "default/LabelShrinkWrapper", "pct": 99.94, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "68-byte fn: `lwz r11,0x94(r31)` then `addi r3,r11,0x164` (target) vs `addi r3,r11,0x184` (ours), both followed by `bl ??1Object@Hmx@@UAA@XZ` (call target itself matches). Delta is exactly 0x20 (32 bytes). lookup_struct_offset resolves 0x164 to RndMesh::mVolume and 0x184 to RndMesh::mCompressedVerts in the currently-annotated header (src/system/rndobj/Mesh.h). The header's declared offsets already say mVolume=0x164 matching target, so either our compiled RndMesh layout doesn't actually match its own header comments (an earlier member compiling 0x20 bytes bigger than documented), or this funclet-shaped body (subi r31,r12,0x80 prologue) is destroying a different embedded Hmx::Object subobject than mVolume and the offset coincidence with RndMesh is spurious. Not conclusively pinned to a specific source line within this triage pass.", "action": "recon-needed", "cascade_hint": "If real (not coincidence), a 0x20-byte layout drift before RndMesh::mVolume would show up as offset mismatches in every other near-100% function anywhere in the codebase that touches RndMesh members at/after 0x164 (mBSPTree, mMultiMesh, mMotionCache, mCompressedVerts, mNumCompressedVerts) -- worth a grep-diagnose sweep across rndobj/Mesh*.cpp if confirmed."}
{"symbol": "?_M_fill_insert_aux@?$vector@UBeatCollisionData@@V?$StlNodeAlloc@UBeatCollisionData@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAUBeatCollisionData@@IABU3@ABU__false_type@2@@Z", "unit": "default/SongCollision", "pct": 99.87, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "13 diff_arg instructions all differ by a uniform delta: target's `li r5,0x38` / `mulli r27,r27,0x38` / `subi r31,r31,0x38` / `addi r31,r31,0x38` (etc, at idx 19,32,40,53,54,55,65,69,86,90,94) vs ours consistently using `0x18`; the outer frame allocation also differs (`stwu r1,-0xe0,r1` target vs `-0xc0,r1` ours, idx2/97, delta 0x20). 0x38=56 and 0x18=24: this is `sizeof(BeatCollisionData)` baked into every memcpy-size/stride/pointer-arithmetic constant in the STL vector's fill-insert path. src/system/hamobj/SongCollision.h currently declares `struct BeatCollisionData { float mMinX; float mMaxX; Vector3 mOffset; };` = 8+16(padded)=0x18, matching our 0x18. Target wants 0x38 (56). BeatCollisionData::Set() takes two `const Transform&` args and does `Subtract(start_xfm.v, end_xfm.v, mOffset)` -- if `mOffset` is actually declared `Transform mOffset` instead of `Vector3 mOffset` (Transform = Matrix3(0x24)+Vector3(0xc) = 0x30), total size becomes 4+4+0x30=0x38, matching target exactly, and Set() would still compile as `Subtract(start_xfm.v, end_xfm.v, mOffset.v)`.", "action": "fix-now-cheap", "fix_sketch": "In src/system/hamobj/SongCollision.h, change `Vector3 mOffset;` to `Transform mOffset;` in struct BeatCollisionData (adds 0x20 bytes -> sizeof==0x38). Update src/system/hamobj/SongCollision.cpp: `Subtract(start_xfm.v, end_xfm.v, mOffset)` -> `Subtract(start_xfm.v, end_xfm.v, mOffset.v)`; and the BinStream operators (`bs << bcd.mOffset` / `d >> bcd.mOffset`) likely need to serialize `.v` (or the whole Transform, matching SAVE_REVS/rev semantics) -- check against retail's serialized byte layout before committing.", "cascade_hint": "Unit-wide: BeatCollisionData is stored in `std::vector<BeatCollisionData> mData[kNumDifficulties]` in SongCollisionOutput/SongCollision -- this single mistyped field explains essentially the entire diff footprint of every other near-100% function in default/SongCollision that touches mData (insert/erase/copy/serialize paths), not just this one instantiation."}
{"symbol": "fn_827C216C", "unit": "default/Cheats", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte, 10-instruction body with the canonical MSVC cleanup-funclet shape: `subi r31,r12,0xf0 / mflr r12 / stw r12,-8(r1) / stwu r1,-0x60(r1)` prologue, one `addi r3,r31,OFF` computing a local's address, `bl <DataNode dtor>`, then matching epilogue. Target calls an anonymous `fn_82270560`, ours calls the named `??1DataNode@@QAA@XZ` -- same function (DataNode's destructor), just unnamed on the target side since dtk doesn't auto-name per-instantiation EH funclets. The lone real mismatch is `addi r3,r31,0x78` (target) vs `addi r3,r31,0x88` (ours), a local-temp address computed relative to r31, which in a funclet is set up relative to the *parent* function's (CheatsManager::Handle) frame -- so this constant tracks the parent's frame layout, not anything local to this funclet.", "action": "skip-mirage", "cascade_hint": "This is a symptom, not the disease: the real driver is CheatsManager::Handle's own frame layout drifting by 0x10 from retail (extra/missing local). Fixing Handle() itself (if it's separately tracked) will make this funclet match for free; hand-editing the funclet is pointless."}
{"symbol": "?Handle@BandUI@@UAA?AVDataNode@@PAVDataArray@@_N@Z", "unit": "default/BandUI", "pct": 99.58, "classification": "BODY_DIVERGED", "confidence": "medium", "evidence": "891 instructions, 884 equal; mismatch is tightly localized to idx 236-243 (region 236-243 = 12% match, everything else 100%). Sequence involves setting up a `ProcessedJoinRequestMsg` vtable (`??_7ProcessedJoinRequestMsg@@6B@`) and storing fields at `0x54(r31)`/`0x50(r31)`/`0xa(r30)`. It's not a pure instruction-schedule shuffle: target has `subi r4,r28,0xcc` (idx240) computing an address 0xcc below r28 that our code has no equivalent for, and the opcode itself differs at idx242 (`addi` target vs `subi` base). Function-call diff shows base calls `OnMsg(BandUI, const ProcessedJoinRequestMsg&)` by name while target's paired call is unidentified -- consistent with a real difference in how the ProcessedJoinRequestMsg case constructs/dispatches its message object, not just naming noise. (The 'count differs' entries for OvershellActiveStatusChangedMsg/UIComponentSelectMsg, target 5 vs base 1, are very likely ICF-fold call-graph counting artifacts given the byte diff is confined to only 8 instructions total -- not further verified here.)", "action": "recon-needed", "cascade_hint": "Function-local: only the ProcessedJoinRequestMsg case (a single arm of this giant Handle() dispatch) is affected; the other ~40+ message-type cases in this same function are already byte-identical, so this doesn't predict other near-misses in default/BandUI beyond possibly BandUI::OnMsg(const ProcessedJoinRequestMsg&) itself if it's tracked separately."}
{"symbol": "fn_8255D0BC", "unit": "default/band3/meta_band/AccomplishmentManager", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte, 10-instruction body, same canonical cleanup-funclet shape as fn_827C216C: `subi r31,r12,0xf0` (target) vs `subi r31,r12,0x90` (ours) at idx0 -- a 0x60 (96-byte) difference in the funclet's base-frame-relative constant, which is itself computed from the *parent* function's total unwind-frame size, not this funclet's own (identical, matching) locals. Body: `addi r3,r31,0x60 / bl <DataNode dtor>` is byte-identical on both sides (frame slot 0x60 matches); only the prologue's parent-frame constant differs. Target calls anonymous `fn_82B69618`, ours calls named `??1DataNode@@QAA@XZ` -- again the same DataNode destructor, just unnamed on target.", "action": "skip-mirage", "cascade_hint": "Same mechanism as the Cheats funclet: the real bug is a 96-byte frame-size drift in the enclosing AccomplishmentManager Handle()-style function (whichever one owns this funclet), likely from a missing/extra local object requiring EH cleanup registration. Chasing the funclet itself won't move the needle; find and fix the parent function's frame size."}
{"symbol": "?CheckBSPTree@@YA_NPBVBSPNode@@ABVBox@@@Z", "unit": "default/Geo", "pct": 99.02, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "67/334 mismatches, 63 of them are register-number swaps in float regs (f25<->f27 dominant, also f21<->f22/f23, f26<->f28, f30<->f31), e.g. idx26 `lfs f21->f23`, idx88 `fneg f26->f28, f22->f23`. Only 3 are D-form offset swaps (0x0,0x18) and 1 is an address-relocation artifact. Instruction count/opcodes are identical between target and base; only register assignment and one displacement pairing differ. unicorn_verdict for this symbol is EQUIVALENT (behaviorally identical), consistent with pure codegen-noise, not a logic bug.", "action": "permuter", "fix_sketch": "", "cascade_hint": "Function-local: this is a leaf float-heavy math routine (BSP/plane tests) with severe /O1 register-pressure churn from many local Vector3/plane temporaries. Other float-heavy leaf functions in Geo.cpp are plausible candidates for the same regalloc-permuter pattern, but this is not a class-layout or header issue that would explain unrelated near-misses."}
{"symbol": "??_GSharedGroup@@UAAPAXI@Z", "unit": "default/Instance", "pct": 99.88, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Only 2 real mismatches (the rest, e.g. `bl fn_822C6600` vs `bl ??1SharedGroup@@...`, are relocation/naming noise already ignored by normalized diff). Real diffs: idx3 `subi r30, r3, 0x28` (target/retail) vs `subi r30, r3, 0x20` (base/ours); idx5 mirrors it. This is the MSVC scalar-deleting-destructor's virtual-base (Hmx::Object, via `RndPollable : public virtual Hmx::Object`) this-adjustment constant, i.e. the byte offset of SharedGroup's own non-virtual-base data before the Object virtual base begins. Retail's constant is 8 bytes larger than ours -> our SharedGroup layout is 8 bytes short somewhere. Instance.h shows: `mGroup` @0x8(4B), `ObjPtr<WorldInstance> mPollMaster` @0xc (retail X360 ObjPtr size is documented as 0xc in Object.h, so mPollMaster should end at 0x18), but `std::list<RndPollable*> mPolls` is annotated `// 0x20` -- an unexplained/undeclared 8-byte gap (0x18-0x20) between mPollMaster and mPolls with no field covering it.", "action": "recon-needed", "fix_sketch": "In src/system/world/Instance.h's SharedGroup, insert a missing 8-byte member (or two 4-byte members) between `mPollMaster` (0xc, ObjPtr ends 0x18) and `mPolls` (0x20) to close the unexplained gap; verify via Ghidra/struct_db what actually lives there before committing a name (possible TU5 insert).", "cascade_hint": "Unit-wide: if SharedGroup's own layout is short by 8 bytes, every other SharedGroup member function/accessor in Instance.cpp that touches fields at or after the gap (mPolls and anything appended later) is a candidate for the same drift; worth checking other near-misses in default/Instance for the same +8/-8 constant pattern."}
{"symbol": "fn_823D0AFC", "unit": "default/CharClipSet", "pct": 99.9, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Single real mismatch: idx4 `lwz r3, 0x54(r31)` (target/retail) vs `lwz r3, 0x50(r31)` (base/ours) -- loads a DataArray* member 4 bytes further into the object in retail than in our build, then both sides pass it to a DataArray-destroy call (target's `fn_822270298` is retail's still-unmapped anonymous symbol for `??3DataArray@@SAXPAX@Z`, which our build correctly names -- that call-target difference itself is normalized-diff noise, not a real mismatch). `lookup_struct_offset` found no field at 0x50 or 0x54 for CharClipSet itself, so the owning class (likely a small helper/inner type in CharClipSet.cpp holding a `DataArray*`, not CharClipSet directly) is not yet identified in struct_db.", "action": "recon-needed", "fix_sketch": "Identify the exact class this destructor-ish helper belongs to (grep CharClipSet.cpp for a small type with a single DataArray* member destroyed here); once found, insert a missing 4-byte field before that DataArray* member to shift it from 0x50 to 0x54 (retail is 4 bytes ahead of us, i.e. we're missing a preceding member -- classic TU5 insert pattern).", "cascade_hint": "Likely unit-wide if the owning class is used elsewhere in CharClipSet.cpp/.h: any other function touching fields after this DataArray* pointer in that same class would show the same +4 offset drift."}
{"symbol": "?FillCompressedVertex@@YAXAAUCompressedVertex_Xbox@@ABVVert@RndMesh@@_N@Z", "unit": "default/Mesh", "pct": 99.85, "classification": "REGALLOC_PERMUTER", "confidence": "medium", "evidence": "8/139 mismatches: a register swap r28<->r29 (idx34-35, `rlwimi`) plus symmetric local-stack float offset swaps (+/-4, +/-12 on `lfs`/`stfs` at idx7,9,102,103,105,113) that pair up (e.g. -12 paired with +12, -4 paired with +4) rather than trending one direction the way a missing struct member would. These are local scratch-variable stack slots inside a leaf vertex-compression routine, not object-field displacements. Verdict tag from objdiff is MaybeFixable/permuter-recommended (~250 builds).", "action": "permuter", "fix_sketch": "", "cascade_hint": "Function-local: this is float-heavy leaf compression math (normal/tangent packing) with local temporaries reordered by the register allocator; unlikely to predict other unit near-misses unless other Mesh.cpp vertex-compression helpers share the same temp-heavy shape."}
{"symbol": "?_M_fill_insert_aux@?$vector@URecordedFrame@@V?$StlNodeAlloc@URecordedFrame@@@stlpmtx_std@@@stlpmtx_std@@AAAXPAURecordedFrame@@IABU3@ABU__false_type@2@@Z", "unit": "default/SkeletonClip", "pct": 99.87, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "Every one of the 13 real mismatches is the same constant, 336 bytes, appearing consistently: stack frame `stwu r1,-0x120,r1` (base/ours) vs `-0x270,r1` (target/retail) -- a 0x150(336)-byte frame delta -- and every `li`/`mulli`/`subi`/`addi` that encodes sizeof(RecordedFrame) shows `0x1c8` (456, our build) vs `0x78` (120, retail). Our src/system/gesture/SkeletonClip.h RecordedFrame struct is commented `// size 0x1c8` and, by hand-summing its fields (mFrameNumber+mElapsedMs+Vector3+Color+bool+`PaddedJointPos mJointPositions[kNumJoints=20]`(320B)+`int mJointTrackingState[20]`(80B)+mQualityFlags+mTrackingID+mSongSeconds), does legitimately total ~456B in our header -- but retail's actual compiled code for this same template instantiation treats RecordedFrame as only 120 bytes. That means our RecordedFrame is over-declared by ~336 bytes relative to what retail's binary actually uses (most likely the mJointPositions/mJointTrackingState arrays, ~400B, don't belong on this struct as declared, or belong in compressed/indirect form). The call-target differences shown (target-only `__uninitialized_copy<SongSequence::Entry*>`-mangled symbols vs base-only `__uninitialized_copy<RecordedFrame*>`/`__uninitialized_fill_n<RecordedFrame*>`) are ICF/COMDAT-folding naming noise (already excluded from the normalized diff_arg count) and not the primary bug -- the primary bug is the literal sizeof/frame-size constant.", "action": "recon-needed", "fix_sketch": "Re-derive RecordedFrame's true field layout from retail (120 bytes): likely mJointPositions[kNumJoints]/mJointTrackingState[kNumJoints] in src/system/gesture/SkeletonClip.h are wrongly declared inline on RecordedFrame and should be removed/replaced (e.g. a pointer/index into a separate compact joint buffer) before touching the `// size 0x1c8` comment -- do not hand-edit without cross-checking every RecordedFrame-touching function in SkeletonClip.cpp (LoadFrame, MakeSkeletonFrame, push_back growth) first.", "cascade_hint": "Strongly unit-wide: any function in SkeletonClip.cpp/.h that uses `sizeof(RecordedFrame)`, `std::vector<RecordedFrame>` growth/copy, or iterates/copies RecordedFrame elements will show the identical 336-byte-scaled drift; this single struct-layout fix is likely the root cause for most of default/SkeletonClip's other near-100% misses."}
{"symbol": "fn_82812218", "unit": "default/HamCharacter", "pct": 99.92, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "12-instr fn (outlined array-element String dtor). `lwz r11,0x84(r31); subi r11,r11,0x358; addi r3,r11,0x2e4` (target) vs `addi r3,r11,0x268` (ours) \u2014 same base arithmetic (load ptr, step back one 0x358-byte array element) but the in-element field offset differs by exactly 0x7c=124 bytes (diagnose confirms root cause = 'Stack/offset shift, dominant delta -124', fully explaining both diff_arg instrs incl. the call-target name swap fn_822B96A8 vs ??1String@@UAA@XZ). Direct HamCharacter offset lookups at 0x2e4/0x268 (mCampaignVODir, Character::mPollState) are spurious \u2014 the real address is computed relative to a reverse-indexed array element (stride 0x358), not HamCharacter's own layout, so the struct in question is an as-yet-unidentified ~856-byte HamCharacter array-element type whose members before this String field appear to differ by 124 bytes (likely several TU5 inserts, or one larger struct/array).", "fix_sketch": "Needs recon: identify the HamCharacter member array whose element type is destructed here (element size 0x358 bytes, this is a reverse-order per-element dtor call to that element's embedded String at offset 0x268 in our layout / 0x2e4 in retail). Once located, diff that element struct's header against rb3-Wii/dc3 to find the missing ~124 bytes of members inserted before the String field.", "action": "recon-needed", "cascade_hint": "Unit-wide: if this array-element type is used broadly in HamCharacter.cpp (ctor/dtor/Copy/Save/Load loops over the same array), the same 124-byte layout gap likely explains other HamCharacter near-misses touching that array, not just this one destructor helper."}
{"symbol": "fn_8271C17C", "unit": "default/Sfx", "pct": 99.91, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "11-instr fn (outlined vector<SampleInst*> dtor call). `lwz r11,0x94(r31); addi r3,r11,0x40` (target) vs `addi r3,r11,0x44` (ours) \u2014 single diff_arg, off:+4, diagnose confirms root cause 'Stack/offset shift dominant delta +4' fully explains both mismatches (offset + the paired call-target-name diff fn_82B69618 vs the named vector<SampleInst*> dtor). Sfx.h's own top-level `mSamples` vector is at 0x48 (src/system/synth/Sfx.h:69), not 0x40/0x44, so this is a different, unidentified struct reached via the pointer loaded from offset 0x94 of the outer object \u2014 its layout is short by 4 bytes relative to retail (one missing member before the vector field).", "fix_sketch": "Needs recon: find what struct/class is pointed to by the field at 0x94 in this Sfx-related object (likely a nested per-sound-effect record containing a vector<SampleInst*>); once identified, retail's vector field sits 4 bytes later, implying a single missing 4-byte member (int/float/pointer/bool+pad) inserted earlier in that struct.", "action": "recon-needed", "cascade_hint": "Likely function-local to this dtor helper unless the same nested struct's vector field is touched by other Sfx.cpp functions (ctor, push_back, iteration) \u2014 worth checking other Sfx near-misses for the same +4 pattern before concluding it's isolated."}
{"symbol": "?DoneEditing@EditSetlistPanel@@QAAXXZ", "unit": "default/band3/meta_band/EditSetlistPanel", "pct": 99.61, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "89-instr fn, only 4 diff_arg instructions, all in one contiguous block: `lwz [reg:r10->r11]` / `lwz [reg:r11->r10, reg:r10->r11]` / `addi [reg:r11->r10, reg:r11->r10]` / `stw [reg:r11->r10, reg:r10->r11]` at indices 57-60. objdiff's own pattern detector flags REGISTER_SWAP (1 pair, r10<->r11) and labels it RarelyHandFixable, recommending the permuter. Instruction count and opcodes are identical between target and base \u2014 pure register-number swap, no structural change.", "fix_sketch": "", "action": "permuter", "cascade_hint": "Function-local: an r10/r11 allocator flip on a 4-instruction local sequence (looks like a small local temp's load/increment/store around a call), not tied to any class layout \u2014 unlikely to systematically explain other near-misses in the unit, though the permuter sweep may reveal a shared source-ordering trigger if this pattern recurs on similar temp-heavy call sites in the same file."}
{"symbol": "??$__uninitialized_copy@PAUFrame@RhythmDetector@@PAU12@@stlpmtx_std@@YAPAUFrame@RhythmDetector@@PAU12@00ABU__false_type@0@@Z", "unit": "default/RhythmDetector", "pct": 99.92, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "24-instr templated copy loop over RhythmDetector::Frame*. `addi r29,r29,0x14` / `addi r30,r30,0x14` (target, both pointer advances) vs `addi r29,r29,0x10` / `addi r30,r30,0x10` (ours) \u2014 the loop's element-pointer increment is literally sizeof(Frame): retail=0x14 (20 bytes), ours=0x10 (16 bytes). The downstream branch targets also shift (beq 0x54 vs 0x5c, b 0x2c vs 0x34) purely as a byte-offset consequence of the same 4-byte size delta, not separate bugs. src/system/hamobj/RhythmDetector.h:17-21 defines `struct Frame { float mTime; /*0x0*/ std::vector<Vector3> mJointVelocities; /*0x4*/ }` \u2014 4 + 12 (stlport vector = 3 ptrs) = 0x10, matching our compiled size exactly, confirming retail's Frame is 4 bytes larger than our header models.", "fix_sketch": "src/system/hamobj/RhythmDetector.h: struct Frame is missing a 4-byte member (int/float/bool+pad/Symbol-lite) to bring sizeof from 0x10 to 0x14. Exact field/position needs a quick cross-check (rb3-Wii doesn't have RhythmDetector \u2014 Kinect-only feature \u2014 so no oracle there; check DC3 hamobj for an equivalent Kinect rhythm-frame struct, or Ghidra-decompile RhythmDetector::Record()/Analyze() call sites that construct/read Frame fields for a 5th field beyond mTime+velocities).", "action": "fix-now-cheap", "cascade_hint": "Unit-wide: RhythmDetector::Frame's sizeof is baked into every vector<Frame> operation across RhythmDetector.cpp (push_back, resize/reserve, copy-construct, this __uninitialized_copy, and the array destructor) \u2014 fixing the struct's size will very likely move several other near-100% functions in this unit to 100% simultaneously."}
{"symbol": "?push_back@?$vector@UPropertyFilter@CameraManager@@V?$StlNodeAlloc@UPropertyFilter@CameraManager@@@stlpmtx_std@@@stlpmtx_std@@QAAXABUPropertyFilter@CameraManager@@@Z", "unit": "default/CameraManager", "pct": 99.97, "classification": "PAIRING_TOOLING", "confidence": "high", "evidence": "objdiff itself reports 'Match: 100.0% normalized (99.6% raw)' \u2014 normalized mode (which ignores relocation/pairing-name noise) already scores this function as a full match. The single diff_arg is `addi r11,r11,0x30` (target) vs `addi r11,r11,0x14` (ours), which is the vector's finish-pointer advance-by-sizeof(element) after copy-construct. But the Function Call Diff shows target's overflow path calls `_M_insert_overflow_aux<PronunciationsLoc>` while ours calls `_M_insert_overflow_aux<CameraManager::PropertyFilter>` and `_Copy_Construct<CameraManager::PropertyFilter>` has no target-side counterpart at all \u2014 i.e. retail's machine code at this address was ICF-folded with a completely unrelated vector<PronunciationsLoc> push_back instantiation (sizeof(PronunciationsLoc)=0x30 explains the '0x30' immediate) and the target-symbol map/dtk labeled the merged blob with that instantiation's name. CameraManager::PropertyFilter itself (DataNode prop@0x0 + DataNode match@0x8 + int mask@0x10 = 0x14) matches rb3-Wii and dc3-decomp exactly \u2014 our struct is correct; the '0x30' is not a real size for PropertyFilter at all.", "fix_sketch": "", "action": "skip-mirage", "cascade_hint": "Function-local ICF-fold coincidence between two unrelated template instantiations (PropertyFilter vs PronunciationsLoc push_back) that happen to compile to identical code shape in retail; not expected to predict other CameraManager near-misses unless other push_back<T> instantiations in the unit hit the same fold."}
{"symbol": "??$__destroy_mv_srcs@PAV?$vector@VVector3@@V?$StlNodeAlloc@VVector3@@@stlpmtx_std@@@stlpmtx_std@@V12@@stlpmtx_std@@YAXPAV?$vector@VVector3@@V?$StlNodeAlloc@VVector3@@@stlpmtx_std@@@0@00@Z", "unit": "default/UIList", "pct": 99.95, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Only mismatch is instr[12]: target `addi r31, r31, 0x10` vs ours `addi r31, r31, 0xc` -- the per-element pointer stride while walking a range of `vector<Vector3, StlNodeAlloc<Vector3>>` objects. Retail's sizeof for this StlNodeAlloc-based vector object is 0x10 (16 bytes); our compiled STLport `_Vector_base<Vector3,StlNodeAlloc<Vector3>>` (EBO-collapsed `_STLP_alloc_proxy`) is the standard 0xc (12 bytes, 3 pointers). Note idx[11]'s call (`_Param_Construct<RecurseInfo>` vs our vector-deleting-dtor call) shows as 'equal' only because normalized diff ignores relocation-target differences (ICF ambiguity), not because they're truly the same call.", "action": "recon-needed", "cascade_hint": "This is the same family as the known per-TU rbtree +4-byte ABI split (RB3_RBTREE_0x1C in _tree.h) but manifesting on vector<T,StlNodeAlloc<T>> instead of map/set -- likely UIList.cpp needs an analogous per-TU vector-alloc-proxy size flag. Would predict near-misses in any other UIList.cpp function that constructs/destroys/copies vector<Vector3,StlNodeAlloc<Vector3>> ranges (or other StlNodeAlloc-vector<T> instantiations in this TU)."}
{"symbol": "??4?$vector@V?$map@HMU?$less@H@stlpmtx_std@@V?$StlNodeAlloc@U?$pair@$$CBHM@stlpmtx_std@@@2@@stlpmtx_std@@V?$StlNodeAlloc@V?$map@HMU?$less@H@stlpmtx_std@@V?$StlNodeAlloc@U?$pair@$$CBHM@stlpmtx_std@@@2@@stlpmtx_std@@@2@@stlpmtx_std@@QAAAAV01@ABV01@@Z", "unit": "default/CharClip", "pct": 99.9, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "8 mismatches, all the same constant swap: `li r30, 0x18`/`mulli r11,r25,0x18`/`addi r28,r28,0x18` (target/retail) vs `0x1c` (ours) -- sizeof(std::map<int,float,less<int>,StlNodeAlloc<pair<const int,float>>>). This is the vector<map<int,float>>::operator= for LightPreset-like mBlendSamples-style fields. CharClip.cpp compiles with `/DRB3_RBTREE_0x1C` (config/45410914/objects.json:272-277, added by commit 8bcd6720/parent for '+1 CharClip'), which blanket-grows every std::map/set tree in the TU to 0x1c. But retail wants THIS map<int,float> instantiation to stay 0x18 -- i.e. the blanket flag is wrong for this particular tree, while some other tree/set in CharClip.cpp evidently needed the +4 to gain the original point.", "action": "recon-needed", "cascade_hint": "Exactly matches the documented per-TU ODR-split caveat in src/system/stlport/stl/_tree.h:318-329 ('For TUs where retail std::map is 0x1c but std::set is 0x18 in the SAME TU... use RB3_MAP_0x1C instead') -- except here it looks like the *opposite* split (this map wants 0x18 while something else in the TU wanted 0x1c). Fix requires finding which specific tree instantiation in CharClip.cpp actually needs the pad and re-gating narrowly (RB3_MAP_0x1C or a type-specific specialization) instead of the blanket flag. Predicts identical near-misses in any other CharClip.cpp function touching vector<map<int,float>> mBlendSamples (ctor, dtor, resize, Save/Load)."}
{"symbol": "?push_back@?$vector@UEnvLightEntry@LightPreset@@V?$StlNodeAlloc@UEnvLightEntry@LightPreset@@@stlpmtx_std@@@stlpmtx_std@@QAAXABUEnvLightEntry@LightPreset@@@Z", "unit": "default/LightPreset", "pct": 99.94, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "instr[11]/[14]: target `li r5, 0x2c` (44) / `addi r11,r11,0x2c` vs ours `0x68` (104) -- sizeof(LightPreset::EnvLightEntry) used for the memcpy of the pushed element and the finish-pointer bump. Our compiled 0x68 is internally consistent with the header (Quat 0x10 + padded-Vector3 0x10 [Vector3 has a hidden `u32 PAD` for SIMD alignment, src/system/math/Vec.h:140] + Color 0x10 + float 0x4 + Type 0x4 + Matrix3(3x padded-Vector3) 0x30 = 0x68), matching LightPreset.h's own offset comments exactly. Retail's 0x2c (44) is far too small to include mRotation/mRange/mLightType at all -- 44 == Quat(16)+*unpadded* Vector3(12)+Color(16) exactly, i.e. retail's real struct here looks smaller/different (missing fields and/or no Vector3 SIMD pad) than what our header declares.", "action": "recon-needed", "cascade_hint": "Would predict near-misses in every other LightPreset.cpp function touching EnvLightEntry by value (Save/Load/Animate/operator!=, resize, other insert/erase helpers) since sizeof(EnvLightEntry) is baked into all of them. Worth first double-checking this isn't a target_symbol_map misidentification (retail VA mapped to the wrong template instantiation) before assuming the header itself is wrong for TU5."}
{"symbol": "fn_8249626C", "unit": "default/system/rndobj/CubeTex", "pct": 99.9, "classification": "EH_FUNCLET_MIRAGE", "confidence": "high", "evidence": "40-byte body, single mismatch at instr[0]: `subi r31, r12, 0x120` (target) vs `subi r31, r12, 0x130` (ours) -- the classic 'recover parent frame pointer from r12' funclet prologue. The only other instruction (the call) is completely unrelated between target and base: target calls `?FinishAsyncUnload@LoadMgr@@QAAXXZ`, base calls `??1FilePath@@UAA@XZ` (a FilePath destructor). Two structurally-identical funclet shells calling unrelated cleanup routines at slightly different frame offsets is a funclet-pairing artifact, not a real code difference. No unicorn probe data exists for this symbol (too small/excluded), consistent with an EH funclet rather than real logic.", "action": "skip-mirage", "cascade_hint": "Function-local artifact; not expected to predict other near-misses in CubeTex.cpp unless the unit has other similarly-shaped tiny EH funclet stubs nearby (worth a quick size/shape scan of CubeTex.cpp's other sub-99% entries if any exist)."}
{"symbol": "?IsInExclusionList@BandSongMgr@@QBA_NPBDH@Z", "unit": "default/band3/meta_band/BandSongMgr", "pct": 99.96, "classification": "CONSTANT_DIFF", "confidence": "high", "evidence": "Single mismatch at instr[21]: `cmplwi cr6, r7, 0x10` (target/retail) vs `cmplwi cr6, r7, 0x20` (ours) -- the outer do-while loop's exit bound, where r7 accumulates in steps of sizeof(ExclusionEntry)=8 (`addi r7,r7,0x8`, unmodified between target/base). Retail bounds at 0x10 (2 entries), ours bounds at 0x20 (4 entries). Our source (src/band3/meta_band/BandSongMgr.cpp:58-63,608) has `exclusionList[]` with 4 entries and `while (i < 4U)`, matching the rb3-Wii dev reference the tool surfaced verbatim -- but retail TU5 apparently only walks the first 2 entries. Strongly corroborated: `sqlite3 decomp.db` shows this symbol's unicorn verdict is DIVERGENT/unmapped_access_mismatch (stable_divergent), consistent with our code reading 2 extra (out-of-bounds-relative-to-retail's-intended-loop) array entries that retail's compiled code never touches.", "action": "fix-now-cheap", "fix_sketch": "In src/band3/meta_band/BandSongMgr.cpp: retail TU5's IsInExclusionList only iterates the first 2 exclusionList entries. Change the loop bound `while (i < 4U)` to `while (i < 2U)` (and/or trim exclusionList[] to its first 2 entries, {\"danicalifornia\",8} and {\"blackholesun\",3}) -- the rb3-Wii dev reference's 4-entry list (with the two later German-DLC entries) looks like a later/dev-build addition not present at this retail TU5 address. Verify entry order/content isn't itself TU-dependent before committing to which 2 entries retail keeps.", "cascade_hint": "Function-local: exclusionList/IsInExclusionList are self-contained to this one function in the unit, unlikely to explain other BandSongMgr.cpp near-misses."}
{"symbol": "??1FlowIf@@UAA@XZ", "unit": "default/FlowIf", "pct": 99.74, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "9 diff_arg out of 34 instrs, all immediate-only. Two clusters: (a) the vbase-vtable-fixup preamble reads/writes `-0x2c(r3)`/`-0x30(r3)` in target(retail) vs `-0x8c(r3)`/`-0x90(r3)` in base(ours) -- a consistent +0x60(96) magnitude shift on the vbtable-pointer lookup used to relocate the Hmx::Object virtual-base subobject and rewrite its vfptr; (b) the member-dtor `this` adjustors differ non-uniformly: `subi r3,r3,0x10`(target)/`0x1c`(base) [\u039412], `subi r3,r30,0x1c`(target)/`0x30`(base) [\u039420], `subi r3,r30,0x28`(target)/`0x2c`(base) [\u03944]. The 3 `bl` call-target NAME differences (retail unnamed vs our `DataNodeObjTrack`/`FlowNode` dtors) are already scored 'equal' by objdiff's normalized mode -- cosmetic/ICF naming, not the real mismatch. decomp.db confirms unicorn_verdict=DIVERGENT/class=call_arg, i.e. the *argument* (this-pointer address) computed for a real call genuinely differs at runtime, corroborating a real layout offset bug rather than pure naming noise.", "action": "recon-needed", "fix_sketch": "The non-uniform deltas (96/12/20/4 bytes) point at more than one member-size discrepancy compounding across FlowNode's own data members and/or DataNodeObjTrack, not a single inserted field. Needs a struct-recon pass on FlowNode.h/DataNodeObjTrack.h/Hmx::Object.h member lists against the true TU5 layout (start from FlowNode::mRunningNodes at 0x30 per lookup_struct_offset and walk forward) before attempting a source edit.", "cascade_hint": "FlowNode/DataNodeObjTrack are shared bases used by every Flow node subclass; any FlowIf method touching mValue1/mValue2 or the FlowNode vbase-fixup path (Handle/SyncProperty/Save/Copy/Load, plus other FlowNode-derived classes' destructors elsewhere) is likely showing the same drift."}
{"symbol": "??_GCharDriver@@UAAPAXI@Z", "unit": "default/CharDriver", "pct": 99.88, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "medium", "evidence": "Only 2 diff_arg of 17 instrs, both the scalar-deleting-destructor's this-adjustor: `subi r30,r3,0x24`(target/retail) vs `subi r30,r3,0x90`(base/ours), and the matching `addi r31,r30,0x24` vs `0x90`. This constant is the byte offset from the vtable-slot-passed `this` (one of CharDriver's multiple/virtual bases: RndHighlightable, CharWeightable, CharPollable) back to CharDriver's own object start. Ours is 0x6c (108) bytes larger than retail's, i.e. our compiled CharDriver-hierarchy layout is materially bigger than the real one at this subobject boundary. decomp.db unicorn_class=cap_exhausted (probe ran out of budget -- uninformative either way, not corroborating).", "action": "recon-needed", "fix_sketch": "108 bytes is too large to be a single small TU5 field insert -- audit CharDriver's own members (CharBones/CharClip/CharClipDriver/CharClipGroup-derived fields) and the RndHighlightable/CharWeightable/CharPollable/RndPollable mixin chain for a missing block (array, sub-struct, or an entire member) rather than assuming one primitive field.", "cascade_hint": "This is the this-adjustor baked into every CharDriver vtable thunk reached via RndHighlightable/CharWeightable/CharPollable, so it likely explains other near-misses across CharDriver.cpp wherever those interfaces are invoked polymorphically."}
{"symbol": "?push_back@?$vector@UNavItem@HamNavProvider@@V?$StlNodeAlloc@UNavItem@HamNavProvider@@@stlpmtx_std@@@stlpmtx_std@@QAAXABUNavItem@HamNavProvider@@@Z", "unit": "default/HamNavProvider", "pct": 99.97, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "Single diff_arg: after the copy-construct call, `addi r11,r11,0xc`(target/retail) vs `addi r11,r11,0x28`(base/ours) -- this is `finish += sizeof(NavItem)`. Retail's real HamNavProvider::NavItem is 12 bytes; ours compiles to 40 (0x28), matching the DC3-sourced header exactly. Checked src/system/hamobj/HamNavProvider.h vs dc3-decomp's copy: byte-for-byte identical struct NavItem with mLabel/mCheckboxState/mSongID/mStarMode/mEnabled/mHidden/mFormatArgs/mLabels(vector<Symbol>)/mSubListProvider. rb3-Wii oracle has no HamNavProvider at all (Xbox/hamobj-only class, per CLAUDE.md sourced from dc3-decomp), so there was no cross-check available before this. This is the CLAUDE.md-documented risk realized: 'dc3-decomp is newer than RB3 ... do not assume dc3's version is correct.'", "action": "recon-needed", "fix_sketch": "Trim HamNavProvider::NavItem in src/system/hamobj/HamNavProvider.h back to the 12-byte retail shape -- almost certainly just `Symbol mLabel; CheckboxMode mCheckboxState; int mSongID;` (mStarMode/mEnabled/mHidden/mFormatArgs/mLabels/mSubListProvider are DC3-only additions). Requires auditing every NavItem use-site in HamNavProvider.cpp (ctor, copy-ctor, dtor, Text(), any Save/Load/Copy) to remove references to the trimmed fields, and rebuilding to confirm no other TU depends on the removed members.", "cascade_hint": "NavItem is a data-only helper struct used throughout HamNavProvider.cpp (construction, copying, list rendering); this single header change plausibly fixes most/all other near-100% misses in this unit, not just push_back."}
{"symbol": "?DoFileRead@VorbisReader@@AAA_NXZ", "unit": "default/VorbisReader", "pct": 99.95, "classification": "STRUCT_OFFSET_DRIFT", "confidence": "high", "evidence": "5 diff_arg, all the same field accessed via lbz/stb: `0x11c(r3)`(target/retail) vs `0xef(r3)`(base/ours), a consistent \u0394=0x2d=45 bytes. lookup_struct_offset confirms our header's own annotation: 'VorbisReader::mFail (bool) at offset 0xef', i.e. our compiled layout matches our own header exactly -- the drift is that RETAIL's real mFail sits 45 bytes later. RB3-Wii reference source (fetched inline by run_objdiff) shows this is exactly the mFail member used in `if (mFail) return false;` / `mFail = mFile->Fail();` guards throughout DoFileRead. decomp.db unicorn_verdict=EQUIVALENT (the offset is used consistently so behavior in the sandbox is unaffected -- consistent with a pure layout/no-logic-change bug).", "action": "recon-needed", "fix_sketch": "Something ~45 bytes wide is missing from VorbisReader.h before mFail (declared // 0xef) -- candidates in the member list before it: mTerminating/mNumChannels/mSampleRate block, ogg_packet mPendingPacket/mHasPendingPacket, OggMap mOggMap, mHdrSize/mHdrBuf/mCtrState/mNonce[16]/mKeyMask[16]/unkec/unked/mEof. CriticalSection base checked and is self-consistent (mEntryCount 4 + RTL_CRITICAL_SECTION 0x1c = 0x20, matches MEM_OVERLOAD(0x20)), so the missing bytes are somewhere in VorbisReader's own declared members, not its bases.", "cascade_hint": "Every VorbisReader member declared after mFail (mVersion, mPcmBuffers, mLastGranulePos, mPcmReadPos) is shifted by the same 45 bytes, so this single struct-layout fix likely explains other near-misses across VorbisReader.cpp that touch those later fields, not just DoFileRead."}
{"symbol": "?ShapeDeltaBox@Waypoint@@AAAXABVVector3@@MMAAV2@@Z", "unit": "default/Waypoint", "pct": 99.08, "classification": "REGALLOC_PERMUTER", "confidence": "high", "evidence": "20 diff_arg of 124 instrs, all register operands on floating-point ops (fmadds/fsubs/fsel/fmuls/lfs/stfs), dominated by f10<->f12 swaps (6/11 in the tool's own REGISTER_SWAP cluster) plus an OFFSET_SWAP pair (0x4,0x8). No diff_op, no insert/delete -- identical instruction count and opcodes, only which physical FPR/GPR holds which live value differs. Tool's own verdict: 'MaybeFixable (Medium) -- Run the source permuter first.' decomp.db unicorn_class=cap_exhausted (uninformative).", "action": "permuter", "fix_sketch": "", "cascade_hint": "This is function-local floating-point expression/temporary scheduling noise specific to ShapeDeltaBox's dense vector-math body, not a class-layout issue -- unlikely to explain other Waypoint.cpp near-misses unless they share similarly dense inline float arithmetic."}
