#pragma once
// Forward-only stub — TrackPanelDirBase has too many transitive deps
// (GemTrackDir → TrackDir, BandTrack, etc.) to include in full here.
// TrackPanel.h and TrackPanelInterface.h use it only via pointer.
class TrackPanelDirBase;
